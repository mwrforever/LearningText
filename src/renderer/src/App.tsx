/**
 * 应用根组件（M0 脚手架）：仅静态结构，业务界面域自 M1 起按 B.1 features/ 细分。
 * React 组件保持纯函数（宪法 A.1-10），无副作用。
 */
export function App(): React.JSX.Element {
  return (
    <main>
      <h1>LearningText</h1>
    </main>
  );
}
