# LearningText 设计系统（M5 批次⓪）

> 全站视觉语言唯一事实来源：Task 3–14 以本章为样式依据，Task 15 以「组件形态语言」为对照验收面。
> tokens 落地于 `src/renderer/src/styles/theme.css`（`@theme` 语义映射 + `:root`/`.dark` 双套 shadcn 语义变量），本文档与代码不一致视为缺陷（宪法 §3 文档同步）。
> 技能驱动声明：本方案按 `ui-ux-pro-max:ui-ux-pro-max`（`--design-system` 模式检索）+ `ui-ux-pro-max:design-system`（三层 token 架构 / Tailwind 集成）工作流产出门控，非首个想法直出。

## 一、方向决策记录（双候选论证，Step 1）

### 1.1 候选来源（技能检索证据）

| 候选 | 检索查询 | 技能产出要点 |
| --- | --- | --- |
| A「冷静专业工具风」 | `developer tool document editor professional minimal --design-system` | 风格命中 Dark Mode（OLED）：石板蓝冷灰系（bg `#0F172A` / fg `#F8FAFC`）、绿 accent、低饱和中性色；字体建议 JetBrains Mono / IBM Plex Sans；明示「light not-recommended」 |
| B「柔和编辑器风」 | `writing notes editor calm soft warm productive --design-system` | 风格命中 Minimalism & Swiss：暖米底 `#FFFBEB`、stone 主色 `#78716C`、amber accent `#D97706`；字体建议 Varela Round / Nunito Sans；明暗双模式支持 |

### 1.2 逐维对照论证

| 维度 | 候选 A 冷静专业工具风 | 候选 B 柔和编辑器风 | 判定 |
| --- | --- | --- | --- |
| 信息密度 | 中性低饱和色不与内容争夺注意力，紧凑密度（标题栏 32px、行高紧凑）适配三栏 + 多标签高密度工作台 | 暖色底 + 舒展行高在树/标签等密集列表中放大行距，同屏信息量下降 | **A** |
| 可读性 | 冷灰中性面对白底 HTML 文档预览无色彩偏置 | 暖米壳与预览白底文档形成可见色缝，长会话暖色疲劳 | **A** |
| 暗色表现 | 石板蓝系为暗色而生（coding platform 语汇），dark 套原生协调 | 暖米系为亮色而生，dark 套需另行发明，双主题成本高 | **A** |
| 与 CM6 编辑区协调性 | CM6 默认亮色主题 / 规划中的 one-dark 均为中性冷调，石板灰壳无缝衔接 | amber accent 与语法高亮争色，暖底与编辑器中性 chrome 冲突 | **A** |
| 实现成本 | shadcn `baseColor: neutral` 惯例对齐，slate 轴即取即用，双主题各一套变量 | 需全量暖色覆盖 shadcn 默认变量，且暗色套从零设计 | **A** |

### 1.3 择定结论与对技能产出的裁剪（不是照单全收）

**择候选 A「冷静专业工具风」轴**：低饱和冷灰（slate）中性色、紧凑密度、直线小圆角（基准 4px）、克制动效。三处有意裁剪并留证：

1. **否决「仅暗色」倾向**：技能输出 A 明示 light not-recommended，但本项目需求为 light/dark 双主题强制（brief Interfaces），技能建议让位于项目约束——落法为 A 轴冷灰在 light/dark 各出一套语义变量（见 §三）。
2. **否决网络字体**（JetBrains Mono / IBM Plex Sans / Nunito Sans 均不采纳）：桌面工具零网络依赖，主文档 CSP `default-src 'self'` 无字体下载面，UI 字体栈沿用系统栈（brief Step 2 明确）。
3. **否决绿 accent**：全站当前无品牌强调色诉求，accent 语义收敛为「悬停表面」（shadcn 惯例），避免引入无业务承载的彩色焦点。

## 二、布局语言

- **三栏工作台网格**：应用壳 = 纵向 flex（标题栏 auto / 三栏区 1fr / 状态栏 auto 预留）；三栏区为 grid，列模板由 Workspace 内联注入（树栏 | 树分隔条 | 编辑器前分隔条 | 编辑器 | 预览分隔条 | 预览），折叠栏收 8px 窄条、编辑器折叠收 0px。分隔条为 4px 实体轨（`bg-border`，可拖拽者 `cursor-col-resize` + hover 提示）。
- **间距标尺（4 的倍数）**：一律使用 Tailwind spacing **整数步进**（1 步 = 4px，合法取值即 p-1/p-2/p-3/p-4… → 4/8/12/16…px）；**禁半步**（`x.5` 步进产生 6/10px 离尺值）与 3/5/7px 等非 4 倍数离尺值——判定规则机械：间距值必须能被 4 整除。面板内边距 8px（p-2）/12px（p-3）；工具栏与列表缝隙 4px（gap-1）；浮层内边距 16px（p-4）。
- **信息层级**：一级=面板标题栏（12px，muted 中性）；二级=内容主体（树节点/标签 12–14px）；三级=辅助信息（状态文案、占位态 12px muted-foreground）。层级只经「字号 + 颜色」表达，不加粗除激活态外的正文。
- **壳高**：应用壳占满视口（`h-screen`），面板内部滚动（树列表 `overflow-auto`），页面级不滚动。

## 三、色彩 tokens（shadcn 语义变量，双主题）

完整取值落地于 `theme.css`（本表为文档面拷贝，代码为准）。取值轴：冷灰 slate 系 + red-600 破坏色。

| 语义变量 | Light | Dark | 用途 |
| --- | --- | --- | --- |
| `--background` | `#F8FAFC` | `#0F172A` | 应用底 |
| `--foreground` | `#0F172A` | `#F8FAFC` | 正文 |
| `--card` / `--card-foreground` | `#FFFFFF` / `#0F172A` | `#1E293B` / `#F8FAFC` | 卡片面（预留） |
| `--popover` / `--popover-foreground` | `#FFFFFF` / `#0F172A` | `#1E293B` / `#F8FAFC` | 浮层（toast/重命名浮层） |
| `--primary` / `--primary-foreground` | `#1E293B` / `#F8FAFC` | `#E2E8F0` / `#0F172A` | 主操作钮（确认类） |
| `--secondary` / `--secondary-foreground` | `#E2E8F0` / `#1E293B` | `#1E293B` / `#F8FAFC` | 次级表面（预留） |
| `--muted` | `#F1F5F9` | `#1E293B` | 安静表面（标题栏底、hover 之外的弱底） |
| `--muted-foreground` | `#5F7085`（slate-500 加深约 8%） | `#94A3B8` | 次要文字 |
| `--accent` / `--accent-foreground` | `#E2E8F0` / `#0F172A` | `#1E293B` / `#F8FAFC` | 悬停表面（唯一 hover 底色） |
| `--destructive` / `--destructive-foreground` | `#B91C1C` / `#FFFFFF` | `#F87171` / `#0F172A` | 破坏性（删除钮预留、dirty 标记文本）；M5 打磨批次⑧ 双侧各深/浅一档，见 §九-1 |
| `--border` / `--input` | `#E2E8F0` | `#2B3A52` | 边框 / 输入框边 |
| `--ring` | `#64748B` | `#94A3B8` | 焦点环 / 分隔条 hover |

**用法边界**：组件禁 raw hex（`validate-tokens` 精神）；语义不够用时先扩 token 再用色。dark 模式 `--destructive` 作表面色时配 `--destructive-foreground`（深色字），不得配白字（白字对比不足，见下表）。

### 3.1 对比度自证（WCAG 2.x 相对亮度公式实算，正文级门槛 4.5:1）

| # | 前景/背景组合 | 对比度 | 判定 |
| --- | --- | --- | --- |
| 1 | light `--foreground` / `--background`（#0F172A / #F8FAFC） | 17.06 | 通过 |
| 2 | light `--muted-foreground` / `--background`（#5F7085 / #F8FAFC） | 4.84 | 通过 |
| 3 | light `--muted-foreground` / `--muted`（#5F7085 / #F1F5F9） | 4.63 | 通过（最坏 light 组合） |
| 4 | light `--foreground` / `--card`（#0F172A / #FFFFFF） | 17.85 | 通过 |
| 5 | light `--muted-foreground` / `--popover`（#5F7085 / #FFFFFF） | 5.07 | 通过 |
| 6 | light `--primary-foreground` / `--primary`（#F8FAFC / #1E293B） | 13.98 | 通过 |
| 7 | light `--secondary-foreground` / `--secondary`（#1E293B / #E2E8F0） | 11.87 | 通过 |
| 8 | light `--accent-foreground` / `--accent`（#0F172A / #E2E8F0） | 14.48 | 通过 |
| 9 | light `--destructive-foreground` / `--destructive`（#FFFFFF / #B91C1C） | 6.47 | 通过（批次⑧ 重算） |
| 10 | light `--destructive` 文本 / `--background`（#B91C1C / #F8FAFC） | 6.18 | 通过（dirty 标记用法） |
| 11 | dark `--foreground` / `--background`（#F8FAFC / #0F172A） | 17.06 | 通过 |
| 12 | dark `--muted-foreground` / `--background`（#94A3B8 / #0F172A） | 6.96 | 通过 |
| 13 | dark `--foreground` / `--card`（#F8FAFC / #1E293B） | 13.98 | 通过 |
| 14 | dark `--muted-foreground` / `--card`（#94A3B8 / #1E293B） | 5.71 | 通过 |
| 15 | dark `--primary-foreground` / `--primary`（#0F172A / #E2E8F0） | 14.48 | 通过 |
| 16 | dark `--destructive` 文本 / `--background`（#F87171 / #0F172A） | 6.45 | 通过（dirty 标记用法） |
| 17 | dark `--destructive-foreground` / `--destructive`（#0F172A / #F87171） | 6.45 | 通过（批次⑧ 重算） |
| 18 | light `--destructive` 文本 / `--accent` 悬停面（#B91C1C / #E2E8F0） | 5.25 | 通过（批次⑧ 增列——#10 之外的破坏色文本最坏表面） |
| 19 | dark `--destructive` 文本 / `--accent` 悬停面（#F87171 / #1E293B） | 5.29 | 通过（批次⑧ 增列——#16 之外的破坏色文本最坏表面） |

焦点环 / 分隔条 hover 属非文本 UI 边界（门槛 3:1）：light `#64748B`/`#F8FAFC` = 4.55、dark `#94A3B8`/`#0F172A` = 6.96，均通过。

## 四、字体阶梯

- **UI 字体栈（`--font-sans`，沿用系统栈）**：`system-ui, -apple-system, 'Segoe UI', 'Microsoft YaHei', sans-serif`——中文Windows 回落微软雅黑，macOS 走系统中文栈；不引入任何网络字体（§1.3-2）。
- **等宽栈（`--font-mono`，仅文档备案）**：`ui-monospace, 'Cascadia Mono', 'Segoe UI Mono', Menlo, Consolas, monospace`。CodeMirror 编辑区沿用 CM 自带等宽栈，本系统不覆盖（避免动编辑器内部度量）。
- **字号阶梯（固定三档，禁止体外值）**：

| 档 | 值 | 工具类 | 用途 |
| --- | --- | --- | --- |
| chrome | 12px | `text-xs` | 标题栏/工具栏/标签页/状态栏/toast |
| content | 14px | `text-sm` | 树节点、空态占位正文 |
| display | 16px | `text-base` | 预留（首启引导等未来场景） |

- **行高**：chrome 面 `leading-5`（20px，配 12px 字号）；内容面正文 1.5（`leading-normal`）。标签行、树行单行截断（`truncate`），禁换行撑高。

## 五、圆角 / 阴影 / 边框标尺

- **圆角（基准 `--radius: 0.25rem` = 4px，shadcn calc 派生）**：

| token | 值 | 用途 |
| --- | --- | --- |
| `rounded-sm` = `calc(var(--radius) - 2px)` | 2px | 树节点/标签/小图标钮 |
| `rounded-md` = `var(--radius)` | 4px | 工具钮、toast、输入框 |
| `rounded-lg` = `calc(var(--radius) + 4px)` | 8px | 浮层卡片（重命名浮层） |

- **阴影**：仅浮层允许投影（toast / 重命名浮层 `shadow-md`）；面板与内容区零阴影——空间分隔只由 1px 边框与分隔条承担（冷静工具风，拒绝装饰性投影）。
- **边框**：统一 1px `border-border`；三栏分隔为 4px 实体轨（`bg-border`）而非 1px 线——宽度即拖拽命中面（E2E `.lt-divider-*` 锚点依赖该轨存在）。

## 六、动效规范

### 6.1 tokens

| token | 值 | Tailwind 用法 | 用途 |
| --- | --- | --- | --- |
| instant | 100ms | `duration-100` | hover/焦点等即时反馈 |
| fast | 180ms | `duration-180` | 内联表面过渡（分隔条 hover、标题栏微变） |
| normal | 240ms | `duration-240` | 浮层/toast 出入场 |
| 缓动·标准 | `ease-out`（Tailwind 内建 = cubic-bezier(0, 0, 0.2, 1)） | `ease-out` | 一切入场 |
| 缓动·出场 | `ease-in`（Tailwind 内建 = cubic-bezier(0.4, 0, 1, 1)） | `ease-in` | 出场快于入场（100ms 级），无当前消费面，预留 |

（时长 tokens 以 CSS 变量 `--duration-instant/--duration-fast/--duration-normal` 落于 `theme.css` 的 `@theme`，作为数值唯一事实来源；工具类字面量必须与 token 数值一致。）

### 6.2 何物可动 / 何物不可动（红线）

**可动**：浮层与 toast 的出入场（fade + 轻位移/缩放）；折叠/展开的面板过渡（仅 CSS 过渡路径，不含拖拽）；hover 与焦点反馈（背景/颜色 100ms）。

**不可动**：
- 文本编辑一切路径——CM6 编辑区不叠加 UI 级过渡/动画（编辑器自身动画由库管理，外壳不得注入）；
- 滚动跟随与滚动位置恢复（`scrollDOM.scrollTop` 直写，无平滑滚动）；
- 拖拽直跟手——分隔条 `pointermove` 路径禁任何 transition（几何由内联 grid 模板逐帧更新）；
- 按钮按下态即时（无按压缩放）。

### 6.3 `prefers-reduced-motion` 降级策略

`theme.css` 基线层全局降级：reduce 用户的动画/过渡时长压至 0.01ms、迭代次数 1——等效「全部动效瞬时完成」，布局与信息呈现零损失。该降级为宪法级兜底，新增动画不得绕过（不使用 inline style 注入动画时长）。

### 6.4 本批次动效交付范围

toast 入场（fade + slide-in-from-bottom，240ms）；hover/焦点过渡（100ms）。toast 退场动画与面板折叠过渡需要动 D11 toast 队列逻辑/折叠渲染分支，超出「逻辑零变更」边界，本批次不做（后续任务承接）。

## 七、组件形态语言

### 7.1 shadcn 组件选型与用法边界

- **本批次零 shadcn 组件实例化**：Dialog/Popover 等组件依赖 `@radix-ui/*` 运行时，本任务禁新增 JS 依赖（体积红线）。后续任务按需 `shadcn add`，主题即接本表变量（components.json 已指向 `theme.css`）。
- `tw-animate-css` 已装配，为 shadcn 组件与自研浮层提供 `animate-in/out` 工具类（本批次 toast 已消费）。
- **类式 dark 变体**：`@custom-variant dark (&:is(.dark *))`（shadcn v4 惯例）已就位；`.dark` 类挂 `document.documentElement` 即全站切暗色。切换 UI 与跟随系统属后续任务，本批次只交付变量双套。

### 7.2 自研组件 Tailwind 形态规范（标准类串，禁体外发挥）

以下类串为各组件的**唯一规范形态**；`lt-*` 语义类名保留为**测试锚点与结构语义**（E2E/单测依赖，见 §7.3），视觉一律由 Tailwind 工具类承载，**禁止再向 `lt-*` 写声明式 CSS**。

**类拼接工具边界（D28 体积红线裁决）**：`cn()`（clsx + tailwind-merge）留给 shadcn 组件产码与「外部类覆盖内部类」的冲突合并场景；自研组件的简单条件拼接（无类冲突可能，如折叠标记、脏态换色）一律用模板字面量——本次实测引入 `cn` 运行时消费即令主 chunk 增加约 30KB（tailwind-merge 首次入包），与 Task 17「主 chunk ≤ 800KB」硬门禁直接冲突，故收敛。

| 组件 | 标准类串 |
| --- | --- |
| 应用壳 `main` | `flex h-screen flex-col overflow-hidden bg-background font-sans text-foreground` |
| 应用标题 `h1` | `m-0 shrink-0 select-none border-b border-border px-4 py-2 text-sm font-semibold tracking-wide` |
| 工作台 `lt-workspace` | `flex min-h-0 flex-1 flex-col`（等价替代原「无行模板 grid」的纵排语义，插槽行自然堆叠） |
| 三栏区 `lt-panes` | `grid min-h-0 flex-1 overflow-hidden`（列模板仍内联注入） |
| 面板 `lt-pane` | `flex min-h-0 min-w-0 flex-col bg-background` |
| 折叠窄条（追加 `lt-pane-collapsed`） | `flex w-full flex-col items-center gap-1 overflow-hidden py-1` |
| 面板标题栏 `lt-pane-titlebar` | `flex h-8 shrink-0 items-center justify-between gap-2 border-b border-border bg-muted/50 px-2`（标题 span：`text-xs font-medium text-muted-foreground`） |
| 折叠/展开图标钮 | `inline-flex h-5 w-5 items-center justify-center rounded-sm text-muted-foreground transition-colors duration-100 hover:bg-accent hover:text-accent-foreground` |
| 分隔条 `lt-divider-*` | `bg-border transition-colors duration-100 hover:bg-ring/50`；可拖拽者加 `cursor-col-resize` |
| 工具钮（树工具栏/编辑条通用） | `inline-flex h-6 items-center justify-center rounded-sm px-2 text-xs font-medium text-foreground transition-colors duration-100 hover:bg-accent hover:text-accent-foreground disabled:pointer-events-none disabled:opacity-40` |
| 主操作钮（确认类） | 工具钮基础 + `bg-primary text-primary-foreground hover:bg-primary/90`（去 hover-accent） |
| **树节点行**（行容器 + 主按钮 + 行内「⋯」钮，M5 批次④ Task 10 回填） | 行容器：`flex items-center`；主 `<button>`：`min-w-0 flex-1 truncate rounded-sm px-2 py-1 text-left text-sm text-foreground transition-colors duration-100 hover:bg-accent hover:text-accent-foreground disabled:pointer-events-none disabled:opacity-40 aria-current:bg-accent aria-current:font-medium aria-current:text-accent-foreground data-[preview-selected=true]:bg-accent/60 data-[move-target=true]:bg-primary/10 data-[move-target=true]:ring-1 data-[move-target=true]:ring-ring`（`data-[preview-selected=true]` = 媒体弱选中态：`bg-accent/60` 减半底色、不加粗，与强选中 `aria-current` 同色系弱一档，可区分「预览中」与「激活标签」并存两态；弱选中行可访问名追加「（预览中）」后缀，aria-current 通道不挪用。M5 批次⑦ Task 14 回填）；「⋯」触发钮（dropdown-menu，根行不渲染）：`inline-flex h-5 w-5 shrink-0 items-center justify-center rounded-sm text-muted-foreground transition-colors duration-100 hover:bg-accent hover:text-accent-foreground`（图标字符 span `text-xs leading-none` + `aria-hidden`；可访问名恒为「更多操作」独占锚——不含节点名防与既有行钮 getByRole name 子串锚串扰，行标识经 `data-node-id` 承载） |
| 树列表 | 顶层 `ul`：`m-0 min-h-0 flex-1 list-none overflow-auto p-2 text-sm`；嵌套 `ul`：`m-0 ml-4 list-none border-l border-border pl-1`；`li`：`list-none` |
| **标签页**（role=tab 钮） | `flex items-center gap-1 whitespace-nowrap rounded-sm px-2 py-1 text-xs text-muted-foreground transition-colors duration-100 hover:bg-accent hover:text-accent-foreground aria-current:bg-background aria-current:font-medium aria-current:text-foreground`；dirty 标记 span：`text-xs text-destructive`（与标签同档 12px，以破坏色区分、不加档外小字号）；关闭钮：`inline-flex h-4 w-4 items-center justify-center rounded-sm text-muted-foreground transition-colors duration-100 hover:bg-accent hover:text-foreground` |
| **状态栏**（预留插槽） | `flex h-7 shrink-0 items-center gap-3 border-t border-border bg-muted/50 px-3 text-xs text-muted-foreground`（gap-3/px-3 = 12px 整数步进，在 4px 标尺上；Task 5+ 消费） |
| **编辑条** `lt-editor-bar` | `flex items-center gap-2 border-t border-border bg-muted/50 px-2 py-1 text-xs`；保存态 span：`ml-auto` + dirty 时 `text-destructive`、否则 `text-muted-foreground` |
| 编辑器容器/宿主 | `lt-editor`：`flex min-h-0 flex-1 flex-col`；`lt-editor-host`：`min-h-0 flex-1`（不加 overflow，滚动语义仍归 CM6） |
| 空态占位 | `flex min-h-0 flex-1 items-center justify-center p-4 text-sm text-muted-foreground` |
| **toast** | 宿主 `lt-toasts`：`pointer-events-none fixed bottom-4 right-4 z-50 flex w-80 flex-col gap-2`；单条 `lt-toast`：`pointer-events-auto rounded-md border border-border bg-popover px-3 py-2 text-xs text-popover-foreground shadow-md duration-240 animate-in fade-in slide-in-from-bottom-2` |
| 输入框（重命名） | `min-w-0 flex-1 rounded-sm border border-input bg-background px-2 py-1 text-sm text-foreground outline-none focus-visible:ring-2 focus-visible:ring-ring` |
| 滑块（设置页，M5 Task 8 回填） | 原生 `input[type=range]`：`h-1 w-48 accent-primary`（accent 走语义主色，零新依赖——不引 radix slider）；值回显 `span`：`text-xs text-muted-foreground`（`{value}px`/`{value}ms`） |
| 设置开关（原生 checkbox，M5 Task 16 回填） | 原生 `input[type=checkbox]`：`accent-primary`（accent 走语义主色，零新依赖——不引 radix switch，jsdom/真实浏览器同语义）；行标签 `label`：`flex items-center gap-2 text-xs text-muted-foreground`（说明文案与控件同行；消费处：设置页备份区「每日自动备份」、维护区「启动时恢复工作区」） |

### 7.3 语义类名策略（迁移原则落法）

- 既有 `lt-*` 类名**全部保留**（单测断言 `lt-pane-collapsed`/`lt-pane-editor` 等、E2E 锚点 `.lt-tree-toolbar`/`.lt-divider-tree`/`.lt-preview-empty`/`iframe.lt-preview-frame` 依赖），仅删除其声明式 CSS 出处（原 `global.css`），视觉改由工具类内联承载。
- 该策略下**零结构变更、零 aria/行为变更、零测试断言变更**，无需 E2E 锚点对照表（本报告 §锚点核对为「逐测试核对通过」记录而非变更对照）。

### 7.4 RenameDialog 裁决

**保留容器 + Tailwind 化为浮层卡片形态**（不迁 shadcn Dialog）：① shadcn Dialog 依赖 `@radix-ui/react-dialog` 新增 JS 依赖，与本批次体积红线冲突；② Dialog 的焦点陷阱/Esc 关闭属行为面新增，违背「行为零变更」。落法：`lt-rename` 由文档流内条块升级为视口居中固定浮层（`fixed left-1/2 top-1/2 z-50 w-80 -translate-x-1/2 -translate-y-1/2 rounded-lg border border-border bg-popover p-4 shadow-md`，p-4 = §二浮层内边距 16px 标尺），`role="dialog"`、`aria-label="重命名"`、input 与确认/取消 aria 锚点逐字保留。无遮罩层（遮罩需新增 wrapper 节点，且点击遮罩关闭属行为新增）。后续任务若引入 radix 依赖可平移至 shadcn Dialog（变量已就绪）。

## 八、主题切换机制（契约预告）

- 双主题唯一开关：`document.documentElement.classList.toggle('dark')`；语义变量经 `:root`/`.dark` 两套取值 + `@theme inline` 映射（工具类直接引用 `var(--*)`），切换零重排即时生效。
- 本批次不落地切换入口（设置 UI/跟随系统属后续任务）；Task 3+ 的任何界面代码不得写死明暗条件色，一律消费语义 token。

## 九、打磨增量（M5 批次⑧ Task 15，taste-skill 双技能驱动）

> 本章记录打磨批次对基线语言的**增量修正与新增惯例**（审计发现基线可改进处，先回写本档再落码）；未列出的既有规范不变。打磨对照表（generic AI 味清单 → 逐项处置）见 `.superpowers/sdd/2026-09-19-M5-导入导出与辅助功能/polish-checklist.md`。

1. **`--destructive` 双主题各调一档（对比度缺口修复）**：审计发现破坏色文本的真正最坏表面是**悬停 accent 面**（标签 dirty 标记、下拉项 hover），原取值在该面不达正文门槛——light `#DC2626`/`#E2E8F0` 实算 3.92:1、dark `#EF4444`/`#1E293B` 实算 3.89:1（原 §3.1 只验了 background 面，属基线验算缺口）。修正为 light `#B91C1C`（red-700，accent 面实算 5.25:1）、dark `#F87171`（red-400，accent 面实算 5.29:1）；全组合重算见 §3.1 表 #9/#10/#16/#17 与新增 #18/#19，均较原值提升。`--destructive-foreground` 配对不变（dark 深字白字裁决照旧：白字对 #F87171 仅 2.77:1）。
2. **键盘焦点环基线（`button:focus-visible`）**：自研钮类（工具钮/树行/标签/「⋯」触发钮/关闭钮）原先仅 UA auto 轮廓叠加 50% 透明 ring 色，弱且与输入框的 2px ring 形态不一致；`theme.css` base 层新增 `button:focus-visible { outline: 2px solid var(--ring) }` 全站统一（shadcn 组件自带 focus-visible 样式在 utilities 层、级联优先不受影响）。ring 色非文本对比度 light 4.55 / dark 6.96（≥3:1）。新增交互钮如自带 ring 形态可不依赖本兜底，但不得写明暗条件色。
3. **shadcn 浮层消费侧覆写惯例**：radix 模板默认形态与本章标尺冲突处，经消费侧 `className` 覆写（cn/tailwind-merge「外部类覆盖内部类」合法场景，不改写 `components/ui/` 源码）：浮层内边距对齐 16px（`AlertDialogContent` 追加 `p-4` 覆写模板 `p-6`）；标题对齐字号三档 display 16px（`AlertDialogTitle` 追加 `text-base` 覆写模板 `text-lg` 18px 体外值）。后续 `shadcn add` 的组件消费同此惯例。
4. **数据数字 `tabular-nums` 惯例**：同屏随操作刷新的数字（进度计数、命中计数、时间戳、滑块值回显、备份大小）统一加 `tabular-nums`——比例数字逐帧刷新时宽度抖动是廉价感来源；静态标签文字不用。
5. **组合空态惯例**：面板级空态占位（编辑器/预览「未选中文件」「文档不可用」）由裸文字升级为「装饰性图标（aria-hidden，1.5 描边、60% 不透明度、24px）+ 原文案」纵向组合，`gap-2`；**文案与 `lt-*` 锚点零变更**（svg 不产生 textContent）。列表内空态（搜索/回收站）维持裸文字——占位面积小，加图标反而喧宾。
6. **树行「⋯」触发钮显形策略**：常态 `opacity-0`，行悬停（`group-hover`）/ 行内焦点（`group-focus-within`）/ 菜单展开（radix `data-[state=open]`）三态显形——每行常驻 20px 钮是恒定噪音；透明态仍占位且可命中，纯呈现零行为变化。列表行内「hover 显形」控件一律照此三态齐全，禁只挂 hover（键盘用户焦点不可达）。
7. **微命中面扩展惯例**：视觉 16px 的钮（标签关闭钮）经 `relative after:absolute after:-inset-1 after:content-['']` 将有效命中面外扩至 24px（伪元素参与命中测试且无视觉呈现）；外扩量以恰好吃满相邻 4px 缝隙为限，不得侵入相邻控件命中区。
8. **审计裁决留证（不做项）**：
   - 三栏折叠/展开的几何过渡（grid-template-columns 动画）：**不做**——列模板动画走 layout 路径，违反「仅 transform/opacity 合成器路径」性能红线（spec D24）；与 §6.4 既有台账一致，继续留待后续专项裁决；
   - toast 退场动画：**不做**——需动 D11 toast 队列逻辑（§6.4 台账原样保留）；
   - 设置覆盖层入场 fade-in 240ms：**已做**（仅 opacity 合成器路径，reduced-motion 全局降级覆盖）；
   - 图片预览加棋盘格/着色底：**不做**——预览保真红线（FR-RENDER-01），UI 不给用户文档内容着色；
   - window.confirm 原生弹窗换应用内对话框、toast 文案措辞（含 FR 编号入文案）、reveal 收敛窗口「（预览中）」后缀抑制：**行为级/aria 语义面，超打磨边界**，留前序台账。
