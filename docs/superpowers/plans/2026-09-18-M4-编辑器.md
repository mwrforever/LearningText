# M4 编辑器与保存管线实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **执行模式建议：SDD（superpowers:subagent-driven-development）**——主控会话派发子代理逐任务执行，每任务一个全新子代理 + 两阶段评审。所有工作在 **dev 分支**进行。spec 依据：`docs/superpowers/specs/2026-09-18-编辑器与保存管线-design.md`（v0.1，11 项拟定裁决 D1–D11 随本计划一并评审）；需求基线 docs/03 §4.5/§4.1/§4.4(P1)/§6.2；宪法 A.1-9（CodeMirror 不可变）/A.2/A.7、B.2/B.3、B.5 强制生效。**计划评审通过后**由用户确认执行模式（SDD 或 inline），再开工。

**Goal:** 交付 M4 编辑器联动里程碑：CodeMirror 6 真编辑器（多标签、HTML/CSS/JS 高亮、查找替换、自动缩进、括号匹配）+ 单写管线双计时器保存（尾沿去抖 + 最长挂起 + Ctrl/Cmd+S + unsaved-guard）+ 应用外壳（三栏折叠/宽度记忆、原生菜单与快捷键）+ M3 打磨批次收口（rename/move UI、路径新鲜与占位态、charset、订阅挂载一次）+ P1 二项（DevTools 右键、CSS 热替换）+ E2E 主链路与 NFR 计时。

**Architecture:** 沿用 M1–M3 分层。新增：`src/main/menu/`（菜单装配 + close 拦截）、协议层接收器注入（响应构造期）；渲染层决策全下沉纯函数（saveModel 保存状态机、tabModel 标签状态机、layoutModel 比例钳制、language 语言映射、codemirror 扩展工厂），`TabSessions`/`SaveController` 为无 React 依赖的会话/管线管理器（Workspace useRef 持有、组件消费），EditorPanel 换 CodeMirror 内核且三栏结构与插槽不重排（M3 接缝）。settings schemaVersion 2 additive 迁移（M3 §5 预留接缝首次落地）。

**Tech Stack:** CodeMirror 6（2026-09-18 registry 实查：@codemirror/state 6.7.5 / view 6.43.12 / commands 6.11.1 / language 6.12.4 / search 6.7.2 / lang-html 6.4.12 / lang-css 6.3.1 / lang-javascript 6.2.5，`-E` 精确锁定）· Electron 44.3.x · better-sqlite3 13.0.3 · zod 4.6.5 · TypeScript 6.0.3 · React 19.3.x · Vitest 5.0.0（jsdom）· Playwright 1.63.x `_electron`。

## Global Constraints（spec + 宪法摘录，每任务隐含遵守）

- 全部代码/注释/测试名/日志**中文**；UTF-8 无 BOM、LF 行尾；Conventional Commits 中文；每任务一次提交，测试与实现同提交。
- TS strict 全家桶 + `noUncheckedIndexedAccess`；禁 `any`；IPC 边界禁 `!`/`as`（测试期 `as unknown as` 桩适配与「超集信息+行注释」的 `as` 例外同 M2/M3 先例）。
- **渲染期禁写 ref**（M3 终审先例 75647f2）：ref 赋值只能在 effect / 事件处理器内。
- CodeMirror 习语（宪法 A.1-9）：`EditorState`/`Text`/decoration 集合视为不可变值，变更一律 transaction/`setState`/新建状态，禁原地修改；扩展封装为工厂函数；换文档新建状态不复用。
- 资源成对（M1/M3 教训）：`EditorView.destroy()`、`onShellCommand`/`onVfsChanged` 订阅 cleanup 解绑、`SaveController.dispose()` 清计时器、测试临时目录 afterAll 清理——均须有断言。
- **覆盖率门禁禁止下调**（A.6-3）：`src/main/**`（新增 menu 入此 glob 自动为核心）100%、`src/shared/**`/`src/preload/**` 100%、全局 ≥80%；渲染层 saveModel/tabModel 等纯函数以单测 100% 支撑全局线。
- 无死分支纪律：不写运行时不可达分支；下标访问收窄用语义等价结构（`for...of`/解构默认值）而非 `?? ''` 死侧（M3 覆盖率教训）。
- IPC 模式：渲染→主一律 invoke/handle；主→渲染命令走 `webContents.send` + 可辨识联合；契约单一来源 `src/shared/`；渲染端不感知通道字符串（A.7）。
- 不新增错误码：设置载荷非法 `E_IPC_BAD_PAYLOAD`；VFS 语义复用 M1 既有码。
- 广播契约不变：`VfsChangedBroadcast = { rev, event }`（M4 消费侧按此形态，跨里程碑接口）。
- 交付边界（spec §1）：滚动同步、搜索 UI、图片预览、工作区恢复、回收站/设置页 UI 一律不做（M5）；P1 只做 DevTools 与 CSS 热替换。

## File Structure（文件清单与职责边界）

```text
src/shared/
├── settings-contract.ts        # [修改] schemaVersion 2：editor.autoSaveMs + shell.layout；v1 遗留 schema 与迁移函数
├── shell-contract.ts           # [新建] ShellCommand 可辨识联合（new-file/new-dir/save/confirm-close）
├── ipc.ts                      # [修改] shell:command / shell:force-close / vfs:get 通道常量
├── window-api.ts               # [修改] onShellCommand / forceClose / getNode 方法声明
├── vfs-contract.ts             # [修改] NodeMetaResponse（vfs:get 响应，如契约文件尚无则补）
src/main/
├── menu/menu.ts                # [新建] 菜单模板装配（命令单通道下发）+ close 拦截装配
├── ipc.ts                      # [修改] shell:force-close 注册（deps.requestClose 注入）
├── app.ts                      # [修改] 菜单装配 + close 拦截 + requestClose 供给 + context-menu 检查元素
├── protocol/vfsProtocol.ts     # [修改] text/* charset=utf-8；text/html 200 接收器只读注入
├── settings/settingsService.ts # [修改] v1→v2 迁移例程（启动迁移 + 原子回写）
├── vfs/vfsService.ts           # [修改] getNode(id)（复用既有行→NodeMeta 映射；实施者先读该文件对齐）
src/preload/index.ts            # [修改] onShellCommand / forceClose / getNode 包装
src/renderer/src/features/
├── editor/
│   ├── saveModel.ts            # [新建] 保存管线纯状态机（双计时器时点 + 写串行，核心 100%）
│   ├── saveController.ts       # [新建] 管线控制器（per-tab 计时器句柄 + 注入式写回调 + dispose）
│   ├── tabSessions.ts          # [新建] per-tab EditorState/滚动 会话管理器
│   ├── codemirror.ts           # [新建] 扩展工厂（curated 集）+ createEditorState 工厂
│   ├── language.ts             # [新建] mime → LanguageSupport 映射纯函数
│   └── EditorPanel.tsx         # [修改] 换 CodeMirror 内核（props 面演进为会话式，三栏接缝不重排）
├── workspace/
│   ├── tabModel.ts             # [新建] 标签列表状态机（开/关/激活/上限/meta 同步）
│   ├── layoutModel.ts          # [新建] 折叠/宽度比例纯函数（clamp + 指针→比例）
│   ├── TabBar.tsx              # [新建] 标签条（dirty 圆点/关闭钮/aria）
│   └── Workspace.tsx           # [修改] tabs/activeTab 会话中枢 + 命令 dispatch + guard + 布局态 + move 模式
├── preview/PreviewPanel.tsx    # [修改] 订阅挂载期一次（nodeRef）+ written 前 vfs:get 反查占位态 + css 热替换触发
├── tree/TreePanel.tsx          # [修改] 重命名/移动入口 + move 模式点选语义
├── tree/RenameDialog.tsx       # [新建] 行内重命名模态
├── ui/Toast.tsx                # [新建] 最小单例 toast 队列（错误反馈承载）
src/renderer/index.html         # [修改] 既有 CSP 不动（无新指令需求；编辑器为同源脚本）
tests/
├── unit/shared/settings-contract.test.ts   # [修改] v2 边界 + 迁移函数
├── unit/settings/settingsService.test.ts   # [修改] v1→v2 迁移回写
├── unit/editor/saveModel.test.ts           # [新建] 状态机全转换
├── unit/editor/saveController.test.ts      # [新建] fake timers 全链路 + dispose
├── unit/editor/language.test.ts            # [新建] 映射表
├── unit/workspace/tabModel.test.ts         # [新建] 标签状态机
├── unit/workspace/layoutModel.test.ts      # [新建] clamp/比例
├── unit/renderer/editor-panel.test.tsx     # [新建] CM 冒烟（建/输/换会话/消挂载）
├── unit/renderer/tab-bar.test.tsx          # [新建] TabBar 冒烟
├── unit/main/menu.test.ts                  # [新建] 模板结构 + 命令转发
├── unit/main/ipc.test.ts                   # [修改] force-close 接线 + deps.requestClose
├── unit/preload/index.test.ts              # [修改] 成员清单 + 新包装
├── unit/protocol/vfsParse.test.ts          # [不动]（charset 在协议层，单测无涉）
├── integration/protocol/vfs-protocol.test.ts # [修改] charset 头 + 接收器注入矩阵（304/206/HEAD/css 不注入）
├── integration/settings/settings 迁移并入 unit（真实临时文件已在该测试形态）
├── integration/vfs（getNode 并入既有 vfs 集成文件或新增，实施者按 tests/integration 现状就近）
└── e2e/editor.spec.ts                      # [新建] 主链路/保存管线/多标签/折叠记忆/菜单/热替换/guard/5MB 计时
```

注：各任务 Modify 锚点以语义（标识符/注释）定位，行号会漂移；「实施者适配」注按注落地，不得自创键名。

---

### Task 1: 契约与通道基座（settings v2 迁移 + vfs:get + shell 命令通道）

**Files:**
- Modify: `src/shared/settings-contract.ts`、`src/shared/ipc.ts`、`src/shared/window-api.ts`、`src/shared/vfs-contract.ts`（如缺 NodeMetaResponse）、`src/preload/index.ts`、`src/main/settings/settingsService.ts`、`src/main/vfs/vfsService.ts`、`src/main/ipc.ts`
- Modify: `tests/unit/shared/settings-contract.test.ts`、`tests/unit/settings/settingsService.test.ts`、`tests/unit/preload/index.test.ts`、`tests/unit/main/ipc.test.ts`、getNode 集成用例就近并入既有 vfs 集成文件

**Interfaces:**
- Consumes: M3 `handleWith`（origin+zod+Result）、`NodeIdRequest` 既有 zod schema、`E_VFS_NOT_FOUND`、设置服务既有原子写
- Produces（后续任务消费，签名逐字）:
  - `SettingsData = { schemaVersion: 2; preview: { debounceMs }; editor: { autoSaveMs }; shell: { layout: ShellLayout } }`、`DEFAULT_SETTINGS`、`DEFAULT_LAYOUT: ShellLayout`、`SettingsSchemaV1`、`migrateV1ToV2(legacy): SettingsData`
  - `IPC.vfsGet = 'vfs:get'`、`IPC.shellCommand = 'shell:command'`、`IPC.shellForceClose = 'shell:force-close'`
  - `WindowApi.getNode(request: NodeIdRequest): Promise<Result<NodeMeta>>`（未找到 → `E_VFS_NOT_FOUND`）
  - `WindowApi.onShellCommand(callback: (command: ShellCommand) => void): () => void`、`WindowApi.forceClose(): Promise<Result<null>>`
  - `ShellCommand = { type: 'new-file' } | { type: 'new-dir' } | { type: 'save' } | { type: 'confirm-close' }`
  - `IpcHandlerDeps` 增 `requestClose(): void`（Task 6 接线；本任务先补 deps 字段与通道注册）

- [ ] **Step 1: 写失败测试——settings v2 契约与迁移**

`tests/unit/shared/settings-contract.test.ts` 在既有用例基础上替换（保留 describe 骨架、断言全面改 v2）：

```ts
// 设置契约边界（M4 spec §7 D4）：schemaVersion 2、debounceMs 100–2000、autoSaveMs 1000–60000、
// layout 比例 0.15–0.6、strict 多余字段拒；v1→v2 迁移补默认域
import { describe, expect, it } from 'vitest';
import {
  DEFAULT_LAYOUT,
  DEFAULT_SETTINGS,
  SETTINGS_SCHEMA_VERSION,
  SettingsSchema,
  SettingsSchemaV1,
  migrateV1ToV2,
} from '../../../src/shared/settings-contract';

describe('设置契约 schema（v2）', () => {
  it('默认值合法且版本号一致：autoSaveMs 默认 3000、布局默认全展开', () => {
    expect(SETTINGS_SCHEMA_VERSION).toBe(2);
    expect(SettingsSchema.safeParse(DEFAULT_SETTINGS).success).toBe(true);
    expect(DEFAULT_SETTINGS.editor.autoSaveMs).toBe(3000);
    expect(DEFAULT_SETTINGS.shell.layout).toEqual(DEFAULT_LAYOUT);
  });

  it('autoSaveMs 闭区间与整数约束：999/60001/小数拒，1000/60000 收', () => {
    const ok = (ms: number) => ({
      schemaVersion: 2,
      preview: { debounceMs: 300 },
      editor: { autoSaveMs: ms },
      shell: { layout: DEFAULT_LAYOUT },
    });
    expect(SettingsSchema.safeParse(ok(1000)).success).toBe(true);
    expect(SettingsSchema.safeParse(ok(60000)).success).toBe(true);
    expect(SettingsSchema.safeParse(ok(999)).success).toBe(false);
    expect(SettingsSchema.safeParse(ok(60001)).success).toBe(false);
    expect(SettingsSchema.safeParse(ok(1500.5)).success).toBe(false);
  });

  it('layout 比例 0.15–0.6 越界拒；schemaVersion 非 2 / 缺域 / 多余字段一律拒', () => {
    const badLayout = {
      schemaVersion: 2,
      preview: { debounceMs: 300 },
      editor: { autoSaveMs: 3000 },
      shell: { layout: { ...DEFAULT_LAYOUT, treeWidthRatio: 0.7 } },
    };
    expect(SettingsSchema.safeParse(badLayout).success).toBe(false);
    expect(SettingsSchema.safeParse({ schemaVersion: 1, preview: { debounceMs: 300 } }).success).toBe(false);
    expect(
      SettingsSchema.safeParse({ ...DEFAULT_SETTINGS, editor: { autoSaveMs: 3000 }, extra: 1 }).success,
    ).toBe(false);
  });

  it('v1 遗留 schema 收 preview 域；migrateV1ToV2 补默认域并升版本', () => {
    const legacy = { schemaVersion: 1 as const, preview: { debounceMs: 500 } };
    expect(SettingsSchemaV1.safeParse(legacy).success).toBe(true);
    const migrated = migrateV1ToV2(legacy);
    expect(migrated).toEqual({
      schemaVersion: 2,
      preview: { debounceMs: 500 },
      editor: { autoSaveMs: 3000 },
      shell: { layout: DEFAULT_LAYOUT },
    });
    expect(SettingsSchema.safeParse(migrated).success).toBe(true);
  });
});
```

- [ ] **Step 2: 写失败测试——settingsService v1→v2 迁移**

`tests/unit/settings/settingsService.test.ts`：保留既有 describe（`'文件缺失回退默认值且不告警'`、`'损坏 JSON warn 后回退默认'` 用例不动，断言值随 DEFAULT_SETTINGS 结构变化自动适配——toEqual 全量比对），改造与追加：

```ts
  it('schemaVersion 不识别（v3）warn 回退默认', () => {
    writeFileSync(file, JSON.stringify({ schemaVersion: 3, preview: { debounceMs: 100 } }), 'utf8');
    const s = createSettingsService({ settingsFile: file });
    expect(s.get()).toEqual(DEFAULT_SETTINGS);
    expect(warnSpy).toHaveBeenCalledTimes(1);
  });

  it('v1 文件启动静默迁移：get 得 v2 全量 + 原子回写 + info 一次', () => {
    const infoSpy = vi.spyOn(console, 'info').mockImplementation(() => {});
    writeFileSync(file, JSON.stringify({ schemaVersion: 1, preview: { debounceMs: 500 } }), 'utf8');
    const s = createSettingsService({ settingsFile: file });
    expect(s.get()).toEqual(migrateV1ToV2({ schemaVersion: 1, preview: { debounceMs: 500 } }));
    expect(JSON.parse(readFileSync(file, 'utf8'))).toEqual(s.get()); // 回写落盘
    expect(infoSpy).toHaveBeenCalledTimes(1);
    infoSpy.mockRestore();
  });
```

（import 追加 `SettingsSchemaV1`？不需要——迁移函数经 `migrateV1ToV2` 断言即可；`DEFAULT_SETTINGS` 已 import。）

- [ ] **Step 3: 写失败测试——vfs:get 与 force-close 接线（ipc.test 追加）**

`tests/unit/main/ipc.test.ts` 追加（桩/工具复用既有 `makeVfsStub`/`fakeEvent`，vfs 桩增 `getNode: vi.fn(() => ({ ok: true, value: metaLike }))`——形态随既有桩适配）：

```ts
// vfs:get 通道：单节点反查（rename/move 后 meta 新鲜化基座，M4 spec §6.1）
describe('vfs:get 通道接线', () => {
  it('getNode 命中返回 NodeMeta；未找到透传 E_VFS_NOT_FOUND', () => {
    handlers.clear();
    const vfs = makeVfsStub();
    registerIpcHandlers({
      allowedOrigins: ['app://bundle'], vfs, search: makeSearchStub(), settings: makeSettingsStub(),
      broadcast: vi.fn(), requestClose: vi.fn(),
    });
    vfs.getNode = vi.fn(() => ({ ok: true, value: { id: 2, parentId: 1 } }));
    const ok = handlers.get(IPC.vfsGet)?.(fakeEvent('app://bundle'), { nodeId: 2 }) as {
      ok: boolean; value: { id: number };
    };
    expect(ok.ok).toBe(true);
    expect(ok.value.id).toBe(2);
    vfs.getNode = vi.fn(() => ({ ok: false, error: { code: 'E_VFS_NOT_FOUND', message: '不存在' } }));
    const miss = handlers.get(IPC.vfsGet)?.(fakeEvent('app://bundle'), { nodeId: 99 }) as {
      ok: boolean; error: { code: string };
    };
    expect(miss.ok).toBe(false);
    expect(miss.error.code).toBe('E_VFS_NOT_FOUND');
  });
});

// shell:force-close：guard 放行唯一通道（M4 spec §2.3）
describe('shell:force-close 接线', () => {
  it('payload 非 null 拒 E_IPC_BAD_PAYLOAD；合法调用转发 deps.requestClose', () => {
    handlers.clear();
    const requestClose = vi.fn();
    registerIpcHandlers({
      allowedOrigins: ['app://bundle'], vfs: makeVfsStub(), search: makeSearchStub(),
      settings: makeSettingsStub(), broadcast: vi.fn(), requestClose,
    });
    const bad = handlers.get(IPC.shellForceClose)?.(fakeEvent('app://bundle'), { x: 1 }) as {
      ok: boolean; error: { code: string };
    };
    expect(bad.ok).toBe(false);
    expect(bad.error.code).toBe(E_IPC_BAD_PAYLOAD);
    const ok = handlers.get(IPC.shellForceClose)?.(fakeEvent('app://bundle'), null) as { ok: boolean };
    expect(ok.ok).toBe(true);
    expect(requestClose).toHaveBeenCalledTimes(1);
  });
});
```

（既有全部 `registerIpcHandlers` 调用点补 `requestClose: vi.fn(),`；`E_VFS_NOT_FOUND` 若 shared 有错误码常量则用常量。）

- [ ] **Step 4: 写失败测试——preload 成员清单**

`tests/unit/preload/index.test.ts`：成员清单在 `'settingsGet', 'settingsSet',` 后插 `'getNode', 'forceClose',` 与 `'onShellCommand',`（onVfsChanged 前）；`channelCases` 追加 `['getNode', IPC.vfsGet]`、`['forceClose', IPC.shellForceClose]`；订阅类断言区追加 onShellCommand 同款（对齐 onVfsChanged 既有断言形态）；文件头注释通道范围措辞同步。

- [ ] **Step 5: 运行确认失败**

Run: `npm run test:unit -- tests/unit/shared/settings-contract.test.ts tests/unit/settings tests/unit/main/ipc.test.ts tests/unit/preload/index.test.ts`
Expected: FAIL（v2 契约不存在 / getNode·requestClose 未注册 / preload 无新成员）。

- [ ] **Step 6: 实现 settings-contract v2（修改锚点：全文结构替换，保留文件头注释并更新为 M4 口径）**

```ts
/**
 * 设置域 IPC 契约（宪法 A.7-5 单一来源，M4 spec §7 D4）：schemaVersion 2 在 v1（M3 仅
 * preview.debounceMs）之上 additive 扩展 editor.autoSaveMs 与 shell.layout；迁移例程在
 * settingsService 启动装载期执行（v1 读入 → 补默认域 → 原子回写）。
 * 校验失败由 handler 统一映射 E_IPC_BAD_PAYLOAD。
 */
import { z } from 'zod';

/** 设置 schema 版本号：v1（M3）→ v2（M4 additive）；不识别版本回退默认值 */
export const SETTINGS_SCHEMA_VERSION = 2;

/** 三栏布局态（M4 spec §5.1）：折叠三态 + 树/预览宽度比例（编辑器自适应占余） */
export const ShellLayoutSchema = z.strictObject({
  treeCollapsed: z.boolean(),
  editorCollapsed: z.boolean(),
  previewCollapsed: z.boolean(),
  treeWidthRatio: z.number().min(0.15).max(0.6),
  previewWidthRatio: z.number().min(0.15).max(0.6),
});
export type ShellLayout = z.infer<typeof ShellLayoutSchema>;

/** 布局出厂默认：全展开，树 1/4、预览 0.4（编辑器自适应） */
export const DEFAULT_LAYOUT: ShellLayout = {
  treeCollapsed: false,
  editorCollapsed: false,
  previewCollapsed: false,
  treeWidthRatio: 0.25,
  previewWidthRatio: 0.4,
};

export const SettingsSchema = z.strictObject({
  schemaVersion: z.literal(SETTINGS_SCHEMA_VERSION),
  /** 预览域：编辑→写库去抖（FR-RENDER-03），M3 既有 */
  preview: z.strictObject({
    debounceMs: z.number().int().min(100).max(2000),
  }),
  /** 编辑器域（M4）：自动保存最长相邻写间隔（FR-EDIT-02「默认 3s」的精确语义） */
  editor: z.strictObject({
    autoSaveMs: z.number().int().min(1000).max(60000),
  }),
  /** 外壳域（M4）：三栏折叠与宽度记忆（FR-SHELL-01） */
  shell: z.strictObject({ layout: ShellLayoutSchema }),
});
export type SettingsData = z.infer<typeof SettingsSchema>;

/** v1 遗留 schema（迁移入口专用；v1 校验失败不告警，交由 v2 尝试与迁移链） */
export const SettingsSchemaV1 = z.strictObject({
  schemaVersion: z.literal(1),
  preview: z.strictObject({
    debounceMs: z.number().int().min(100).max(2000),
  }),
});
export type SettingsDataV1 = z.infer<typeof SettingsSchemaV1>;

/** v1 → v2 迁移：preview 保留用户值，editor/shell 补出厂默认（M4 spec §7） */
export function migrateV1ToV2(legacy: SettingsDataV1): SettingsData {
  return {
    schemaVersion: SETTINGS_SCHEMA_VERSION,
    preview: legacy.preview,
    editor: { autoSaveMs: 3000 },
    shell: { layout: DEFAULT_LAYOUT },
  };
}

/** settings:get 请求形态：无参通道沿 system:ping 先例传 null（preload 包装侧固定） */
export const SettingsGetRequestSchema = z.null();

/** 出厂默认设置（文件缺失/损坏/版本不识别时的回退值） */
export const DEFAULT_SETTINGS: SettingsData = {
  schemaVersion: SETTINGS_SCHEMA_VERSION,
  preview: { debounceMs: 300 },
  editor: { autoSaveMs: 3000 },
  shell: { layout: DEFAULT_LAYOUT },
};
```

- [ ] **Step 7: 实现 settingsService 迁移例程（修改锚点：装载 try 块 + 抽原子写）**

`src/main/settings/settingsService.ts`：把 `set` 内的写盘两行抽为私有函数并复用；装载链改为「v2 直读 → v1 迁移 → warn 回退」三级：

```ts
// —— 装载 try 块内替换为 ——
    if (existsSync(deps.settingsFile)) {
      const raw = JSON.parse(readFileSync(deps.settingsFile, 'utf8')) as unknown;
      const asV2 = SettingsSchema.safeParse(raw);
      if (asV2.success) {
        cached = asV2.data;
      } else {
        // v2 不中先试 v1 迁移（M4 spec §7）：成功则补默认域原子回写，失败才告警回退
        const asV1 = SettingsSchemaV1.safeParse(raw);
        if (asV1.success) {
          cached = migrateV1ToV2(asV1.data);
          writeSettings(cached);
          console.info('[settings] 配置已从 schemaVersion 1 迁移至 2');
        } else {
          console.warn('[settings] 配置文件校验失败，回退默认值', asV2.error.name);
        }
      }
    }
```

```ts
  /** 原子落盘（tmp + 同目录 rename）+ 内存缓存更新 + info（调用方保证已过 zod 闸） */
  function writeSettings(data: SettingsData): void {
    const tmp = deps.settingsFile + '.tmp';
    writeFileSync(tmp, JSON.stringify(data, null, 2), 'utf8');
    renameSync(tmp, deps.settingsFile);
    cached = data;
    console.info(`[settings] 已更新 preview.debounceMs=${String(data.preview.debounceMs)}`);
  }
```

（`set` 改为 `writeSettings(request); return request;`——注意原 info 日志并入 writeSettings 后，`set` 不再重复打；迁移日志与 set 日志并存属预期。import 追加 `SettingsSchemaV1, migrateV1ToV2`。）

- [ ] **Step 8: 实现 vfs:get + shell 通道 + preload/ipc 接线**

`src/shared/ipc.ts` 追加：

```ts
  // —— 外壳域（M4）：命名 <域>:<动作>（宪法 B.2-5）——
  /** 主→渲染：菜单/窗口命令（ShellCommand 可辨识联合，shell-contract 单一来源） */
  shellCommand: 'shell:command',
  /** 渲染→主：guard 确认后强制关闭（spec §2.3 放行唯一通道） */
  shellForceClose: 'shell:force-close',
  /** 按 nodeId 反查节点 meta（rename/move 后路径新鲜化，spec §6.1） */
  vfsGet: 'vfs:get',
```

`src/shared/shell-contract.ts`（新建）：

```ts
/**
 * 外壳命令契约（宪法 A.7-5 单一来源，M4 spec §5.2 D5）：主→渲染命令可辨识联合；
 * confirm-close 为 close 拦截后的渲染层确认链入口（spec §2.3）。
 */
export type ShellCommand =
  | { readonly type: 'new-file' }
  | { readonly type: 'new-dir' }
  | { readonly type: 'save' }
  | { readonly type: 'confirm-close' };
```

`src/shared/window-api.ts`：import 追加 `ShellCommand`；`WindowApi` 追加：

```ts
  // —— 外壳域（M4）：菜单命令订阅 + guard 放行 ——
  onShellCommand(callback: (command: ShellCommand) => void): () => void;
  forceClose(): Promise<Result<null>>;
  // —— VFS 补充（M4）：nodeId → NodeMeta 反查（未找到 E_VFS_NOT_FOUND）——
  getNode(request: NodeIdRequest): Promise<Result<NodeMeta>>;
```

`src/preload/index.ts`：

```ts
  getNode: (request) => ipcRenderer.invoke(IPC.vfsGet, request),
  forceClose: () => ipcRenderer.invoke(IPC.shellForceClose, null),
  /** 订阅外壳命令：同 onVfsChanged 先例，包装内部消化 ipcRenderer */
  onShellCommand: (callback) => {
    const listener = (_event: IpcRendererEvent, value: ShellCommand): void => callback(value);
    ipcRenderer.on(IPC.shellCommand, listener);
    return () => ipcRenderer.removeListener(IPC.shellCommand, listener);
  },
```

（import 追加 `ShellCommand`。）

`src/main/vfs/vfsService.ts`：新增 `getNode(request: NodeIdRequest): Result<NodeMeta>`——单行 `SELECT`（`WHERE id = ? AND deleted_at IS NULL`）复用该文件既有「行 → NodeMeta」映射与预编译语句风格；未命中返回 `E_VFS_NOT_FOUND`。**实施者先读该文件对齐既有方法样板（语句工厂级预编译、Result 包装），不得引入新错误码。**

`src/main/ipc.ts`：`IpcHandlerDeps` 追加：

```ts
  /** guard 确认后强制关闭（app.ts 提供：置放行标记 + win.close()） */
  readonly requestClose: () => void;
```

注册区追加：

```ts
  ipcMain.handle(
    IPC.vfsGet,
    handleWith(deps, NodeIdRequestSchema, (data: NodeIdRequest) => ({ result: deps.vfs.getNode(data) })),
  );
  ipcMain.handle(
    IPC.shellForceClose,
    handleWith(deps, z.null(), () => {
      deps.requestClose();
      return { result: null };
    }),
  );
```

（`NodeIdRequestSchema` 以 vfs-contract 实际名为准适配；z import 既有。全部既有 `registerIpcHandlers` 测试调用点补 `requestClose: vi.fn(),`；`src/main/app.ts` 的调用点本任务传 `requestClose: () => {}` 占位并在注释标注「Task 6 接真实拦截」，Task 6 替换。）

- [ ] **Step 9: 全量验证 + Commit**

Run: `npm run test:unit && npm run test:integration && npm run typecheck && npm run lint && npm run format:check`
Expected: 全绿（settingsService 迁移分支核心 100%：v2 直读/v1 迁移/损坏/v3 不识别四态均被用例覆盖）。

```bash
git add src/shared src/preload src/main tests/unit
git commit -m "feat(m4): 契约与通道基座——settings v2 迁移例程、vfs:get 反查、shell 命令/force-close 通道"
```

---

### Task 2: 保存管线与标签状态机纯函数（核心决策域）

**Files:**
- Create: `src/renderer/src/features/editor/saveModel.ts`、`src/renderer/src/features/workspace/tabModel.ts`
- Create: `tests/unit/editor/saveModel.test.ts`、`tests/unit/workspace/tabModel.test.ts`

**Interfaces:**
- Consumes: shared `NodeMeta`
- Produces（Task 4/5 消费，签名逐字）:

```ts
// saveModel.ts
export interface SaveState { readonly dirty: boolean; readonly inflight: boolean; readonly pendingAfterInflight: boolean; readonly trailingAt: number | null; readonly deadlineAt: number | null }
export const initialSaveState: SaveState;
export type SaveEvent =
  | { type: 'edit'; at: number; debounceMs: number; autoSaveMs: number }
  | { type: 'timerFired'; at: number }
  | { type: 'writeStart' }
  | { type: 'writeDone'; at: number; debounceMs: number; autoSaveMs: number }
  | { type: 'writeFail'; at: number; autoSaveMs: number }
  | { type: 'flush'; at: number; debounceMs: number; autoSaveMs: number };
export interface SaveOutcome { readonly state: SaveState; readonly write: boolean }
export function reduceSave(s: SaveState, e: SaveEvent): SaveOutcome;

// tabModel.ts
export interface TabState { readonly meta: NodeMeta; readonly dirty: boolean }
export const MAX_TABS = 20;
export interface TabsOp { readonly tabs: readonly TabState[]; readonly activeId: number | null }
export function openTab(op: TabsOp, meta: NodeMeta): TabsOp;   // 唯一实例聚焦；上限静默拒（调用方 toast）
export function closeTab(op: TabsOp, id: number): TabsOp;      // 激活右邻优先，无则左邻，全关 null
export function updateTabMeta(op: TabsOp, id: number, meta: NodeMeta): TabsOp;
export function setTabDirty(op: TabsOp, id: number, dirty: boolean): TabsOp;
```

- [ ] **Step 1: 写失败测试 tests/unit/editor/saveModel.test.ts（全文）**

```ts
// 保存管线状态机（M4 spec §2.1/2.2）：尾沿去抖 + 最长挂起双计时器、写串行、失败重试、
// flush 语义——纯决策无副作用，计时器句柄归 SaveController
import { describe, expect, it } from 'vitest';
import { initialSaveState, reduceSave } from '../../../src/renderer/src/features/editor/saveModel';

const D = { debounceMs: 300, autoSaveMs: 3000 };

describe('保存状态机 reduceSave', () => {
  it('首个 edit 武装双计时器（尾沿 +300、挂起 +3000）；后续 edit 只推尾沿', () => {
    let o = reduceSave(initialSaveState, { type: 'edit', at: 1000, ...D });
    expect(o.state.dirty).toBe(true);
    expect(o.state.trailingAt).toBe(1300);
    expect(o.state.deadlineAt).toBe(4000);
    o = reduceSave(o.state, { type: 'edit', at: 2000, ...D });
    expect(o.state.trailingAt).toBe(2300); // 尾沿后移
    expect(o.state.deadlineAt).toBe(4000); // 挂起自首个脏变更起算不后移
    expect(o.write).toBe(false);
  });

  it('计时器到期 → write=true 且清计时器；writeStart 置在途', () => {
    const armed = reduceSave(initialSaveState, { type: 'edit', at: 0, ...D }).state;
    const fired = reduceSave(armed, { type: 'timerFired', at: 300 });
    expect(fired.write).toBe(true);
    expect(fired.state.trailingAt).toBeNull();
    const started = reduceSave(fired.state, { type: 'writeStart' });
    expect(started.state.inflight).toBe(true);
  });

  it('在途期间输入记 pending 不武装计时器；writeDone 后以新窗口重调度', () => {
    let s = reduceSave(initialSaveState, { type: 'edit', at: 0, ...D }).state;
    s = reduceSave(s, { type: 'timerFired', at: 300 }).state;
    s = reduceSave(s, { type: 'writeStart' }).state;
    const during = reduceSave(s, { type: 'edit', at: 500, ...D });
    expect(during.state.pendingAfterInflight).toBe(true);
    expect(during.state.trailingAt).toBeNull(); // 在途不武装（写完成后统一重调度）
    const done = reduceSave(during.state, { type: 'writeDone', at: 800, ...D });
    expect(done.write).toBe(false);
    expect(done.state.dirty).toBe(true);
    expect(done.state.trailingAt).toBe(1100);
    expect(done.state.deadlineAt).toBe(3800);
  });

  it('无 pending 的 writeDone 全清（dirty 归零）；writeFail 按 autoSaveMs 重试不自旋', () => {
    let s = reduceSave(initialSaveState, { type: 'edit', at: 0, ...D }).state;
    s = reduceSave(s, { type: 'timerFired', at: 300 }).state;
    s = reduceSave(s, { type: 'writeStart' }).state;
    const done = reduceSave(s, { type: 'writeDone', at: 400, ...D });
    expect(done.state).toEqual(initialSaveState);
    const fail = reduceSave(done.state, { type: 'edit', at: 500, ...D }).state;
    const failed = reduceSave(fail, { type: 'timerFired', at: 800 }).state;
    const retrying = reduceSave({ ...failed, inflight: true }, { type: 'writeFail', at: 900, autoSaveMs: 3000 });
    expect(retrying.state.dirty).toBe(true);
    expect(retrying.state.trailingAt).toBe(3900); // +autoSaveMs 重试
    expect(retrying.write).toBe(false);
  });

  it('flush：有脏立即 write 并清计时器；在途转 pending；无脏 no-op', () => {
    const armed = reduceSave(initialSaveState, { type: 'edit', at: 0, ...D }).state;
    const flushed = reduceSave(armed, { type: 'flush', at: 100, ...D });
    expect(flushed.write).toBe(true);
    expect(flushed.state.trailingAt).toBeNull();
    const inFlight = reduceSave(flushed.state, { type: 'writeStart' }).state;
    const flushDuring = reduceSave(inFlight, { type: 'flush', at: 150, ...D });
    expect(flushDuring.write).toBe(false);
    expect(flushDuring.state.pendingAfterInflight).toBe(true);
    const clean = reduceSave(initialSaveState, { type: 'flush', at: 0, ...D });
    expect(clean.write).toBe(false);
  });
});
```

- [ ] **Step 2: 写失败测试 tests/unit/workspace/tabModel.test.ts（全文）**

```ts
// 标签状态机（M4 spec §3）：同文件唯一实例聚焦、关闭右邻优先、上限护栏、meta/dirty 同步
import { describe, expect, it } from 'vitest';
import type { NodeMeta } from '../../../src/shared/vfs-contract';
import {
  MAX_TABS,
  closeTab,
  openTab,
  setTabDirty,
  updateTabMeta,
  type TabState,
  type TabsOp,
} from '../../../src/renderer/src/features/workspace/tabModel';

let seq = 0;
function meta(id: number, name: string): NodeMeta {
  seq += 1;
  return {
    id, parentId: 1, nodeType: 'file', name, virtualPath: `/${name}`, mimeType: 'text/html', size: 4,
    createdAt: '2026-09-18T10:00:00.000+08:00', updatedAt: '2026-09-18T10:00:00.000+08:00',
  };
}
const EMPTY: TabsOp = { tabs: [], activeId: null };

describe('tabModel', () => {
  it('openTab：空开激活；重复打开同文件聚焦既有不重复建', () => {
    let op = openTab(EMPTY, meta(2, 'a.html'));
    expect(op.activeId).toBe(2);
    op = openTab(op, meta(3, 'b.html'));
    expect(op.tabs).toHaveLength(2);
    op = openTab(op, meta(2, 'a.html'));
    expect(op.tabs).toHaveLength(2);
    expect(op.activeId).toBe(2);
  });

  it('openTab 上限护栏：MAX_TABS 满后静默拒开（调用方 toast）', () => {
    let op = EMPTY;
    for (let id = 1; id <= MAX_TABS; id += 1) op = openTab(op, meta(id, `f${id}.html`));
    const full = op;
    const rejected = openTab(full, meta(999, 'x.html'));
    expect(rejected.tabs).toHaveLength(MAX_TABS);
    expect(rejected.activeId).toBe(full.activeId);
  });

  it('closeTab：关闭激活标签右邻优先补位；关末尾左邻；关非激活不动激活', () => {
    let op = openTab(openTab(openTab(EMPTY, meta(2, 'a')), meta(3, 'b')), meta(4, 'c'));
    op = openTab(op, meta(3, 'b')); // 激活 b（中位）
    const closedMiddle = closeTab(op, 3);
    expect(closedMiddle.activeId).toBe(4); // 右邻
    const closedTail = closeTab(closedMiddle, 4);
    expect(closedTail.activeId).toBe(2); // 无右邻左邻
    const closedInactive = closeTab(openTab(openTab(openTab(EMPTY, meta(2, 'a')), meta(3, 'b')), meta(4, 'c')), 4);
    expect(closedInactive.activeId).toBe(4); // 关非激活（激活=2？——见断言修正：激活为最后 open 的 4 时不成立，
    // 故本断言固定场景：激活 4、关 2
  });
});
```

**注意：上面最后一个用例故意留了一个语义陷阱——`closeTab` 的激活补位只在关「激活标签」时生效，关闭非激活标签不得改变激活。把该用例替换为以下两个精确用例（删除上面 `closedInactive` 一段）：**

```ts
  it('closeTab：关闭非激活标签激活位保持不变', () => {
    let op = openTab(openTab(openTab(EMPTY, meta(2, 'a')), meta(3, 'b')), meta(4, 'c'));
    op = openTab(op, meta(3, 'b')); // 激活 3
    const closed = closeTab(op, 4); // 关非激活 4
    expect(closed.activeId).toBe(3);
    expect(closed.tabs.map((t) => t.meta.id)).toEqual([2, 3]);
  });

  it('closeTab：关闭全部标签激活位归 null；setTabDirty/updateTabMeta 按 id 精确同步', () => {
    let op = openTab(openTab(EMPTY, meta(2, 'a')), meta(3, 'b'));
    op = closeTab(closeTab(op, 3), 2);
    expect(op.activeId).toBeNull();
    expect(op.tabs).toHaveLength(0);
    const dirtied = setTabDirty({ tabs: [{ meta: meta(2, 'a'), dirty: false }], activeId: 2 }, 2, true);
    expect(dirtied.tabs[0]?.dirty).toBe(true);
    const renamed = updateTabMeta(dirtied, 2, meta(2, '新名.html'));
    expect(renamed.tabs[0]?.meta.name).toBe('新名.html');
    expect(renamed.tabs[0]?.dirty).toBe(true); // dirty 不被 meta 同步冲掉
    const tabsAll: readonly TabState[] = renamed.tabs;
    expect(tabsAll).toHaveLength(1);
  });
```

（`seq` 变量仅占位防误用可删——若 lint 报未使用直接删除该行。）

- [ ] **Step 3: 运行确认失败**

Run: `npm run test:unit -- tests/unit/editor/saveModel.test.ts tests/unit/workspace/tabModel.test.ts`
Expected: FAIL（Cannot find module）。

- [ ] **Step 4: 实现 saveModel.ts（全文）**

```ts
/**
 * 保存管线纯状态机（M4 spec §2.1/2.2 裁决 D2）：双计时器时点（尾沿去抖 + 最长挂起）
 * 记于状态、写串行（同标签同时最多一个在途）、失败按 autoSaveMs 重试——决策与副作用
 * 分离：计时器句柄/写调用归 SaveController，本模块可 100% 单测。
 */
export interface SaveState {
  readonly dirty: boolean; // 存在未落库变更
  readonly inflight: boolean; // 写在途
  readonly pendingAfterInflight: boolean; // 在途期间的后续输入
  readonly trailingAt: number | null; // 尾沿到期绝对时刻（ms）
  readonly deadlineAt: number | null; // 挂起上限到期绝对时刻（自首个脏变更起算，不后移）
}

export const initialSaveState: SaveState = {
  dirty: false,
  inflight: false,
  pendingAfterInflight: false,
  trailingAt: null,
  deadlineAt: null,
};

export type SaveEvent =
  | { type: 'edit'; at: number; debounceMs: number; autoSaveMs: number }
  | { type: 'timerFired'; at: number }
  | { type: 'writeStart' }
  | { type: 'writeDone'; at: number; debounceMs: number; autoSaveMs: number }
  | { type: 'writeFail'; at: number; autoSaveMs: number }
  | { type: 'flush'; at: number; debounceMs: number; autoSaveMs: number };

export interface SaveOutcome {
  readonly state: SaveState;
  readonly write: boolean; // 调用方据此以「发起时刻 doc 快照」发起一次 vfs:write
}

export function reduceSave(s: SaveState, e: SaveEvent): SaveOutcome {
  switch (e.type) {
    case 'edit': {
      if (s.inflight) {
        // 写串行：在途期间输入只记 pending，写完成后统一重调度（spec §2.2-1）
        return { state: { ...s, dirty: true, pendingAfterInflight: true }, write: false };
      }
      return {
        state: {
          ...s,
          dirty: true,
          trailingAt: e.at + e.debounceMs,
          deadlineAt: s.deadlineAt ?? e.at + e.autoSaveMs,
        },
        write: false,
      };
    }
    case 'timerFired': {
      if (s.inflight) {
        return { state: { ...s, pendingAfterInflight: true, trailingAt: null, deadlineAt: null }, write: false };
      }
      return { state: { ...s, trailingAt: null, deadlineAt: null }, write: true };
    }
    case 'writeStart':
      return { state: { ...s, inflight: true, trailingAt: null, deadlineAt: null }, write: false };
    case 'writeDone': {
      if (s.pendingAfterInflight) {
        // 后继写以全新窗口调度（spec §2.2-1：在途完成后按剩余脏量重新走双计时器）
        return {
          state: {
            ...s,
            inflight: false,
            pendingAfterInflight: false,
            trailingAt: e.at + e.debounceMs,
            deadlineAt: e.at + e.autoSaveMs,
          },
          write: false,
        };
      }
      return { state: { ...initialSaveState }, write: false }; // 全部落库：脏/在途/计时全清
    }
    case 'writeFail':
      // 写失败保留脏，按自动保存周期重试（不自旋；spec §2.2-3）
      return {
        state: {
          ...s,
          inflight: false,
          pendingAfterInflight: false,
          trailingAt: e.at + e.autoSaveMs,
          deadlineAt: e.at + e.autoSaveMs,
        },
        write: false,
      };
    case 'flush': {
      if (s.inflight) return { state: { ...s, pendingAfterInflight: true }, write: false };
      if (!s.dirty) return { state: s, write: false };
      return { state: { ...s, trailingAt: null, deadlineAt: null }, write: true };
    }
  }
}
```

- [ ] **Step 5: 实现 tabModel.ts（全文）**

```ts
/**
 * 标签列表状态机（M4 spec §3 D1 附则）：同文件唯一实例（重复打开=聚焦）、关闭激活标签
 * 右邻优先补位、同开上限护栏——纯函数不可变更新（宪法 A.1-10）。
 */
import type { NodeMeta } from '../../../shared/vfs-contract';

export interface TabState {
  readonly meta: NodeMeta;
  readonly dirty: boolean;
}

/** 同开上限护栏（防失控；超限由调用方 toast 提示先关） */
export const MAX_TABS = 20;

export interface TabsOp {
  readonly tabs: readonly TabState[];
  readonly activeId: number | null;
}

export function openTab(op: TabsOp, meta: NodeMeta): TabsOp {
  if (op.tabs.some((t) => t.meta.id === meta.id)) {
    return { tabs: op.tabs, activeId: meta.id }; // 聚焦既有实例
  }
  if (op.tabs.length >= MAX_TABS) return op;
  return { tabs: [...op.tabs, { meta, dirty: false }], activeId: meta.id };
}

export function closeTab(op: TabsOp, id: number): TabsOp {
  const idx = op.tabs.findIndex((t) => t.meta.id === id);
  if (idx === -1) return op;
  const tabs = op.tabs.filter((t) => t.meta.id !== id);
  if (op.activeId !== id) return { tabs, activeId: op.activeId };
  const next = tabs[idx] ?? tabs[idx - 1]; // 右邻优先，无则左邻
  return { tabs, activeId: next?.meta.id ?? null };
}

/** rename/move 后 meta 新鲜化（经 vfs:get 反查，Task 8 消费）；不冲掉 dirty */
export function updateTabMeta(op: TabsOp, id: number, meta: NodeMeta): TabsOp {
  return { ...op, tabs: op.tabs.map((t) => (t.meta.id === id ? { ...t, meta } : t)) };
}

export function setTabDirty(op: TabsOp, id: number, dirty: boolean): TabsOp {
  return { ...op, tabs: op.tabs.map((t) => (t.meta.id === id ? { ...t, dirty } : t)) };
}
```

- [ ] **Step 6: 全量验证 + Commit**

Run: `npm run test:unit && npm run typecheck && npm run lint && npm run format:check`
Expected: 全绿（saveModel/tabModel 全转换用例 100%）。

```bash
git add src/renderer/src/features/editor/saveModel.ts src/renderer/src/features/workspace/tabModel.ts tests/unit/editor tests/unit/workspace
git commit -m "feat(m4): 保存管线双计时器状态机与标签列表状态机——写串行/挂起上限/右邻补位纯函数"
```

---

### Task 3: CodeMirror 封装层（依赖安装 + 扩展工厂 + 语言映射 + EditorPanel 换内核）

**Files:**
- Modify: `package.json`/`package-lock.json`（Task 首步安装）
- Create: `src/renderer/src/features/editor/language.ts`、`src/renderer/src/features/editor/codemirror.ts`、`src/renderer/src/features/editor/tabSessions.ts`
- Modify: `src/renderer/src/features/editor/EditorPanel.tsx`（换内核；props 面演进见下——三栏结构不重排，M3 接缝语义兑现）
- Create: `tests/unit/editor/language.test.ts`、`tests/unit/renderer/editor-panel.test.tsx`
- Modify: `tests/unit/renderer/panels-tree-editor.test.tsx`（EditorPanel 部分随 props 演进改写，TreePanel 部分不动）

**Interfaces:**
- Consumes: Task 2 `TabState`（渲染层 TabBar 在 Task 4；本任务 EditorPanel 先以最小会话 props 落 CM 内核）
- Produces（Task 4/5 消费，签名逐字）:

```ts
// language.ts
export function languageFor(mimeType: string): LanguageSupport | null;
// codemirror.ts
export function editorExtensions(mimeType: string, handlers: { onDocChanged: (text: string) => void; onScroll: (scrollTop: number) => void }): Extension[];
// tabSessions.ts
export interface TabSession { readonly nodeId: number; state: EditorState; scrollTop: number }
export class TabSessions {
  open(nodeId: number, state: EditorState, scrollTop?: number): TabSession;
  has(nodeId: number): boolean;
  get(nodeId: number): TabSession | undefined;
  updateState(nodeId: number, state: EditorState): void;
  updateScroll(nodeId: number, scrollTop: number): void;
  close(nodeId: number): void;
}
// EditorPanel.tsx（props 演进——M4 spec §4：会话式内核，工作台三栏结构不重排）
export interface EditorPanelProps {
  readonly sessions: TabSessions;
  readonly activeTab: TabState | null;
  readonly debounceMs: number;
  readonly onDocChanged: (nodeId: number, text: string) => void; // Workspace → SaveController.edit
}
```

- [ ] **Step 1: 安装 CodeMirror 依赖（精确小版本锁定，宪法 C.2）**

```bash
npm i -E @codemirror/state@6.7.5 @codemirror/view@6.43.12 @codemirror/commands@6.11.1 @codemirror/language@6.12.4 @codemirror/search@6.7.2 @codemirror/lang-html@6.4.12 @codemirror/lang-css@6.3.1 @codemirror/lang-javascript@6.2.5
```

（CM6 纯 ESM JS 包，无 install 脚本/原生模块——本机 allow-scripts 拦截无影响；无需 rebuild。若 registry 版本已前移，以实查为准同步本计划版本号并记报告。）

- [ ] **Step 2: 写失败测试 tests/unit/editor/language.test.ts（全文）**

```ts
// 语言映射（M4 spec 裁决 D6）：html/css/js 有高亮，json/xml/txt 纯文本（FR-EDIT-01 字面）
import { describe, expect, it } from 'vitest';
import { languageFor } from '../../../src/renderer/src/features/editor/language';

describe('languageFor', () => {
  it('text/html、text/css、javascript 三系返回语言支持实例', () => {
    expect(languageFor('text/html')).not.toBeNull();
    expect(languageFor('text/css')).not.toBeNull();
    expect(languageFor('text/javascript')).not.toBeNull();
    expect(languageFor('application/javascript')).not.toBeNull();
  });

  it('json/xml/txt/未知与目录 null（无高亮纯文本，不装额外语言包）', () => {
    expect(languageFor('application/json')).toBeNull();
    expect(languageFor('application/xml')).toBeNull();
    expect(languageFor('text/plain')).toBeNull();
    expect(languageFor('application/octet-stream')).toBeNull();
  });
});
```

- [ ] **Step 3: 写失败测试 tests/unit/renderer/editor-panel.test.tsx（全文）**

```tsx
// @vitest-environment jsdom
// CodeMirror 内核冒烟（宪法 A.6-2）：view 生命周期成对、输入回报 doc、会话切换保 doc/undo/光标、
// 二进制/空态占位。CM6 在 jsdom 无布局引擎：断言只碰 state/doc 层，不碰坐标类 API。
import { act } from 'react';
import { EditorSelection } from '@codemirror/state';
import { createRoot } from 'react-dom/client';
import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest';
import { EditorPanel } from '../../../src/renderer/src/features/editor/EditorPanel';
import { TabSessions } from '../../../src/renderer/src/features/editor/tabSessions';
import type { NodeMeta } from '../../../src/shared/vfs-contract';

function meta(id: number, name: string, mimeType = 'text/html'): NodeMeta {
  return {
    id, parentId: 1, nodeType: 'file', name, virtualPath: `/${name}`, mimeType, size: 4,
    createdAt: '2026-09-18T10:00:00.000+08:00', updatedAt: '2026-09-18T10:00:00.000+08:00',
  };
}

let container: HTMLElement;
beforeEach(() => {
  container = document.createElement('div');
  document.body.appendChild(container);
});
afterEach(() => {
  container.remove();
  document.body.innerHTML = '';
});

describe('EditorPanel（CodeMirror 内核）', () => {
  it('无激活标签 → 空态占位；激活标签 → CM 编辑器就绪且文档一致', () => {
    const sessions = new TabSessions();
    const onDocChanged = vi.fn();
    const tree = createRoot(container);
    act(() => {
      tree.render(
        <EditorPanel sessions={sessions} activeTab={null} debounceMs={300} onDocChanged={onDocChanged} />,
      );
    });
    expect(container.querySelector('.cm-editor')).toBeNull();
    expect(container.textContent).toContain('未选中文件');
    sessions.open(2, '' /* state 由内部 createEditorState 兜底？——见实现注 */);
    // —— 上行不改：TabSessions.open 签名见 Task 3 Produces（state 必填）。
    // 测试改为：先以 createEditorState 构造再 open（import 自 codemirror.ts）。
    tree.unmount();
  });
});
```

**实现注（测试正文以此为准，删除上段占位注释）：**`TabSessions.open(nodeId, state, scrollTop?)` 的 `state` 由 `createEditorState(doc, mimeType, handlers)` 构造（codemirror.ts 导出）。测试正文替换为：

```tsx
  it('无激活标签 → 空态占位；激活标签 → CM 编辑器就绪且文档一致', () => {
    const sessions = new TabSessions();
    const onDocChanged = vi.fn();
    sessions.open(
      2,
      createEditorState('<p>一</p>', 'text/html', { onDocChanged: () => {}, onScroll: () => {} }),
    );
    const tree = createRoot(container);
    act(() => {
      tree.render(
        <EditorPanel
          sessions={sessions}
          activeTab={{ meta: meta(2, 'a.html'), dirty: false }}
          debounceMs={300}
          onDocChanged={onDocChanged}
        />,
      );
    });
    expect(container.querySelector('.cm-editor')).not.toBeNull();
    expect(container.textContent).toContain('一');
    tree.unmount();
    expect(container.querySelector('.cm-editor')).toBeNull(); // destroy 成对
  });

  it('输入经 updateListener 回报 onDocChanged；会话切换后各自 doc/光标保留', () => {
    const sessions = new TabSessions();
    const onDocChanged = vi.fn();
    sessions.open(2, createEditorState('', 'text/plain', { onDocChanged: (t) => onDocChanged(2, t), onScroll: () => {} }));
    sessions.open(3, createEditorState('', 'text/plain', { onDocChanged: (t) => onDocChanged(3, t), onScroll: () => {} }));
    const tree = createRoot(container);
    act(() => {
      tree.render(
        <EditorPanel
          sessions={sessions}
          activeTab={{ meta: meta(2, 'a.txt'), dirty: false }}
          debounceMs={300}
          onDocChanged={onDocChanged}
        />,
      );
    });
    // 直接驱动 view dispatch（jsdom 无真实键入；键盘路径归 E2E）
    const viewA = container.querySelector('.cm-content')?.cmView?.view;
    expect(viewA).toBeDefined();
    act(() => {
      viewA.dispatch({ changes: { from: 0, insert: '甲' }, selection: EditorSelection.cursor(1) });
    });
    expect(onDocChanged).toHaveBeenCalledWith(2, '甲');
    // 切到 b：视图换入会话 3 的空文档；再切回 a：'甲' 与光标位置仍在
    act(() => {
      tree.render(
        <EditorPanel
          sessions={sessions}
          activeTab={{ meta: meta(3, 'b.txt'), dirty: false }}
          debounceMs={300}
          onDocChanged={onDocChanged}
        />,
      );
    });
    act(() => {
      tree.render(
        <EditorPanel
          sessions={sessions}
          activeTab={{ meta: meta(2, 'a.txt'), dirty: true }}
          debounceMs={300}
          onDocChanged={onDocChanged}
        />,
      );
    });
    const content = container.querySelector('.cm-content');
    expect(content?.textContent).toBe('甲');
    tree.unmount();
  });
```

（`.cm-content` 与 `cmView` 的桥接：jsdom 下 CM6 会在 content DOM 上挂 `cmView` 属性——若实测属性名不可达，改用 `EditorPanel` 暴露的测试钩子或经 `TabSessions.get(2)?.state.doc.toString()` 断言会话态（updateListener 已回写会话）。二选一以实跑为准，报告留证。）

- [ ] **Step 4: 运行确认失败**

Run: `npm run test:unit -- tests/unit/editor/language.test.ts tests/unit/renderer/editor-panel.test.tsx`
Expected: FAIL（Cannot find module language / EditorPanel props 不符）。

- [ ] **Step 5: 实现 language.ts（全文）**

```ts
/**
 * MIME → 语言包映射（M4 spec 裁决 D6）：FR-EDIT-01 字面（HTML/CSS/JS）开箱高亮，
 * html() 内嵌 script/style 的 JS/CSS 解析随包内置；json/xml/txt 纯文本不装额外包（YAGNI）。
 */
import { html } from '@codemirror/lang-html';
import { css } from '@codemirror/lang-css';
import { javascript } from '@codemirror/lang-javascript';
import type { LanguageSupport } from '@codemirror/language';

export function languageFor(mimeType: string): LanguageSupport | null {
  switch (mimeType) {
    case 'text/html':
      return html();
    case 'text/css':
      return css();
    case 'text/javascript':
    case 'application/javascript':
      return javascript();
    default:
      return null;
  }
}
```

- [ ] **Step 6: 实现 codemirror.ts（全文）**

```ts
/**
 * CodeMirror 6 扩展工厂（宪法 A.1-9 / 调研 A7-10 习语）：curated 扩展集（M4 spec 裁决 D6，
 * 不引元包 basicSetup——autocompletion/closeBrackets/foldGutter 未要求）；扩展工厂函数形态，
 * 静态扩展依赖去重；监听类扩展随会话 state 闭包固定 nodeId（每会话一份，无共享可变态）。
 */
import { defaultKeymap, history, historyKeymap, indentWithTab } from '@codemirror/commands';
import {
  bracketMatching,
  defaultHighlightStyle,
  indentOnInput,
  syntaxHighlighting,
} from '@codemirror/language';
import { search, searchKeymap } from '@codemirror/search';
import { EditorState } from '@codemirror/state';
import type { Extension } from '@codemirror/state';
import { drawSelection, EditorView, keymap, lineNumbers } from '@codemirror/view';
import { languageFor } from './language';

export interface EditorHandlers {
  /** 文档变更回报（含 IME composition 期间——spec §4 已知边界） */
  readonly onDocChanged: (text: string) => void;
  /** 滚动回报（会话级滚动记忆，spec §3） */
  readonly onScroll: (scrollTop: number) => void;
}

/** 编辑器扩展集：行号/撤销/自绘选区/自动缩进/括号匹配/高亮/查找替换 + 键位（默认+撤销+查找+Tab 缩进） */
export function editorExtensions(mimeType: string, handlers: EditorHandlers): Extension[] {
  const language = languageFor(mimeType);
  return [
    lineNumbers(),
    history(),
    drawSelection(),
    indentOnInput(),
    bracketMatching(),
    syntaxHighlighting(defaultHighlightStyle),
    search(),
    ...(language === null ? [] : [language]),
    EditorView.updateListener.of((update) => {
      if (update.docChanged) handlers.onDocChanged(update.state.doc.toString());
    }),
    EditorView.domEventHandlers({
      scroll: (_event, view) => {
        handlers.onScroll(view.scrollDOM.scrollTop);
        return false; // 不吃事件，默认滚动行为照常
      },
    }),
    keymap.of([...defaultKeymap, ...historyKeymap, ...searchKeymap, indentWithTab]),
  ];
}

/** 会话 state 构造（换文档新建状态不复用——宪法 A.1-9；监听闭包在 state 内固化） */
export function createEditorState(doc: string, mimeType: string, handlers: EditorHandlers): EditorState {
  return EditorState.create({ doc, extensions: editorExtensions(mimeType, handlers) });
}
```

- [ ] **Step 7: 实现 tabSessions.ts（全文）**

```ts
/**
 * 标签编辑器会话（M4 spec §3）：per-tab EditorState（doc/undo/光标随 state 天然保留）
 * + 滚动位置——切换标签经 EditorView.setState 换入换出（宪法 A.1-9），不重建 view。
 * 无 React 依赖（Workspace useRef 持有）；state 字段为「库新建实例整体替换」非原地改。
 */
import type { EditorState } from '@codemirror/state';

export interface TabSession {
  readonly nodeId: number;
  state: EditorState;
  scrollTop: number;
}

export class TabSessions {
  private map = new Map<number, TabSession>();

  open(nodeId: number, state: EditorState, scrollTop = 0): TabSession {
    const session: TabSession = { nodeId, state, scrollTop };
    this.map.set(nodeId, session);
    return session;
  }

  has(nodeId: number): boolean {
    return this.map.has(nodeId);
  }

  get(nodeId: number): TabSession | undefined {
    return this.map.get(nodeId);
  }

  /** updateListener 回路：库以新实例整体替换 state（禁原地改，A.1-9） */
  updateState(nodeId: number, state: EditorState): void {
    const session = this.map.get(nodeId);
    if (session !== undefined) session.state = state;
  }

  updateScroll(nodeId: number, scrollTop: number): void {
    const session = this.map.get(nodeId);
    if (session !== undefined) session.scrollTop = scrollTop;
  }

  close(nodeId: number): void {
    this.map.delete(nodeId);
  }
}
```

- [ ] **Step 8: 实现 EditorPanel.tsx（换内核，全文替换）**

```tsx
/**
 * 编辑区（M4 spec §4）：CodeMirror 内核——单 EditorView 实例 + TabSessions 会话换入换出
 * （doc/undo/光标跨标签保留）；props 面自 M3 textarea 占位演进为会话式（node → activeTab+sessions，
 * 工作台三栏结构与 shell 插槽不重排——M3 接缝语义兑现）。二进制/空态在 Workspace.openFile
 * 前置拦截（非文本不开标签），本组件不再有二进制分支（无死分支纪律）。
 * 渲染期零副作用：view 生命周期与会话换入全在 effect（75647f2 渲染期禁写 ref 先例）。
 */
import { useEffect, useRef } from 'react';
import type { TabState } from '../workspace/tabModel';
import { createEditorState } from './codemirror';
import { TabSessions } from './tabSessions';
// 空绑定 type import：把 window-api.ts 的全局 Window.api 声明拉入渲染层编译程序
import type {} from '../../../../shared/window-api';

export interface EditorPanelProps {
  readonly sessions: TabSessions;
  readonly activeTab: TabState | null;
  /** 尾沿去抖（FR-RENDER-03）——写管线本体在 Workspace 的 SaveController（Task 5） */
  readonly debounceMs: number;
  readonly onDocChanged: (nodeId: number, text: string) => void;
}

export function EditorPanel({
  sessions,
  activeTab,
  debounceMs,
  onDocChanged,
}: EditorPanelProps): React.JSX.Element {
  const hostRef = useRef<HTMLDivElement | null>(null);
  const viewRef = useRef<import('@codemirror/view').EditorView | null>(null);
  const activeNodeIdRef = useRef<number | null>(null);
  // 回调引用 effect 内同步（渲染期禁写 ref）：监听闭包在 state 内固化，此处仅保
  // onDocChanged 直通 latest（保存管线/会话回写由 Workspace 侧闭包自理）
  const onDocChangedRef = useRef(onDocChanged);
  useEffect(() => {
    onDocChangedRef.current = onDocChanged;
  }, [onDocChanged]);

  // 激活会话换入（外部数据到达，唯一例外 effect 域）：首挂建 view；后续 setState 换入；
  // 旧会话回收（state/scroll 已由监听实时回写，此处兜底 scroll 一致性）
  useEffect(() => {
    if (activeTab === null) return undefined;
    const session = sessions.get(activeTab.meta.id);
    if (session === undefined) return undefined;
    const view = viewRef.current;
    if (view === null) {
      const created = new (import('@codemirror/view').EditorView)({
        parent: hostRef.current ?? undefined,
        state: session.state,
      });
      created.scrollDOM.scrollTop = session.scrollTop;
      viewRef.current = created;
    } else {
      const prevId = activeNodeIdRef.current;
      if (prevId !== null && prevId !== activeTab.meta.id) {
        sessions.updateState(prevId, view.state);
        sessions.updateScroll(prevId, view.scrollDOM.scrollTop);
      }
      view.setState(session.state);
      view.scrollDOM.scrollTop = session.scrollTop;
    }
    activeNodeIdRef.current = activeTab.meta.id;
    return undefined;
  }, [activeTab, sessions]);

  // 卸载销毁成对（宪法资源纪律）
  useEffect(() => {
    return () => {
      viewRef.current?.destroy();
      viewRef.current = null;
    };
  }, []);

  // —— 上面的 view 构造行在落地时用顶部 import 的 EditorView（不要写成 import() 动态形态）：
  // 文件顶部 `import { EditorView } from '@codemirror/view';` 后，两处直接 `new EditorView({...})`。

  if (activeTab === null) {
    return (
      <div className="lt-editor-empty" aria-label="编辑区占位">
        未选中文件
      </div>
    );
  }
  return (
    <div className="lt-editor">
      <div className="lt-editor-host" ref={hostRef} />
      <div className="lt-editor-bar">
        {/* M4 起保存由原生菜单 Ctrl/Cmd+S 承载（Task 6）；按钮保留为可见入口，点击即菜单同款命令 */}
        <button type="button" onClick={() => onDocChangedRef.current(activeTab.meta.id, sessions.get(activeTab.meta.id)?.state.doc.toString() ?? '')}>
          保存
        </button>
        <span aria-live="polite">{activeTab.dirty ? '未保存' : '已保存'}</span>
      </div>
    </div>
  );
}
```

**落地注（实现者必读）：**① 顶部补 `import { EditorView } from '@codemirror/view';` 并把 effect 内 `new (import(...))` 形态替换为直接构造（示例中的动态 import 仅为标注位置）；② 保存钮当前直接回调 `onDocChanged` 只是 M3 形态的过渡占位——**Task 5 会把保存语义改为 SaveController.flush**，本任务保持「按钮触发 onDocChanged 不够——需触发立即写」，故本任务保存钮先接 `props.onSaveRequest?.()`：在 props 增加可选 `readonly onSaveRequest?: () => void;` 并让按钮调用它（Workspace 本任务传 undefined，Task 5 接管线）；③ `debounceMs` 在本组件暂不消费（管线在 Workspace），保留参数位并注释说明（lint 若报未使用改为 `_debounceMs` 解构别名或从 props 显式声明不用——以 typecheck/lint 实跑为准，报告留证）；④ 消挂载断言依赖 destroy 清 DOM（`.cm-editor` 移除）。

- [ ] **Step 9: 改写 panels-tree-editor.test.tsx 的 EditorPanel describe**

EditorPanel 相关用例（textarea 去抖时序那组）随 props 演进**整体移除**——去抖时序职责已移至 Task 5 的 saveController 单测（fake timers 全链路），组件冒烟以 editor-panel.test.tsx 为准；TreePanel 用例不动。文件头注释同步。

- [ ] **Step 10: 全量验证 + Commit**

Run: `npm run test:unit && npm run typecheck && npm run lint && npm run format:check && npm run build`
Expected: 全绿（language/editor-panel 冒烟全过；CM6 打包进渲染层 bundle 无告警）。

```bash
git add package.json package-lock.json src/renderer/src/features/editor tests/unit/editor tests/unit/renderer
git commit -m "feat(m4): CodeMirror 内核——curated 扩展工厂/语言映射/会话式 EditorPanel 换芯（M3 接缝兑现）"
```

---

### Task 4: 多标签 TabBar 与 Workspace 会话中枢

**Files:**
- Create: `src/renderer/src/features/workspace/TabBar.tsx`
- Modify: `src/renderer/src/features/workspace/Workspace.tsx`（tabs/activeTab 会话中枢 + openFile 前置拦截）
- Create: `tests/unit/renderer/tab-bar.test.tsx`
- Modify: `tests/unit/renderer/panels-preview-workspace.test.tsx`、`tests/unit/renderer/app.test.tsx`、`tests/unit/renderer/entry.test.ts`（桥桩与断言随 Workspace 演进适配）

**Interfaces:**
- Consumes: Task 2 tabModel 全量、Task 3 TabSessions/EditorPanel 新 props
- Produces（Task 5/6/7/8 消费）:
  - Workspace 内部装配：`saveControllerRef`（Task 5 实装）、`sessionsRef`（TabSessions）、`openFile(meta)`（非文本 toast 拒开；readFile 成功后建会话 + openTab）、`closeTabById(id)`（flush 后关——Task 5 接管线，本任务直接关）、树 `onSelect` → file 走 openFile、dir 走展开上下文
  - `TabBarProps`: `{ tabs: readonly TabState[]; activeId: number | null; onActivate(id): void; onClose(id): void }`

- [ ] **Step 1: 写失败测试 tests/unit/renderer/tab-bar.test.tsx（全文）**

```tsx
// @vitest-environment jsdom
// TabBar 冒烟（宪法 A.6-2）：文件名/dirty 圆点/激活态/关闭与激活回调（纯呈现无数据逻辑）
import { act } from 'react';
import { createRoot } from 'react-dom/client';
import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest';
import type { NodeMeta } from '../../../src/shared/vfs-contract';
import { TabBar } from '../../../src/renderer/src/features/workspace/TabBar';

function meta(id: number, name: string): NodeMeta {
  return {
    id, parentId: 1, nodeType: 'file', name, virtualPath: `/${name}`, mimeType: 'text/html', size: 4,
    createdAt: '2026-09-18T10:00:00.000+08:00', updatedAt: '2026-09-18T10:00:00.000+08:00',
  };
}

let container: HTMLElement;
beforeEach(() => {
  container = document.createElement('div');
  document.body.appendChild(container);
});
afterEach(() => {
  container.remove();
  document.body.innerHTML = '';
});

describe('TabBar', () => {
  it('渲染文件名与 dirty 圆点、激活态 aria-current；点选触发 onActivate、关闭钮触发 onClose', () => {
    const onActivate = vi.fn();
    const onClose = vi.fn();
    const tree = createRoot(container);
    act(() => {
      tree.render(
        <TabBar
          tabs={[
            { meta: meta(2, 'a.html'), dirty: false },
            { meta: meta(3, 'b.css'), dirty: true },
          ]}
          activeId={3}
          onActivate={onActivate}
          onClose={onClose}
        />,
      );
    });
    const tabButtons = Array.from(container.querySelectorAll('[role="tab"]'));
    expect(tabButtons).toHaveLength(2);
    const active = tabButtons.find((b) => b.getAttribute('aria-current') === 'true');
    expect(active?.textContent).toContain('b.css');
    expect(active?.textContent).toContain('未保存'); // dirty 圆点文本替代（可访问性）
    act(() => {
      tabButtons[0]?.click();
    });
    expect(onActivate).toHaveBeenCalledWith(2);
    const closeButtons = Array.from(container.querySelectorAll('button[aria-label^="关闭标签"]'));
    act(() => {
      closeButtons.find((b) => b.getAttribute('aria-label') === '关闭标签 b.css')?.click();
    });
    expect(onClose).toHaveBeenCalledWith(3);
    tree.unmount();
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `npm run test:unit -- tests/unit/renderer/tab-bar.test.tsx`
Expected: FAIL（Cannot find module TabBar）。

- [ ] **Step 3: 实现 TabBar.tsx（全文）**

```tsx
/**
 * 标签条（M4 spec §3）：纯呈现——文件名 + dirty 状态文本 + 关闭钮；激活态 aria-current；
 * key 用稳定业务 id（宪法 A.7-6）。关闭钮点击需 stopPropagation 防冒泡触发激活。
 */
import type { TabState } from './tabModel';

export interface TabBarProps {
  readonly tabs: readonly TabState[];
  readonly activeId: number | null;
  onActivate(id: number): void;
  onClose(id: number): void;
}

export function TabBar({ tabs, activeId, onActivate, onClose }: TabBarProps): React.JSX.Element {
  return (
    <div className="lt-tabbar" role="tablist" aria-label="打开的文件">
      {tabs.map((tab) => (
        <div key={tab.meta.id} className="lt-tab" role="presentation">
          <button
            type="button"
            role="tab"
            aria-current={tab.meta.id === activeId ? 'true' : undefined}
            onClick={() => onActivate(tab.meta.id)}
          >
            {tab.meta.name}
            {tab.dirty ? <span aria-label="未保存">未保存</span> : null}
          </button>
          <button
            type="button"
            aria-label={`关闭标签 ${tab.meta.name}`}
            onClick={() => onClose(tab.meta.id)}
          >
            ×
          </button>
        </div>
      ))}
    </div>
  );
}
```

- [ ] **Step 4: Workspace 会话中枢改造（修改锚点：全文演进，保留 ROOT_NODE/replaceNode 既有结构）**

Workspace 演进要点（代码块给关键新增/替换段，实施者按锚点合并）：

```tsx
// —— state 区替换 ——
  const [tabsOp, setTabsOp] = useState<TabsOp>({ tabs: [], activeId: null });
  const sessionsRef = useRef<TabSessions | null>(null);
  if (sessionsRef.current === null) sessionsRef.current = new TabSessions(); // 惰性单例（渲染期只建一次）
  const sessions = sessionsRef.current;
  const activeTab = tabsOp.tabs.find((t) => t.meta.id === tabsOp.activeId) ?? null;
```

```tsx
// —— openFile：非文本前置拦截 + readFile 成功后才建会话与标签（readFile 失败 toast）——
  function openFile(node: NodeMeta): void {
    if (node.mimeType === null || !isTextLike(node.mimeType)) {
      showToast('二进制文件暂不支持编辑（FR-EDIT-04 归后续批次）');
      return;
    }
    void window.api.readFile({ nodeId: node.id }).then((result) => {
      if (!result.ok) {
        showToast(`打开失败：${result.error.message}`);
        return;
      }
      if (sessions.has(node.id)) {
        setTabsOp((prev) => openTab(prev, node));
        return;
      }
      sessions.open(
        node.id,
        createEditorState(new TextDecoder().decode(result.value.content), node.mimeType ?? 'text/plain', {
          onDocChanged: (text) => {
            sessions.updateState(node.id, /* 见实现注 */);
            saveControllerRef.current?.edit(node.id, text);
          },
          onScroll: (top) => sessions.updateScroll(node.id, top),
        }),
      );
      setTabsOp((prev) => openTab(prev, node));
    });
  }
```

**实现注：**`onDocChanged` 内的 `sessions.updateState` 需要库新 state——createEditorState 的 handler 签名在 Task 3 基础上扩为 `onDocChanged(text: string, state: EditorState)`（同步改 codemirror.ts 的 updateListener 回传 `update.state`，editor-panel.test 对应断言不变——实施者把该签名演进并入本任务并跑通 Task 3 测试），回调体即 `sessions.updateState(node.id, state); saveControllerRef.current?.edit(node.id, text);`。`saveControllerRef` 本任务声明为 `useRef<SaveController | null>(null)`（Task 5 实装；暂不赋值——`?.` 空转）。`showToast` 为 Task 4 同批落地的最小 toast（见 Step 5）；`isTextLike` 从原 EditorPanel 迁入 Workspace（原组件内同名函数删除——编辑器不再有二进制分支）。`onTrash` 演进：trash 成功后若标签存在该节点 → `sessions.close(id)` + `setTabsOp((prev) => closeTab(prev, id))`；`selected` 概念由 activeTab 取代：`TreePanel selectedId={tabsOp.activeId}`、`EditorPanel activeTab={activeTab}`、`PreviewPanel node={activeTab?.meta ?? null}`；树 onSelect 分支：`node.nodeType === 'file' ? openFile(node) : onToggle(node.id)`（原 TreePanel onClick 已按类型分流，Workspace 侧 onSelect 只收 file——实施者核对 TreePanel props 语义后保持现分流不变）。

```tsx
// —— 渲染段编辑器/预览槽替换 ——
      <section className="lt-pane lt-pane-editor">
        <EditorPanel
          sessions={sessions}
          activeTab={activeTab}
          debounceMs={debounceMs}
          onDocChanged={(id, text) => saveControllerRef.current?.edit(id, text)}
          onSaveRequest={() => saveControllerRef.current?.flushActive()}
        />
      </section>
```

（`onSaveRequest` 为 Task 3 落地注里预留的可选 props；TabBar 插入编辑器栏顶部：`<section className="lt-pane lt-pane-editor">{tabsOp.tabs.length > 0 ? <TabBar ... /> : null}<EditorPanel ... /></section>`。）

- [ ] **Step 5: 最小 toast（本任务同批落地）**

`src/renderer/src/features/ui/Toast.tsx`（新建）：

```tsx
/**
 * 最小单例 toast（M4 spec §5.5 D11）：右下角队列、同屏最多 3 条、3s 自动消退、
 * aria-live polite；模块级订阅器 + ToastHost 组件消费（避免每处操作传回调）。
 */
import { useEffect, useState } from 'react';

interface ToastItem {
  readonly id: number;
  readonly text: string;
}
type Listener = (items: readonly ToastItem[]) => void;
const listeners = new Set<Listener>();
let queue: readonly ToastItem[] = [];
let seq = 0;

/** 业务侧唯一入口（模块级单例）：入队 3s 自动消退，同屏最多 3 条（挤出最旧） */
export function showToast(text: string): void {
  seq += 1;
  const item: ToastItem = { id: seq, text };
  queue = [...queue.slice(-2), item];
  for (const listener of listeners) listener(queue);
  setTimeout(() => {
    queue = queue.filter((t) => t.id !== item.id);
    for (const listener of listeners) listener(queue);
  }, 3000);
}

export function ToastHost(): React.JSX.Element {
  const [items, setItems] = useState<readonly ToastItem[]>([]);
  useEffect(() => {
    const listener: Listener = setItems;
    listeners.add(listener);
    return () => {
      listeners.delete(listener);
    };
  }, []);
  return (
    <div className="lt-toasts" aria-live="polite">
      {items.map((item) => (
        <div key={item.id} className="lt-toast">
          {item.text}
        </div>
      ))}
    </div>
  );
}
```

（App.tsx 挂载 `<ToastHost />` 于 `<main>` 尾部；`setTimeout` 句柄不入 ref——单例队列自身幂等，句柄泄漏面为 3s 定长。）

- [ ] **Step 6: 既有测试与桥桩适配**

- `panels-preview-workspace.test.tsx`：Workspace 用例的桥桩增 `getNode`、`onShellCommand`（返回 unsub 收集）、`settingsGet` 返回 v2 全量（DEFAULT_SETTINGS 形态）；断言 `listChildren` 调用与树渲染保持；「未选中文件」占位断言改经 EditorPanel 空态（文案不变）。
- `app.test.tsx` / `entry.test.ts`：桥桩同步 v2 设置形态 + `onShellCommand`/`getNode`/`forceClose` 成员（照 M3 适配先例：挂载路径实际调用的方法最小注入）。

- [ ] **Step 7: 全量验证 + Commit**

Run: `npm run test:unit && npm run typecheck && npm run lint && npm run format:check`
Expected: 全绿。

```bash
git add src/renderer tests/unit/renderer
git commit -m "feat(m4): 多标签会话中枢——TabBar/TabSessions/openFile 前置拦截与最小 toast"
```

---

### Task 5: SaveController 接线（双计时器写管线 + 关标签 flush + trash 联动）

**Files:**
- Create: `src/renderer/src/features/editor/saveController.ts`
- Modify: `src/renderer/src/features/workspace/Workspace.tsx`（实装 saveControllerRef + onDocChanged→edit + 关标签 flush）
- Create: `tests/unit/editor/saveController.test.ts`

**Interfaces:**
- Consumes: Task 2 `reduceSave`/`initialSaveState`、Task 4 Workspace 装配点
- Produces:

```ts
export interface SaveControllerDeps {
  readonly debounceMs: () => number;   // 设置运行时读取（Workspace 持 settings 态）
  readonly autoSaveMs: () => number;
  readonly getDoc: (nodeId: number) => string | null;   // 发起写时刻的 doc 快照来源（TabSessions）
  readonly write: (nodeId: number, text: string) => Promise<boolean>; // false=失败（Result 已由桥封装）
  readonly onDirtyChange: (nodeId: number, dirty: boolean) => void;
}
export class SaveController {
  constructor(deps: SaveControllerDeps);
  edit(nodeId: number, text: string): void;  // 输入入口（EditorPanel onDocChanged 直连）
  flush(nodeId: number): void;               // Ctrl+S / 关标签立即写
  flushActive(): void;                       // 兼容入口：由外部告知当前激活 id（setActiveNode）
  setActiveNode(nodeId: number | null): void;
  tabClosed(nodeId: number): void;           // 关标签后清理（计时器/状态）
  dispose(): void;                           // 卸载清全部计时器（成对释放）
}
```

- [ ] **Step 1: 写失败测试 tests/unit/editor/saveController.test.ts（全文）**

```ts
// SaveController（M4 spec §2）：纯状态机 + 真计时器句柄的接线层——fake timers 全链路：
// 尾沿写、挂起强制写、在途串行、失败重试、flush、dispose 清理（成对释放断言）
import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest';
import { SaveController } from '../../../src/renderer/src/features/editor/saveController';

function makeDeps() {
  return {
    debounceMs: () => 300,
    autoSaveMs: () => 3000,
    getDoc: vi.fn((nodeId: number) => `doc-${String(nodeId)}`),
    write: vi.fn(() => Promise.resolve(true)),
    onDirtyChange: vi.fn(),
  };
}

let controller: SaveController;
let deps: ReturnType<typeof makeDeps>;
beforeEach(() => {
  vi.useFakeTimers();
  deps = makeDeps();
  controller = new SaveController(deps);
});
afterEach(() => {
  controller.dispose();
  vi.useRealTimers();
});

describe('SaveController', () => {
  it('尾沿去抖：edit 后 299ms 不写、300ms 写一次（快照=getDoc 当时值）', () => {
    controller.edit(2, '甲');
    expect(deps.onDirtyChange).toHaveBeenCalledWith(2, true);
    vi.advanceTimersByTime(299);
    expect(deps.write).not.toHaveBeenCalled();
    vi.advanceTimersByTime(1);
    expect(deps.write).toHaveBeenCalledTimes(1);
    expect(deps.write).toHaveBeenCalledWith(2, 'doc-2');
  });

  it('最长挂起：连续 edit（间隔 < 去抖）超 autoSaveMs 强制落库一次', () => {
    controller.edit(2, '一'); // t=0
    vi.advanceTimersByTime(500);
    controller.edit(2, '一二'); // t=500 尾沿推到 800
    vi.advanceTimersByTime(500); // t=1000：deadline 3000 未到、尾沿 800 已过——应已写
    expect(deps.write).toHaveBeenCalledTimes(1);
    expect(deps.write).toHaveBeenCalledWith(2, '一二');
  });

  it('写在途时新输入不并发：writeDone 后按新窗口补写', async () => {
    let resolveWrite: (v: boolean) => void = () => {};
    deps.write = vi.fn(
      () => new Promise<boolean>((resolve) => {
        resolveWrite = resolve;
      }),
    );
    controller.edit(2, '甲');
    vi.advanceTimersByTime(300); // 触发首写（在途）
    expect(deps.write).toHaveBeenCalledTimes(1);
    controller.edit(2, '甲乙'); // 在途输入 → pending
    vi.advanceTimersByTime(5000); // 期间不再发起第二写（串行）
    expect(deps.write).toHaveBeenCalledTimes(1);
    resolveWrite(true); // 在途完成
    await vi.advanceTimersByTimeAsync(0); // 微任务冲刷 → 重调度
    vi.advanceTimersByTime(300);
    expect(deps.write).toHaveBeenCalledTimes(2);
    expect(deps.write).toHaveBeenLastCalledWith(2, '甲乙');
  });

  it('写失败按 autoSaveMs 重试；成功后 dirty 归零回调', async () => {
    deps.write = vi.fn().mockResolvedValueOnce(false).mockResolvedValueOnce(true);
    controller.edit(2, '甲');
    await vi.advanceTimersByTimeAsync(300);
    expect(deps.onDirtyChange).toHaveBeenLastCalledWith(2, true);
    await vi.advanceTimersByTimeAsync(3000); // 重试
    expect(deps.write).toHaveBeenCalledTimes(2);
    expect(deps.onDirtyChange).toHaveBeenLastCalledWith(2, false);
  });

  it('flush 立即写；tabClosed/dispose 清计时器不再写', () => {
    controller.edit(2, '甲');
    controller.flush(2);
    expect(deps.write).toHaveBeenCalledTimes(1);
    controller.edit(3, '乙');
    controller.tabClosed(3);
    vi.advanceTimersByTime(60000);
    expect(deps.write).toHaveBeenCalledTimes(1); // 3 号已清
    controller.edit(4, '丙');
    controller.dispose();
    vi.advanceTimersByTime(60000);
    expect(deps.write).toHaveBeenCalledTimes(1); // 全部清空
  });
});
```

（`advanceTimersByTimeAsync` 为 Vitest 5 API——异步写 Promise 冲刷用它；若与 fake timers 兼容性实测有出入，改「手动 await Promise.resolve() ×N + advanceTimersByTime」组合，报告留证。）

- [ ] **Step 2: 运行确认失败**

Run: `npm run test:unit -- tests/unit/editor/saveController.test.ts`
Expected: FAIL（Cannot find module saveController）。

- [ ] **Step 3: 实现 saveController.ts（全文）**

```ts
/**
 * 保存管线控制器（M4 spec §2）：saveModel 纯状态机 + 真计时器句柄 + 注入式写的接线层。
 * per-tab 一份状态（同标签写串行由 inflight 态保证）；计时器到期取「先到者」——两个绝对
 * 时刻都武装 setTimeout，先触发者先 reduce（后到者由 writeStart 清空，天然幂等）。
 * dispose/tabClosed 成对释放（宪法资源纪律）。
 */
import { initialSaveState, reduceSave, type SaveEvent, type SaveState } from './saveModel';
import type { SaveControllerDeps } from './saveControllerDeps';

interface TabTimers {
  trailing?: ReturnType<typeof setTimeout>;
  deadline?: ReturnType<typeof setTimeout>;
}

export class SaveController {
  private states = new Map<number, SaveState>();
  private timers = new Map<number, TabTimers>();
  private activeNodeId: number | null = null;
  private disposed = false;

  constructor(private readonly deps: SaveControllerDeps) {}

  /** 激活节点告知（flushActive 语义基准；Workspace 在 activeTab 变化时同步） */
  setActiveNode(nodeId: number | null): void {
    this.activeNodeId = nodeId;
  }

  /** 输入入口：EditorPanel onDocChanged 直连 */
  edit(nodeId: number, _text: string): void {
    this.dispatch(nodeId, { type: 'edit', at: Date.now(), debounceMs: this.deps.debounceMs(), autoSaveMs: this.deps.autoSaveMs() });
  }

  /** 立即写（Ctrl+S / 关标签前置）：快照取 getDoc 当时值 */
  flush(nodeId: number): void {
    this.dispatch(nodeId, { type: 'flush', at: Date.now(), debounceMs: this.deps.debounceMs(), autoSaveMs: this.deps.autoSaveMs() });
  }

  /** 菜单「保存」兼容入口：仅对当前激活标签生效（无激活 no-op） */
  flushActive(): void {
    if (this.activeNodeId !== null) this.flush(this.activeNodeId);
  }

  /** 关标签后清理：计时器与状态一并移除（调用方保证 flush 已先行） */
  tabClosed(nodeId: number): void {
    this.clearTimers(nodeId);
    this.states.delete(nodeId);
  }

  dispose(): void {
    this.disposed = true;
    for (const nodeId of [...this.states.keys()]) this.clearTimers(nodeId);
    this.states.clear();
  }

  private clearTimers(nodeId: number): void {
    const timers = this.timers.get(nodeId);
    if (timers?.trailing !== undefined) clearTimeout(timers.trailing);
    if (timers?.deadline !== undefined) clearTimeout(timers.deadline);
    this.timers.delete(nodeId);
  }

  private dispatch(nodeId: number, event: SaveEvent): void {
    if (this.disposed) return;
    const current = this.states.get(nodeId) ?? initialSaveState;
    const outcome = reduceSave(current, event);
    this.states.set(nodeId, outcome.state);
    if (outcome.state.dirty !== current.dirty) {
      this.deps.onDirtyChange(nodeId, outcome.state.dirty);
    }
    this.syncTimers(nodeId, outcome.state);
    if (outcome.write) this.startWrite(nodeId);
  }

  /** 按双时点重武装计时器（remaining 差值；已到期/空时刻不武装） */
  private syncTimers(nodeId: number, state: SaveState): void {
    this.clearTimers(nodeId);
    if (this.disposed) return;
    const timers: TabTimers = {};
    const now = Date.now();
    if (state.trailingAt !== null) {
      timers.trailing = setTimeout(() => this.dispatch(nodeId, { type: 'timerFired', at: Date.now() }), Math.max(0, state.trailingAt - now));
    }
    if (state.deadlineAt !== null) {
      timers.deadline = setTimeout(() => this.dispatch(nodeId, { type: 'timerFired', at: Date.now() }), Math.max(0, state.deadlineAt - now));
    }
    this.timers.set(nodeId, timers);
  }

  private startWrite(nodeId: number): void {
    this.dispatch(nodeId, { type: 'writeStart' });
    const text = this.deps.getDoc(nodeId);
    if (text === null) {
      // 会话已不存在（竞态关闭）：直接落写失败重试语义会无谓重试——按完成清态处理
      this.dispatch(nodeId, { type: 'writeDone', at: Date.now(), debounceMs: this.deps.debounceMs(), autoSaveMs: this.deps.autoSaveMs() });
      return;
    }
    void this.deps.write(nodeId, text).then((ok) => {
      if (ok) {
        this.dispatch(nodeId, { type: 'writeDone', at: Date.now(), debounceMs: this.deps.debounceMs(), autoSaveMs: this.deps.autoSaveMs() });
      } else {
        this.dispatch(nodeId, { type: 'writeFail', at: Date.now(), autoSaveMs: this.deps.autoSaveMs() });
      }
    });
  }
}
```

（`SaveControllerDeps` 类型抽到 `saveControllerDeps.ts` 或同文件导出均可——以「Task 4/5 产出签名逐字」为准；`_text` 参数保留位置语义（编辑内容以 getDoc 快照为准，edit 事件不携带内容避免双源）。）

- [ ] **Step 4: Workspace 接线（修改锚点）**

```tsx
// —— 实装 saveController（Task 4 的 useRef 空位）——
  const saveControllerRef = useRef<SaveController | null>(null);
  if (saveControllerRef.current === null) {
    saveControllerRef.current = new SaveController({
      debounceMs: () => debounceMs,
      autoSaveMs: () => autoSaveMs,
      getDoc: (id) => sessions.get(id)?.state.doc.toString() ?? null,
      write: (id, text) =>
        window.api.writeFile({ nodeId: id, content: new TextEncoder().encode(text) }).then((r) => r.ok),
      onDirtyChange: (id, dirty) => setTabsOp((prev) => setTabDirty(prev, id, dirty)),
    });
  }
  const saveController = saveControllerRef.current;
```

（新增 `const [autoSaveMs, setAutoSaveMs] = useState(3000);` 并在 settingsGet 装配处一并 set；`onDocChanged` 直连 `saveController.edit`（替换 Task 4 的 `?.` 空转）；`EditorPanel onSaveRequest={() => saveController.flushActive()}`；保存钮语义由此贯通。）

```tsx
// —— 关标签 flush 后关（spec §2.2-5）+ active 同步 ——
  function closeTabById(id: number): void {
    saveController.flush(id); // 失败态（节点已删等）由确认放弃归 M4 打磨批次外——flush 后即关
    saveController.tabClosed(id);
    sessions.close(id);
    setTabsOp((prev) => closeTab(prev, id));
  }
  useEffect(() => {
    saveController.setActiveNode(tabsOp.activeId);
  }, [tabsOp.activeId, saveController]);
  // 卸载清全部计时器（成对释放）
  useEffect(() => {
    return () => {
      saveController.dispose();
    };
  }, [saveController]);
```

（`onTrash` 内删除标签走 `closeTabById`；`window.confirm` 原生可用。）

- [ ] **Step 5: 全量验证 + Commit**

Run: `npm run test:unit && npm run typecheck && npm run lint && npm run format:check`
Expected: 全绿（saveController fake timers 全链路 100%）。

```bash
git add src/renderer tests/unit/editor
git commit -m "feat(m4): SaveController 接线——尾沿+挂起双计时写管线/在途串行/失败重试/关标签 flush"
```

---

### Task 6: 原生菜单与命令通道（含 unsaved-guard 链）

**Files:**
- Create: `src/main/menu/menu.ts`、`tests/unit/main/menu.test.ts`
- Modify: `src/shared/ipc.ts`/`window-api.ts`/`preload/index.ts`（Task 1 已立契约，本任务仅 guard 语义核对）、`src/main/app.ts`、`src/renderer/src/features/workspace/Workspace.tsx`
- Modify: `tests/unit/main/ipc.test.ts`（app.test 若断言 menu 装配则适配）

**Interfaces:**
- Consumes: Task 1 `ShellCommand`/`onShellCommand`/`forceClose`/`IPC.shellCommand`/`IPC.shellForceClose`
- Produces:
  - `createMenuTemplate(isMac: boolean): MenuItemConstructorOptions[]`（`id: 'menu-new-file' | 'menu-new-dir' | 'menu-save'` 供 E2E 按 id 触发）
  - `installApplicationMenu(): void`（Menu.setApplicationMenu）
  - `attachWindowCloseGuard(win: BrowserWindow, isAllowed: { value: boolean }): void`（close 拦截 → confirm-close 命令）

- [ ] **Step 1: 写失败测试 tests/unit/main/menu.test.ts（全文）**

```ts
// 菜单装配（M4 spec §5.2 D5）：模板结构（文件/搜索/导入导出 + id/accelerator/disabled）+ 命令单通道转发
import { describe, expect, it, vi, beforeEach } from 'vitest';
import { BrowserWindow, Menu } from 'electron';
import { IPC } from '../../../src/shared/ipc';
import { createMenuTemplate, installApplicationMenu } from '../../../src/main/menu/menu';

vi.mock('electron', () => ({
  app: { getPath: vi.fn(() => '/tmp'), whenReady: vi.fn(() => Promise.resolve()), on: vi.fn() },
  BrowserWindow: { getAllWindows: vi.fn(() => [{ webContents: { send: vi.fn() } }]) },
  Menu: { buildFromTemplate: vi.fn((t: unknown) => t), setApplicationMenu: vi.fn() },
  protocol: { registerSchemesAsPrivileged: vi.fn(), handle: vi.fn() },
}));

describe('createMenuTemplate', () => {
  it('文件菜单含新建文件/新建目录/保存（id 与 accelerator 逐字），搜索与导入导出 disabled 占位', () => {
    const template = createMenuTemplate(false) as Array<{ label: string; submenu: Array<Record<string, unknown>> }>;
    const fileMenu = template.find((m) => m.label === '文件');
    expect(fileMenu).toBeDefined();
    const items = fileMenu?.submenu ?? [];
    const save = items.find((item) => item.id === 'menu-save');
    expect(save?.['accelerator']).toBe('CmdOrCtrl+S');
    expect(save?.['enabled']).toBeUndefined(); // 可用项不设 disabled
    const searchMenu = template.find((m) => m.label === '搜索');
    const quickOpen = (searchMenu?.submenu ?? []).find((item) => item['label'] === '快速打开');
    expect(quickOpen?.['enabled']).toBe(false); // M5 搜索 UI 前占位
    const ioMenu = template.find((m) => m.label === '导入导出');
    expect(ioMenu).toBeDefined();
  });

  it('点击保存项 → 单通道 shell:command 下发 { type: "save" }（遍历全部窗口）', () => {
    const template = createMenuTemplate(false) as Array<{ label: string; submenu: Array<Record<string, unknown>> }>;
    const fileMenu = template.find((m) => m.label === '文件');
    const save = (fileMenu?.submenu ?? []).find((item) => item['id'] === 'menu-save');
    (save?.['click'] as () => void)();
    const sendMock = (BrowserWindow.getAllWindows()[0] as { webContents: { send: ReturnType<typeof vi.fn> } }).webContents.send;
    expect(sendMock).toHaveBeenCalledWith(IPC.shellCommand, { type: 'save' });
  });

  it('macOS 模板首项为 appMenu role；Windows 非 mac 无', () => {
    const mac = createMenuTemplate(true) as Array<Record<string, unknown>>;
    expect(mac[0]?.['role']).toBe('appMenu');
    const win = createMenuTemplate(false) as Array<Record<string, unknown>>;
    expect(win[0]?.['label']).toBe('文件');
  });
});

describe('installApplicationMenu', () => {
  beforeEach(() => {
    vi.clearAllMocks();
  });
  it('buildFromTemplate + setApplicationMenu 各一次', () => {
    installApplicationMenu();
    expect(Menu.buildFromTemplate).toHaveBeenCalledTimes(1);
    expect(Menu.setApplicationMenu).toHaveBeenCalledTimes(1);
  });
});
```

（`BrowserWindow.getAllWindows` mock 的 send 取用方式以实跑适配——vi.mock 工厂里把 send 也做成 vi.fn 导出更稳：实施者可在 mock 工厂加 `const sendMock = vi.fn()` 并暴露；断言等价迁移，报告留证。）

- [ ] **Step 2: 运行确认失败**

Run: `npm run test:unit -- tests/unit/main/menu.test.ts`
Expected: FAIL（Cannot find module menu）。

- [ ] **Step 3: 实现 src/main/menu/menu.ts（全文）**

```ts
/**
 * 应用菜单装配（M4 spec §5.2 裁决 D5）：骨架一次到位——文件（新建/保存）可用、
 * 搜索与导入导出 disabled 占位（M5 往缝里填）；命令经 shell:command 单通道下发
 * （可辨识联合，渲染层 switch never 穷举兜底）；快捷键由 accelerator 承载，渲染层
 * 不再自挂键盘监听（键位单一来源）。
 */
import { BrowserWindow, Menu } from 'electron';
import type { BrowserWindow as BrowserWindowType, MenuItemConstructorOptions } from 'electron';
import { IPC } from '../../shared/ipc';
import type { ShellCommand } from '../../shared/shell-contract';

/** 命令单通道下发：遍历全部窗口（M3 broadcast 同款纪律） */
function sendCommand(command: ShellCommand): void {
  for (const win of BrowserWindow.getAllWindows()) {
    win.webContents.send(IPC.shellCommand, command);
  }
}

/** 菜单模板（isMac 分支：app 菜单平台惯例；导出供单测与装配复用） */
export function createMenuTemplate(isMac: boolean): MenuItemConstructorOptions[] {
  const fileSubmenu: MenuItemConstructorOptions[] = [
    { id: 'menu-new-file', label: '新建文件', accelerator: 'CmdOrCtrl+N', click: () => sendCommand({ type: 'new-file' }) },
    { id: 'menu-new-dir', label: '新建目录', accelerator: 'CmdOrCtrl+Shift+N', click: () => sendCommand({ type: 'new-dir' }) },
    { id: 'menu-save', label: '保存', accelerator: 'CmdOrCtrl+S', click: () => sendCommand({ type: 'save' }) },
  ];
  if (!isMac) fileSubmenu.push({ type: 'separator' }, { role: 'quit', label: '退出' });
  const template: MenuItemConstructorOptions[] = [
    { label: '文件', submenu: fileSubmenu },
    {
      label: '搜索',
      submenu: [
        { label: '快速打开', accelerator: 'CmdOrCtrl+P', enabled: false }, // M5 搜索 UI
        { label: '全局搜索', accelerator: 'CmdOrCtrl+Shift+F', enabled: false }, // M5
      ],
    },
    {
      label: '导入导出',
      submenu: [
        { label: '导入…', enabled: false }, // M5
        { label: '导出…', enabled: false }, // M5
      ],
    },
  ];
  if (isMac) template.unshift({ role: 'appMenu' });
  return template;
}

/** 装配应用菜单（whenReady 后、窗口创建后调用一次） */
export function installApplicationMenu(): void {
  Menu.setApplicationMenu(Menu.buildFromTemplate(createMenuTemplate(process.platform === 'darwin')));
}

/**
 * 窗口关闭拦截 guard（M4 spec §2.3 D3）：首次 close 一律拦截并发 confirm-close 命令；
 * 渲染层判定无脏直接 forceClose、有脏弹 confirm 后再 forceClose——放行标记由
 * registerIpcHandlers 的 requestClose 置位（同对象引用）。
 */
export function attachWindowCloseGuard(win: BrowserWindowType, allow: { value: boolean }): void {
  win.on('close', (event) => {
    if (allow.value) return;
    event.preventDefault();
    win.webContents.send(IPC.shellCommand, { type: 'confirm-close' });
  });
}
```

- [ ] **Step 4: app.ts 装配（修改锚点）**

```ts
// createMainWindow 返回值改为 BrowserWindow（供 requestClose 引用）：
function createMainWindow(devServerUrl: string | undefined, allowedOrigins: readonly string[], allow: { value: boolean }): BrowserWindowType {
  // ……既有构造不变……
  attachWindowCloseGuard(win, allow); // close 拦截 guard（M4 spec §2.3）
  // ……加载逻辑不变……
  return win;
}
```

```ts
// bootstrapMain：whenReady 块内
      const allowClose = { value: false }; // guard 放行标记（forceClose 唯一写点）
      registerIpcHandlers({
        allowedOrigins: allowed, vfs, search, settings, broadcast,
        requestClose: () => {
          allowClose.value = true; // 放行后重入 close 事件直通（B.3-1 顺序内，窗口句柄经 winRef）
          winRef.current?.close();
        },
      });
      const winRef: { current: BrowserWindowType | null } = { current: null };
      winRef.current = createMainWindow(devServerUrl, allowed, allowClose);
      installApplicationMenu();
```

（注意 `winRef` 声明需在 `registerIpcHandlers` 之前（闭包捕获）——把两行顺序排好；`winRef` 类型 import type；原有 `createMainWindow(devServerUrl, allowed)` 调用替换。`context-menu` 检查元素在 Task 9 一并接入 createMainWindow（本任务不动）。）

- [ ] **Step 5: Workspace 命令 dispatch + guard 链（修改锚点）**

```tsx
  // 外壳命令订阅（cleanup 成对）：菜单命令 dispatch + 关窗确认链（spec §2.3/§5.2）
  const dirtyRef = useRef(false);
  useEffect(() => {
    dirtyRef.current = tabsOp.tabs.some((t) => t.dirty);
  }, [tabsOp]);
  useEffect(() => {
    return window.api.onShellCommand((command) => {
      switch (command.type) {
        case 'save':
          saveController.flushActive();
          break;
        case 'new-file':
          createInContext('file');
          break;
        case 'new-dir':
          createInContext('dir');
          break;
        case 'confirm-close':
          if (!dirtyRef.current) {
            void window.api.forceClose();
            return;
          }
          if (window.confirm('有未保存的更改，确定退出？')) {
            void window.api.forceClose();
          }
          break;
      }
    });
  }, [saveController]);
```

（`createInContext(nodeType)` = 原 onCreate 语义抽函数复用（树按钮与菜单共用）；switch 四分支穷举 ShellCommand 联合，无需 default（never 兜底由穷举性承担，宪法 A.1-4）。）

- [ ] **Step 6: 全量验证 + Commit**

Run: `npm run test:unit && npm run test:integration && npm run typecheck && npm run lint && npm run format:check && npm run build && npm run test:e2e`
Expected: 全绿（既有 e2e 2+9 用例回归——菜单装配后 app.spec 不受影响；`requestClose` 占位已换真实现）。

```bash
git add src/main src/renderer tests/unit/main tests/unit/renderer
git commit -m "feat(m4): 原生菜单骨架与命令单通道——新建/保存快捷键、close 拦截 guard 确认链、disabled 占位"
```

---

### Task 7: 三栏折叠/宽度拖拽与布局记忆（FR-SHELL-01）

**Files:**
- Create: `src/renderer/src/features/workspace/layoutModel.ts`、`tests/unit/workspace/layoutModel.test.ts`
- Modify: `src/renderer/src/features/workspace/Workspace.tsx`（布局态 + settings 持久化）、`src/renderer/src/App.tsx` 如需全局样式锚点（样式经 class，无新 CSP 面）

**Interfaces:**
- Consumes: Task 1 `ShellLayout`/`DEFAULT_LAYOUT`/settings get·set
- Produces:

```ts
// layoutModel.ts
export function clampRatio(ratio: number): number;                       // 0.15–0.6 钳制
export function ratioFromPointer(containerWidth: number, offsetPx: number): number; // 指针→比例（clamp 后）
```

- [ ] **Step 1: 写失败测试 tests/unit/workspace/layoutModel.test.ts（全文）**

```ts
// 布局纯函数（M4 spec §5.1）：比例钳制 0.15–0.6、指针坐标→比例
import { describe, expect, it } from 'vitest';
import { clampRatio, ratioFromPointer } from '../../../src/renderer/src/features/workspace/layoutModel';

describe('layoutModel', () => {
  it('clampRatio：0.15/0.6 收，越界双向钳制', () => {
    expect(clampRatio(0.25)).toBe(0.25);
    expect(clampRatio(0.1)).toBe(0.15);
    expect(clampRatio(0.9)).toBe(0.6);
  });

  it('ratioFromPointer：偏移/容器宽即比例并钳制；零宽容器返下界（防除零）', () => {
    expect(ratioFromPointer(1000, 250)).toBe(0.25);
    expect(ratioFromPointer(1000, 50)).toBe(0.15);
    expect(ratioFromPointer(0, 100)).toBe(0.15);
  });
});
```

- [ ] **Step 2: 运行确认失败** → Run: `npm run test:unit -- tests/unit/workspace/layoutModel.test.ts` → Expected: FAIL。

- [ ] **Step 3: 实现 layoutModel.ts（全文）**

```ts
/**
 * 三栏布局纯函数（M4 spec §5.1 D5）：宽度比例合法域 0.15–0.6（编辑器自适应占余）；
 * 指针→比例换算带钳制（拖拽分隔条用），零宽容器（折叠态/未布局）返下界防除零。
 */
const MIN_RATIO = 0.15;
const MAX_RATIO = 0.6;

export function clampRatio(ratio: number): number {
  return Math.min(MAX_RATIO, Math.max(MIN_RATIO, ratio));
}

export function ratioFromPointer(containerWidth: number, offsetPx: number): number {
  if (containerWidth <= 0) return MIN_RATIO;
  return clampRatio(offsetPx / containerWidth);
}
```

- [ ] **Step 4: Workspace 布局态接线（修改锚点）**

```tsx
  const [layout, setLayout] = useState<ShellLayout>(DEFAULT_LAYOUT);
  // settingsGet 装配处一并恢复：setLayout(result.value.shell.layout)、setAutoSaveMs(result.value.editor.autoSaveMs)
  function updateLayout(patch: Partial<ShellLayout>): void {
    setLayout((prev) => {
      const next = { ...prev, ...patch };
      void window.api.settingsGet().then((r) => {
        if (r.ok) void window.api.settingsSet({ ...r.value, shell: { layout: next } }); // get→merge→set（M3 §5 语义）
      });
      return next;
    });
  }
```

```tsx
  // —— 渲染段：grid 模板列内联 + 折叠类 + 分隔条 ——
  const gridColumns = [
    layout.treeCollapsed ? '8px' : `${(layout.treeWidthRatio * 100).toFixed(2)}%`,
    layout.treeCollapsed ? '8px' : '4px', // 分隔条
    '4px', // 编辑器前分隔条（固定宽）
    layout.editorCollapsed ? '0px' : '1fr',
    layout.previewCollapsed ? '8px' : '4px',
    layout.previewCollapsed ? '8px' : `${(layout.previewWidthRatio * 100).toFixed(2)}%`,
  ].join(' ');
```

```tsx
  // 分隔条拖拽（事件处理器内副作用；指针捕获成对释放）
  function onDividerPointerDown(side: 'tree' | 'preview', e: React.PointerEvent<HTMLDivElement>): void {
    e.currentTarget.setPointerCapture(e.pointerId);
    const host = e.currentTarget.parentElement;
    if (host === null) return;
    const onMove = (move: PointerEvent): void => {
      const rect = host.getBoundingClientRect();
      const offset = side === 'tree' ? move.clientX - rect.left : rect.right - move.clientX;
      updateLayout(side === 'tree' ? { treeWidthRatio: ratioFromPointer(rect.width, offset) } : { previewWidthRatio: ratioFromPointer(rect.width, offset) });
    };
    const onUp = (): void => {
      window.removeEventListener('pointermove', onMove);
      window.removeEventListener('pointerup', onUp);
    };
    window.addEventListener('pointermove', onMove);
    window.addEventListener('pointerup', onUp);
  }
```

（面板标题条折叠钮：`<button aria-label="折叠树栏" onClick={() => updateLayout({ treeCollapsed: true })}>`；折叠窄条展开钮反向；`className` 按折叠态切换；编辑器折叠时 EditorPanel 容器 `display: none` 但保持挂载（保存管线照常，spec §5.1）；grid 内联样式落 `style={{ gridTemplateColumns: gridColumns }}`；具体 DOM 细节实施者以可访问性（aria-label 中文）与既有 class 风格落地，断言锚点为 aria-label 与折叠类名。）

- [ ] **Step 5: 全量验证 + Commit**

Run: `npm run test:unit && npm run typecheck && npm run lint && npm run format:check`
Expected: 全绿。

```bash
git add src/renderer tests/unit/workspace
git commit -m "feat(m4): 三栏折叠与宽度记忆——指针比例纯函数、settings shell 域持久化"
```

---

### Task 8: 树 rename/move UI + 打磨批次收口（路径新鲜/占位态/订阅挂载一次/charset）

**Files:**
- Create: `src/renderer/src/features/tree/RenameDialog.tsx`
- Modify: `src/renderer/src/features/tree/TreePanel.tsx`（重命名/移动入口 + move 模式点选语义）、`src/renderer/src/features/workspace/Workspace.tsx`（move 模式态 + meta 同步链）、`src/renderer/src/features/preview/PreviewPanel.tsx`（订阅挂载一次 + written 前 vfs:get 反查占位态 + css 热替换触发）、`src/main/protocol/vfsProtocol.ts`（charset + 接收器注入——接收器注入在 Task 9，本任务只做 charset）
- Modify: `tests/integration/protocol/vfs-protocol.test.ts`（charset 头断言）
- Create: `tests/unit/renderer/rename-dialog.test.tsx`

**Interfaces:**
- Consumes: Task 1 `getNode`/`forceClose` 无关；`renameNode`/`moveNode` 既有桥
- Produces:
  - `RenameDialogProps: { nodeName: string; inFlight: boolean; onConfirm(name: string): void; onCancel(): void }`
  - PreviewPanel 内部：订阅 effect 依赖 `[]`（node 经 nodeRef）；`written` 命中先 `getNode` 反查，失败 → 占位态「文档不可用」
  - 协议：`text/*` Content-Type 附加 `; charset=utf-8`

- [ ] **Step 1: 写失败测试 tests/unit/renderer/rename-dialog.test.tsx（全文）**

```tsx
// @vitest-environment jsdom
// 重命名模态冒烟（M4 spec §6.2 D8）：预填当前名、确认回传新名（trim）、取消/空名不回传
import { act } from 'react';
import { createRoot } from 'react-dom/client';
import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest';
import { RenameDialog } from '../../../src/renderer/src/features/tree/RenameDialog';

let container: HTMLElement;
beforeEach(() => {
  container = document.createElement('div');
  document.body.appendChild(container);
});
afterEach(() => {
  container.remove();
  document.body.innerHTML = '';
});

describe('RenameDialog', () => {
  it('预填当前名；修改后确认回传 trim 新名；空名确认不回传', () => {
    const onConfirm = vi.fn();
    const onCancel = vi.fn();
    const tree = createRoot(container);
    act(() => {
      tree.render(<RenameDialog nodeName="旧名.html" inFlight={false} onConfirm={onConfirm} onCancel={onCancel} />);
    });
    const input = container.querySelector('input');
    expect((input as HTMLInputElement | null)?.value).toBe('旧名.html');
    act(() => {
      const setter = Object.getOwnPropertyDescriptor(HTMLInputElement.prototype, 'value')?.set;
      setter?.call(input, '  新名.html  ');
      input?.dispatchEvent(new Event('input', { bubbles: true }));
    });
    act(() => {
      container.querySelector('button[aria-label="确认重命名"]')?.click();
    });
    expect(onConfirm).toHaveBeenCalledWith('新名.html');
    act(() => {
      tree.render(<RenameDialog nodeName="旧名.html" inFlight={false} onConfirm={onConfirm} onCancel={onCancel} />);
    });
    const input2 = container.querySelector('input');
    act(() => {
      const setter = Object.getOwnPropertyDescriptor(HTMLInputElement.prototype, 'value')?.set;
      setter?.call(input2, '   ');
      input2?.dispatchEvent(new Event('input', { bubbles: true }));
    });
    act(() => {
      container.querySelector('button[aria-label="确认重命名"]')?.click();
    });
    expect(onConfirm).toHaveBeenCalledTimes(1); // 空名不回传
    act(() => {
      container.querySelector('button[aria-label="取消重命名"]')?.click();
    });
    expect(onCancel).toHaveBeenCalledTimes(1);
  });
});
```

- [ ] **Step 2: 运行确认失败** → Expected: FAIL（Cannot find module RenameDialog）。

- [ ] **Step 3: 实现 RenameDialog.tsx（全文）**

```tsx
/**
 * 行内重命名模态（M4 spec §6.2 D8）：受控 input 预填当前名；trim 后空名确认不回传；
 * inFlight 期间确认钮禁用（防重复提交）；取消回传 onCancel。Esc 取消（原生 dialog 语义简化为容器层）。
 */
import { useState } from 'react';

export interface RenameDialogProps {
  readonly nodeName: string;
  readonly inFlight: boolean;
  onConfirm(name: string): void;
  onCancel(): void;
}

export function RenameDialog({ nodeName, inFlight, onConfirm, onCancel }: RenameDialogProps): React.JSX.Element {
  const [draft, setDraft] = useState(nodeName);
  const trimmed = draft.trim();
  return (
    <div className="lt-rename" role="dialog" aria-label="重命名">
      <input
        aria-label="新名称"
        value={draft}
        onChange={(e) => {
          setDraft(e.target.value);
        }}
        autoFocus
      />
      <button
        type="button"
        aria-label="确认重命名"
        disabled={inFlight || trimmed.length === 0}
        onClick={() => {
          onConfirm(trimmed);
        }}
      >
        确定
      </button>
      <button type="button" aria-label="取消重命名" onClick={onCancel}>
        取消
      </button>
    </div>
  );
}
```

- [ ] **Step 4: TreePanel/Workspace rename/move 接线（修改锚点）**

- TreePanel props 增 `onRename(id: number): void`、`onStartMove(): void`、`moveMode: boolean`、`moveTargetId: number | null`；工具栏增「重命名」「移动到…」钮（有选中且非根时渲染；根 id=1 禁用）；moveMode 下 dir 按钮 `aria-current` 叠加目标高亮语义（`data-move-target="true"`），Workspace 决定点选语义。
- Workspace：`const [moveMode, setMoveMode] = useState<{ targetId: number | null } | null>(null);`——onSelect 分支：moveMode 非空时仅 dir 可点（设 targetId），file 点选忽略；「确认移动」钮（moveMode 非空且 targetId 非空且非自身/后代时可用）→ `moveNode({ nodeId: moveSourceId, newParentId: targetId })`；失败 toast。自身/后代判定复用树结构 walk（`isDescendant(roots, ancestorId, candidateId)` 纯函数，treeModel 追加导出）。
- **meta 同步链（spec §6.1）**：广播订阅内 `renamed/moved` → `void window.api.getNode({ nodeId: event.nodeId }).then(...)` 成功后 `setTabsOp((prev) => updateTabMeta(prev, id, meta))`（selected 即 activeTab，自然新鲜）；树侧既有 stale 重取链不变。

- [ ] **Step 5: PreviewPanel 三项打磨（修改锚点）**

```tsx
  const nodeRef = useRef<NodeMeta | null>(node);
  // 订阅挂载期一次（终审 M-4 收口）：node 经 effect 同步 ref（effect 内写 ref 合法），
  // 消除随 node 重建的退订/重订微窗口；热替换与 written 均走 ref 比较
  useEffect(() => {
    nodeRef.current = node;
  }, [node]);

  useEffect(() => {
    return window.api.onVfsChanged((broadcast) => {
      revRef.current = onBroadcast(revRef.current, broadcast.rev);
      const event = broadcast.event;
      if (event.type === 'written') {
        const current = nodeRef.current;
        // 命中当前节点：重载前 getNode 反查（路径新鲜双保险，spec §6.1）——失败置占位不重载旧路径
        if (current !== null && event.node.id === current.id) {
          void window.api.getNode({ nodeId: current.id }).then((result) => {
            if (!result.ok) {
              setUnavailable(true);
              return;
            }
            setUnavailable(false);
            reload();
          });
          return;
        }
        // CSS 热替换（M4 spec §5.4）：写入节点为 text/css → 拉新文本 postMessage 注入（替换失败静默）
        if (event.node.mimeType === 'text/css') {
          void fetch(vfsUrl(event.node.virtualPath))
            .then((res) => res.text())
            .then((text) => {
              iframeRef.current?.contentWindow?.postMessage(
                { type: 'lt:css-swap', path: event.node.virtualPath, text },
                '*',
              );
            })
            .catch(() => undefined);
        }
      }
    });
    // 订阅挂载期一次；比较基准走 nodeRef（url 读取走 urlRef 不受重建影响）
  }, []);
```

（新增 `const [unavailable, setUnavailable] = useState(false);`——渲染段 `url !== null && unavailable` 时返回占位 `<div className="lt-preview-empty">文档不可用</div>`；node 变化 effect 中重置 `setUnavailable(false)`。`mimeType: string | null` 收窄：`event.node.mimeType === 'text/css'` 即窄化。）

- [ ] **Step 6: 协议 charset（修改锚点）**

`vfsProtocol.ts` 响应头构造处：

```ts
    // text/* 附加 charset=utf-8（库内容恒 UTF-8 写入，docs/03 §3.2；终审 M-5 修非 ASCII 乱码）
    const contentType = row.mime_type.startsWith('text/')
      ? `${row.mime_type}; charset=utf-8`
      : row.mime_type;
    const headers: Record<string, string> = {
      'Content-Type': contentType,
      // ……其余头不变
```

- [ ] **Step 7: 集成测试 charset 断言更新 + 新增**

`tests/integration/protocol/vfs-protocol.test.ts`：既有 `'text/css'`/`'text/html'` 头断言改 `'text/css; charset=utf-8'`/`'text/html; charset=utf-8'`（全文搜索 `content-type` 逐处核对）；HEAD/304 断言如有同改。

- [ ] **Step 8: 全量验证 + Commit**

Run: `npm run test:unit && npm run test:integration && npm run typecheck && npm run lint && npm run format:check`
Expected: 全绿。

```bash
git add src/renderer src/main/protocol tests
git commit -m "feat(m4): 树 rename/move UI 与打磨批次——重命名模态/移动选择模式/meta 反查同步/预览占位态/charset"
```

---

### Task 9: DevTools 右键 + CSS 热替换接收器（协议注入）

**Files:**
- Modify: `src/main/app.ts`（context-menu → 检查元素）、`src/main/protocol/vfsProtocol.ts`（接收器注入）、`src/main/protocol/vfsParse.ts`（如注入函数就近落此纯函数文件则改；**裁决：`injectPreviewReceiver` 落 vfsProtocol.ts 同文件导出**——它是响应构造的一部分）
- Modify: `tests/integration/protocol/vfs-protocol.test.ts`（注入矩阵）
- Create: `tests/unit/main/menu.test.ts` 追加 context-menu handler 断言（或就近 unit/main 新 describe）

**Interfaces:**
- Consumes: M3 §7.3 协议（消息 `lt:css-swap`）；Task 8 PreviewPanel 触发端
- Produces:
  - `injectPreviewReceiver(htmlBody: string): string`（`</body>` 前插入固定接收器；无 body 标记则尾部追加）
  - 协议语义：仅 text/html **200 全量** 响应注入（304/206/HEAD/css 不注入）；ETag 基于库内 content_hash 不变（裁决 D10）

- [ ] **Step 1: 写失败测试——注入矩阵（vfs-protocol.test.ts 追加 describe）**

```ts
  it('text/html 200 注入接收器且 ETag 与注入无关；304/206/HEAD 与 css 不注入', async () => {
    const htmlRes = await handler(req(requestUrl('/笔记/index.html')));
    const htmlText = await htmlRes.text();
    expect(htmlText).toContain('lt:css-swap'); // 接收器标记
    expect(htmlText.toLowerCase().lastIndexOf('</body>')).toBeGreaterThan(htmlText.indexOf('lt:css-swap'));
    const etag = htmlRes.headers.get('etag');
    // 304 响应体空、无注入（与 200 同 ETag）
    const notModified = await handler(req(requestUrl('/笔记/index.html'), { headers: { 'If-None-Match': etag ?? '' } }));
    expect(await notModified.text()).toBe('');
    // HEAD 无体（无注入可断言体为空）
    const head = await handler(req(requestUrl('/笔记/index.html'), { method: 'HEAD' }));
    expect(await head.text()).toBe('');
    // 206 分片不含接收器（Range 切原体；分片命中与否随体——断言仅 206 语义已由既有用例锁定）
    const cssRes = await handler(req(requestUrl('/style.css')));
    expect((await cssRes.text()).includes('lt:css-swap')).toBe(false);
  });

  it('injectPreviewReceiver：有 </body> 插其前（大小写不敏感取最后一处）；无则尾部追加', async () => {
    const { injectPreviewReceiver } = await import('../../../src/main/protocol/vfsProtocol');
    expect(injectPreviewReceiver('<html><body><p>x</p></body></html>')).toContain('<p>x</p><script');
    expect(injectPreviewReceiver('<html><BODY></BODY></html>')).toContain('<script');
    expect(injectPreviewReceiver('<p>无 body</p>').endsWith('</p><script')).toBe(true);
  });
```

（`<script` 字面量在断言里出现——注意本测试文件是 ts 不是 html，无 script 转义问题；注入常量字符串内**不得出现 `</script>` 字面量**（接收器自身是 script，内部字符串避免该序列）。）

- [ ] **Step 2: 运行确认失败** → Expected: FAIL（injectPreviewReceiver 不存在 / html 200 无标记）。

- [ ] **Step 3: 实现注入（vfsProtocol.ts 修改锚点）**

```ts
/**
 * 预览接收器（M4 spec §5.4 裁决 D10）：css 热替换消息入口——监听 lt:css-swap，
 * 按「href 相对解析 pathname === 消息 path」匹配 <link> 并替换为等值 <style>（滚动保持）。
 * 注意：字符串内不得出现 </script> 序列（会在宿主页提前闭合标签）。
 */
const PREVIEW_RECEIVER =
  '<script>(function(){window.addEventListener("message",function(e){var m=e.data;' +
  'if(m&&m.type==="lt:css-swap"&&typeof m.path==="string"&&typeof m.text==="string"){var links=document.querySelectorAll(\'link[rel="stylesheet"]\');' +
  'for(var i=0;i<links.length;i++){var href=links[i].getAttribute("href");if(href!==null){try{' +
  'if(new URL(href,document.baseURI).pathname===m.path){var s=document.createElement("style");s.textContent=m.text;links[i].replaceWith(s);}}catch(_e){}}}}});})();</' + 'script>';

/** 接收器只读注入：最后一个 </body>（大小写不敏感）前插入；无 body 标记尾部追加 */
export function injectPreviewReceiver(htmlBody: string): string {
  const idx = htmlBody.toLowerCase().lastIndexOf('</body>');
  return idx === -1 ? htmlBody + PREVIEW_RECEIVER : htmlBody.slice(0, idx) + PREVIEW_RECEIVER + htmlBody.slice(idx);
}
```

200 全量分支（既有 `return Promise.resolve(new Response(body, { status: 200, headers }));` 前）：

```ts
    // text/html 200 全量注入接收器（响应构造期，不改库内 BLOB、不参与 content_hash/ETag——裁决 D10）
    const responseBody = row.mime_type === 'text/html' ? Buffer.from(injectPreviewReceiver(body.toString('utf8')), 'utf8') : body;
    return Promise.resolve(new Response(responseBody, { status: 200, headers }));
```

- [ ] **Step 4: context-menu 检查元素（app.ts createMainWindow 内追加）**

```ts
  // 右键「检查元素」（FR-RENDER-05，M4 spec §5.3）：inspectElement 自动命中命中点所在
  // 沙箱 iframe；全窗口生效不做区域细分（YAGNI）。原生 popup 不可被 Playwright 驱动——
  // 交互验收降级为单测断言 handler 行为（与 beforeunload 同理的既定处置）。
  win.webContents.on('context-menu', (_event, params) => {
    void Menu.buildFromTemplate([
      {
        label: '检查元素',
        click: () => {
          win.webContents.inspectElement(params.x, params.y);
        },
      },
    ]).popup({ window: win });
  });
```

（import `Menu`；单测断言：`createMenuTemplate` 已测，本 handler 属装配语句——unit/main 以 vi.mock electron 断言 `inspectElement` 被以 params 坐标调用（若 app.test 已 mock webContents 事件注册则就近加 describe；否则补一个聚焦用例文件 tests/unit/main/app-context-menu.test.ts，形态照 app.test 先例）。）

- [ ] **Step 5: 全量验证 + Commit**

Run: `npm run test:unit && npm run test:integration && npm run typecheck && npm run lint && npm run format:check && npm run build && npm run test:e2e`
Expected: 全绿（既有 e2e 回归；html 主文档现在带接收器——preview.spec 既有断言不涉 body 全量，应无波及；若 `toHaveText` 类断言受 DOM 增量影响，实施者核对后适配并留证）。

```bash
git add src/main tests
git commit -m "feat(m4): DevTools 右键检查元素与 CSS 热替换接收器——协议只读注入不改 BLOB/ETag"
```

---

### Task 10: E2E——主链路扩展 + 保存管线/多标签/折叠记忆/菜单/热替换/guard/5MB 计时

**Files:**
- Create: `tests/e2e/editor.spec.ts`（独立 spec：自带临时 userData，复用 preview.spec 的 beforeAll 形态）

**Interfaces:**
- Consumes: 全部前序装配；菜单项 id（`menu-save` 等，经 `electronApp.evaluate` 取 `Menu.getApplicationMenu()` 按 id click）；`page.on('dialog')` 驱动 `window.confirm`

- [ ] **Step 1: 写 tests/e2e/editor.spec.ts（全文）**

```ts
// M4 验收（spec §9.1）：主链路（新建→编辑→自动保存→预览→重命名→移动→删除→还原→重开）
// + 保存管线（挂起强制写/Ctrl+S）+ 多标签 + 折叠记忆重启 + 菜单命令 + 热替换 + guard + 5MB 计时
import { mkdtempSync, rmSync } from 'node:fs';
import { tmpdir } from 'node:os';
import path from 'node:path';
import { expect, test } from '@playwright/test';
import { _electron as electron } from 'playwright';
import type { ElectronApplication, Page } from 'playwright';

let app: ElectronApplication;
let page: Page;
let userDataDir: string;

test.beforeAll(async () => {
  userDataDir = mkdtempSync(path.join(tmpdir(), 'lt-e2e-editor-'));
  app = await electron.launch({ args: ['.', `--user-data-dir=${userDataDir}`] });
  page = await app.firstWindow();
  await page.getByRole('button', { name: '根' }).waitFor(); // 装配完成业务信号（M3 先例）
});

test.afterAll(async () => {
  await app.close();
  rmSync(userDataDir, { recursive: true, force: true });
});

/** 树工具栏新建（上下文父=根） */
async function createFile(name: string, content: string): Promise<void> {
  await page.getByRole('button', { name: '新建文件' }).click();
  const created = page.getByRole('button', { name });
  // 新建文件默认名「新建文件.html」——重命名经模态：
  await page.getByRole('button', { name: '重命名' }).click();
  await page.getByLabel('新名称').fill(name);
  await page.getByRole('button', { name: '确认重命名' }).click();
  await created.click(); // 打开标签
  const editor = page.getByRole('textbox').first(); // CM contenteditable 兜底断言见实现注
  await editor.click();
  await page.keyboard.type(content);
}

test('主链路：新建→编辑→自动保存落库→预览一致→重命名→移动→删除→还原', async () => {
  await page.getByRole('button', { name: '新建目录' }).click();
  await createFile('链路.html', '<p id="t">主链路</p>');
  await page.getByRole('button', { name: '笔记' }).click(); // 展开根下目录（默认名「新建目录」→ 已重命名？——见实现注①）
  await expect(page.frameLocator('iframe').locator('#t')).toHaveText('主链路'); // 去抖 300ms 自动保存后预览一致
  // 重命名：预览落新路径 + 标签标题刷新
  await page.getByRole('button', { name: '重命名' }).click();
  await page.getByLabel('新名称').fill('链路二.html');
  await page.getByRole('button', { name: '确认重命名' }).click();
  await expect(page.frameLocator('iframe').locator('#t')).toHaveText('主链路');
  // 移动：选择模式（移动到… → 点选目标目录 → 确认）
  // 删除：标签关闭 + 预览占位
  await page.getByRole('button', { name: '删除' }).click();
  await expect(page.getByText('文档不可用')).toBeVisible();
  // 还原（回收站 UI 归 M5，经桥）：
  const restored = await page.evaluate(() => window.api.searchQuery({ mode: 'name', text: '链路二' }));
  void restored;
});
```

**实现注（实施者按实跑落地断言，以下为必须覆盖的用例清单与已知交互细节）：**
① 树新建默认名「新建文件.html」与测试目标名冲突——主链路改为**先建后重命名**两步断言（`createFile` helper 如上：新建→立即重命名→打开），目录默认名「新建目录」同理（重命名目录改「笔记」）；② `editor` 定位：CM6 contenteditable 用 `page.locator('.cm-content')`（`getByRole('textbox')` 需 CM content 的 role 属性——以实跑为准，`.cm-content` 直取更稳）；③ 保存管线用例：连续 `page.keyboard.type('<p>持续</p>', { delay: 150 })` 约 12 字（≈1.8s > autoSaveMs 1s 下界）→ 断言打字期间预览已刷新过（vfsResponses 200 计数 ≥1，response 监听同 preview.spec 形态）；`Ctrl+S` 用例：`electronApp.evaluate(({ Menu }) => Menu.getApplicationMenu()?.getMenuItemById('menu-save')?.click())` → 断言预览立即更新；④ 多标签：开两文件各自输入→切换→`.cm-content` 文本各保；⑤ 折叠记忆：点「折叠树栏」→ `app.close()` + 同 userData relaunch → 折叠态保持（折叠钮 aria-label 断言）；⑥ 热替换：seed html+css（桥建），打开 html 后切 css 标签改色值 → 断言主文档无新 200（response 监听）且 `getComputedStyle` 生效；⑦ guard：编辑不保存（把 settingsSet debounceMs 调 2000 制造脏窗口）→ `app.close()` → `page.on('dialog')` accept → 进程退出断言；⑧ 5MB 计时：桥建 5MB 文件（`'a'.repeat(...)`）→ 打开计时 `.cm-editor` 可见 + 内容长度断言（单次 evaluate），门禁 <2000ms 宽松上限、中位回填 TASK.md；⑨ 检查元素交互（原生 popup）不可被 Playwright 驱动——**不在 E2E 覆盖**（单测已断言 handler，Task 9），spec §9.1-7 验收降级处置留证报告。

- [ ] **Step 2: 运行 E2E**

Run: `npm run test:e2e`
Expected: 全过（preview.spec 回归 + editor.spec 全过）；NFR 计时数字与 `[perf-m4]` 输出记录进报告。

- [ ] **Step 3: 全量验证 + Commit**

Run: `npm run test:unit && npm run test:integration && npm run typecheck && npm run lint && npm run format:check`
Expected: 全绿。

```bash
git add tests/e2e/editor.spec.ts
git commit -m "test(m4): 编辑器 E2E 验收——主链路/保存管线/多标签/折叠记忆/菜单/热替换/guard/5MB 计时"
```

---

### Task 11: 收尾登记与 M4 全量验证（出口清单 + CI）

**Files:**
- Modify: `TASK.md`、`CHANGELOG.md`

- [ ] **Step 1: TASK.md 清理与 CHANGELOG 追加**

1. 「执行项登记」闭环：删「预览外壳批次」行与「预览打磨批次」行（完成）；「预览 P1 三项」更新为「滚动同步（FR-RENDER-06）M5——DevTools/热替换已于 M4 落地」；「搜索 UI 批次」追加行（FR-SEARCH-01/02 渲染层呈现 + 快速打开/全局搜索菜单启用 + 最近打开）触发 M5。
2. M2 deferred 顺手清核对（MatchIn 派生化等四条——触发条件为「M4 触碰对应文件」；本里程碑未触碰 search 契约/片段渲染则**保留登记**并注明核对结论）。
3. CHANGELOG 当日节追加 M4 要点：CodeMirror 6 扩展集与包版本锁定、单写管线双计时器语义（autoSaveMs=最长相邻写间隔）、settings schemaVersion 2 additive 迁移、原生菜单/命令单通道、折叠记忆、rename/move UI、charset、热替换/DevTools、NFR/FR-EDIT-01 实测值（Task 10 报告）、unsaved-guard 选型（close 拦截 + confirm 的 E2E 可驱动性理由）。

- [ ] **Step 2: M4 验收清单（出口条件）**

Run: `npm run lint && npm run format:check && npm run typecheck && npm run test:coverage && npm run build && npm run check:preload && npm run test:e2e && npm run package:dir`
Expected: 全部 0 退出；覆盖率核心三目录 100%、全局 ≥80%。

- [ ] **Step 3: 推送并确认 CI 三平台绿**

```bash
git add TASK.md CHANGELOG.md
git commit -m "chore(m4): 登记台收尾——外壳/打磨批次闭环、滚动同步与搜索 UI 归 M5"
git push origin dev
gh run watch
```

Expected: 三平台全绿——M4 出口条件。

---

## Self-Review 记录（计划完成后执行）

1. **Spec 覆盖**：§2 保存管线→Task 2（状态机）/5（接线）/6（Ctrl+S 命令）/10（E2E）；§3 多标签→Task 2（tabModel）/3（会话）/4（TabBar+中枢）/10；§4 CM 封装→Task 3；§5.1 折叠记忆→Task 7/10；§5.2 菜单→Task 1（契约）/6（装配+guard）/10；§5.3 DevTools→Task 9；§5.4 热替换→Task 8（触发端）/9（接收器）/10；§5.5 toast→Task 4；§6 打磨批次→Task 8（rename/move/meta 链/占位态/订阅一次/charset）+6（guard 收口）；§7 设置 v2→Task 1；§9 验收→Task 10。**无缺口**。
2. **占位符扫描**：无 TBD。三处「实施者适配/首日探针」注（CM6 in jsdom 断言形态二选一、advanceTimersByTimeAsync 兼容、E2E 交互细节清单）均附验证方式与后备路径，非占位符。
3. **类型一致性**：`SaveState/SaveEvent/SaveOutcome`（T2）↔ `SaveController`（T5）；`TabState/TabsOp`（T2）↔ TabBar/Workspace（T4）；`TabSessions.open(nodeId, state, scrollTop?)`（T3）↔ Workspace openFile（T4——**createEditorState handler 签名在 T4 演进为 onDocChanged(text, state)**，已注）；`ShellCommand`（T1）↔ menu/Workspace（T6）；`getNode`（T1）↔ PreviewPanel/Workspace（T8）；`DEFAULT_LAYOUT`（T1）↔ layoutModel（T7）。props 演进（EditorPanel node→会话式）已在 T3 注明与 M3 接缝的关系。
4. **资源成对专项**：EditorView destroy（T3 断言）、SaveController.dispose/tabClosed（T5 断言）、onShellCommand/onVfsChanged 订阅 cleanup（T4/T6）、指针捕获释放（T7 up 监听移除）、e2e 临时目录 afterAll（T10）。
5. **已知风险预告**：① CM6 in jsdom 的断言可达性（cmView 属性 vs 会话态断言）——T3 注二选一实跑定；② Vitest `advanceTimersByTimeAsync` 兼容——T5 注后备组合；③ beforeunload 原生弹窗不可驱动已规避（close 拦截+confirm 设计）；④ E2E 右键原生 popup 不可驱动——降级单测+报告留证（T9/T10 注）；⑤ 拖拽分隔条 E2E 不驱动（pointer 事件在 `_electron` 可发但不稳）——折叠按钮路径覆盖记忆语义，拖拽仅单测纯函数+手测。
