# M5 批次⑧ Task 15 · taste 深度打磨对照表

> 技能驱动：`taste-skill:high-end-visual-design` + `taste-skill:redesign-existing-projects` 已于开工前经 Skill 工具加载（留痕于 task-15-report.md）。
> 工作流：redesign-existing-projects 三段（Scan → Diagnose → Fix）驱动审计与处置；high-end-visual-design 提供审计维度与禁令清单。
> **裁决边界**：技能产出规范（禁系统字体 / 禁 Lucide / 大留白 / rounded-[2rem] / spring 动效等）与本项目设计系统基线（系统字体栈、冷静紧凑、4px 圆角、克制动效，均经 §1 双候选论证定档）冲突时，按宪法 §2 裁决顺序以更特化的**项目基线为准**；技能只用于驱动审计维度（对比度 / 焦点态 / 空态 / 微交互 / 动效 / 措辞 / 对齐节奏 / 阴影 / 灰上灰），不引入其产出形态。零新依赖（motion 未在依赖清单，未引入；动效全部 CSS transition / tw-animate-css）。
> 红线自检：行为与 aria 语义锚点零变更（既有测试全绿零改动）；动画仅 opacity/颜色路径（无 layout 属性动画）；`prefers-reduced-motion` 由 theme.css 全局降级兜底覆盖全部新增动效。

## 一、generic AI 味清单 → 逐项处置

| # | 面 | 审计发现（域） | 处置 | 状态 |
| --- | --- | --- | --- | --- |
| 1 | 对比度 | dirty/失败破坏色文本最坏表面 = 悬停 accent 面：light `#DC2626`/`#E2E8F0` = 3.92:1、dark `#EF4444`/`#1E293B` = 3.89:1，均 < 4.5（前序台账项；基线 §3.1 只验了 background 面，属验算缺口） | token 双侧各调一档：light `#B91C1C`（accent 面 5.25:1）/ dark `#F87171`（5.29:1），全组合实算回写设计系统 §3 表 + §3.1 #9/#10/#16/#17 重算、#18/#19 增列 | ✅ 处置（基线级，§九-1） |
| 2 | 焦点态 | 自研钮（工具钮/树行/标签/⋯/关闭钮）键盘焦点仅 UA auto 轮廓 + 50% 透明 ring 色，弱且与输入框 2px ring 不一致 | theme.css base 层 `button:focus-visible { outline: 2px solid var(--ring) }` 全站统一；shadcn 组件 utilities 层级联优先不受影响 | ✅ 处置（基线级，§九-2） |
| 3 | 空态 | 面板级空态（编辑器/预览「未选中文件」「文档不可用」）为裸文字，未完成感 | 升级「装饰性图标（aria-hidden、1.5 描边、60% 不透明度）+ 原文案」组合空态；svg 不产生 textContent，`toHaveText`/`textContent` 锚点零变更 | ✅ 处置（§九-5） |
| 4 | 对齐/体外值 | shadcn `AlertDialogTitle` text-lg（18px）与 `AlertDialogContent` p-6（24px）偏离字号三档 / 浮层 16px 内边距标尺（导入确认、还原确认两处） | 消费侧 className 覆写：`AlertDialogContent className="p-4"`、`AlertDialogTitle className="text-base"`（cn/tailwind-merge 合法场景，不改组件源码） | ✅ 处置（§九-3） |
| 5 | 动效缺失 | 设置全屏覆盖层入场瞬时生硬（全站唯一无入场动效的浮层面） | `animate-in fade-in duration-240`（仅 opacity 合成器路径；reduced-motion 全局降级覆盖；出场随卸载瞬时） | ✅ 处置（§九-8） |
| 6 | 视觉噪音 | 树每行常驻 20px「⋯」钮，恒定 chrome 噪音 | 常态 `opacity-0`，行悬停 / 行内焦点 / radix 菜单展开三态显形（禁只挂 hover 的键盘不可达形态）；透明态仍占位可命中，零行为变化 | ✅ 处置（§九-6） |
| 7 | 微交互/命中面 | 标签关闭钮 16px 命中面低于桌面最小目标惯例 | `relative after:-inset-1` 伪元素外扩至 24px 有效命中（无视觉呈现、恰好吃满 4px 缝隙不侵入邻区） | ✅ 处置（§九-7） |
| 8 | 数字排版 | 进度计数 / 命中计数 / 时间戳 / 滑块值 / 备份大小用比例数字，刷新时宽度抖动 | 上列数据数字统一 `tabular-nums`（导入/导出进度面板、搜索状态条、回收站时间行、设置滑块值与备份列表、toast） | ✅ 处置（§九-4） |
| 9 | 台账项（回收站后代行死项） | 后代行「还原必失败」无呈现层指向（TASK.md 登记项） | 纯呈现处置：父链命中另一回收站条目的行加「随上级还原」弱化标注（还原钮可点性不变，行为零变更）；配套单测 1 例（后代行标注且唯一、顶层行无） | ✅ 处置（TASK.md 项勾销） |
| 10 | 动效生硬（折叠几何） | 三栏折叠/展开瞬时跳变（§6.4 台账项） | **不做**——grid-template-columns 动画走 layout 路径，违反 D24「仅 transform/opacity 合成器路径」红线 | ⛔ 红线不做（留台账，§九-8） |
| 11 | 动效缺失（toast 退场） | toast 3s 摘除瞬时消失 | **不做**——需动 D11 toast 队列逻辑（行为级） | ⛔ 行为级，超打磨边界（§6.4 台账原样） |
| 12 | 保真红线 | 图片预览透明像素无棋盘格/着色底 | **不做**——FR-RENDER-01 保真预览，UI 不给用户文档内容着色 | ⛔ 记录不做 |
| 13 | 原生弹窗 | window.confirm（大文件/彻底删除/清空/退出确认）原生样式与应用脱节 | **不做**——换应用内对话框属行为面新增（且 M4 裁决 confirm 为 Playwright 可驱动的既定选型） | ⛔ 行为级，超打磨边界 |
| 14 | 措辞 | toast 文案内嵌 FR 编号（「二进制文件暂不支持编辑（FR-EDIT-04 归后续批次）」）等内部措辞泄漏 | **不做**——toast 措辞变更与前序台账「取消 toast 措辞」同口径判行为级 | ⛔ 行为级，超打磨边界（留台账） |
| 15 | aria 微窗口 | reveal 与 previewOnly 收敛同一行时「（预览中）」后缀被抑制（前序台账项） | **不处置**——后缀生成逻辑在 aria-label 语义面，触碰即越「aria 锚点零变更」红线；该窗口行仍以强选中高亮呈现，无信息丢失 | ⛔ aria 语义面，记录（留台账） |
| 16 | 行为级台账 | 注入下行 scrollTo 清零预览滚动位 / openPath 失败零反馈 / beginExport 无互斥 / 失败日志上限 / rename 已预览媒体陈旧帧 / T6/T8 测试鉴别力 | **不处置**——均行为级或测试补强，非呈现层；前序台账原样保留 | ⛔ 行为级，超打磨边界（留台账） |

## 二、审计通过面（无缺陷，不需处置）

| 面 | 结论 |
| --- | --- |
| 阴影滥用 | 零滥用：投影仅存于浮层（toast / 重命名 / 进度面板 shadow-md、radix 浮层 shadow-lg），面板与内容区零阴影，符合 §五「拒绝装饰性投影」 |
| 灰上灰 | 标题栏/编辑条/状态栏 `bg-muted/50` + muted-foreground 文字组合经 §3.1 #3 验算达标；无失控灰叠灰 |
| hover/焦点过渡 | 全站自研钮统一 `transition-colors duration-100`（§6.1 instant 档），无瞬时跳变遗漏（除上述 #5 设置页入场，已补） |
| 色彩纪律 | 零 raw hex 散落（组件全走语义 token）；slate 单一灰轴；单一破坏色；无 AI 紫蓝渐变指纹 |
| 字号/间距节奏 | 组件面全部落在 12/14px 两档与 4 整除间距（p-2/p-3/gap-1/gap-2）；本次新增 gap-2/24px 等均在尺上 |
| 措辞（除 #14 外） | toast/空态/按钮文案直白具体（「导入完成：新增 X、跳过 Y、失败 Z」），无感叹号、无「无缝/极致」类 AI 腔 |
| 图标语言 | « » × ⋯ 文字字形 + 本次 lucide 装饰图标（已有依赖，dialog/command 先例）描边 1.5 统一，无混用 |
| z-index 纪律 | 仅体系层值（z-40 设置覆盖 < z-50 浮层/toast），无 9999 类任意值 |
| reduced-motion | theme.css 基线层全局降级覆盖全部动效（含本次新增 fade-in），新增动画零绕过（无 inline 时长注入） |

## 三、验证与体积留证

- 验证链（brief Step 3 全量）：`test:unit` 464 ✓ / `test:integration` 132 ✓ / `typecheck` ✓ / `lint` ✓ / `format:check` ✓ / `build` ✓ / `test:e2e` 18 ✓ / `check:preload` ✓ —— 既有测试**全绿零改动**（唯一测试变更为新增回收站标注用例 1 例，属「新增功能同步交付测试」规范要求）；E2E 锚点零失配，无需对照表。
- 主 chunk：基线 1,030,983 B → 打磨后 **1,032,656 B（+1,673 B）**——增量来自两个 lucide 装饰图标（FileText/FileX，svg path 数据）与回收站后代判定逻辑；零新依赖（motion 未在依赖清单，确认后未引入，动效全走 CSS）。
- 文档回写：`docs/design/设计系统.md` §3 表、§3.1 对比度表（重算 + 增列）、新增 §九「打磨增量」；CHANGELOG 2026-09-21 先记后改。
