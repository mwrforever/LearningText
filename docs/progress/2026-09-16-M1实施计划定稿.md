# 任务交接文档 (Session Handoff)

> ⚠ **交接声明（务必先读）**：本文档是新会话续接任务的**唯一事实来源**，内容仅来自当前会话的事实，自包含、不引用其他进度文档或历史会话记录。
> 「第 2 节 未完成」中的全部任务必须在【新会话】执行；生成本文档的会话已交接封存，不再实现任何任务。
> 新会话续接前必须完整读取本文档、以本文档为准，禁止凭空推测进度或重复已完成的工作。

## 元信息 (Meta)
- workspace_dir: D:\code\project\LearningText
- saved_at: 2026-09-16
- 版本控制提交号: dev 分支 d4ee98b + 交接提交（已推送 origin）
- 交接状态: 已交接（本会话封存，未完成部分须在新会话执行）
- 任务一句话描述: LearningText M0 全部完成并验收；M1 实施计划（SDD 模式）与存储/VFS spec 已定稿于 dev 分支，**新会话直接按计划开工 M1**

## 1. 已完成 (Completed) — 只列结论，让新会话知道哪些不用再做
- [x] **M0 全部 15 任务完成并通过验收**（SDD 执行，dev 4a03874）：脚手架、TS 6.0.3 三配置、ESLint/Prettier（文档豁免策略）、shared 契约（Result DTO/IPC 常量/WindowApi）、Vite+React 渲染层、app:// 协议主进程、system:ping 类型化 IPC、better-sqlite3 WAL 原语、覆盖率门禁（核心 100%/全局 80%）、_electron E2E、husky+lint-staged、electron-builder、CI 三平台矩阵、dev/main 分支保护；最终全分支评审修复（electron-builder files 补 dist/shared、e2e globalSetup 构建前置）
- [x] M0 验收清单七命令全 0 退出；CI run 35012167678 三平台绿
- [x] **存储与 VFS spec 定稿**：`docs/superpowers/specs/2026-09-16-存储与VFS-design.md`（用户评审确认；五项决策范围全部落定，§11 八条裁决 + §1.1 M1 领域外前置项交接表）；TASK.md 登记行已删除
- [x] **M1 实施计划定稿并标记 SDD 执行**：`docs/superpowers/plans/2026-09-16-M1-存储与VFS.md`（13 任务、TDD 步骤粒度、含完整代码块与验证命令、无占位符；writing-plans 自查三项通过 + 一轮实质修复：坏示例代码/语句泛型形态/affectedCount 双算/测试违例断言）
- [x] zod v4 习语待调研项闭环（spec §8.2：基础 API 无破坏、不定制逐字段消息故 v4 变化零影响、禁用废弃形态）
- [x] configLoader 'native' 裁决已写入计划 Task 1 Step 6（暂不切 "type":"module"，待 Vite native 成为默认再评估）

## 2. 进行中 / 未完成 (Pending) — 重点详述（全部须在新会话执行）

### 2.1 执行 M1 实施计划（SDD 模式，用户已确认可直接开工）
- 目标: 按 `docs/superpowers/plans/2026-09-16-M1-存储与VFS.md` 完成 13 个任务；M1 验收清单全绿（Task 13 Step 3：lint / format:check / typecheck / test:coverage / build / check:preload / test:e2e / package:dir 全 0 退出 + CI 三平台绿）
- 当前状态: 计划已定稿（dev d4ee98b），实现代码为零
- 任务清单: Task 1 preload 构建拆出（含 configLoader 裁决落地）→ Task 2 B.5-4/5 窗口安全 → Task 3 shared 契约扩展 → Task 4 迁移体系 → Task 5 事务/错误映射 → Task 6 名称校验/MIME 纯函数 → Task 7-10 VFS 服务（查询/写路径/重构级联/软删除域）→ Task 11 IPC 十通道接线与 vfs:changed 广播 → Task 12 性能基准 + 宪法 A.5-4 毫秒预算回填（修宪先记再改）→ Task 13 B.1/C.4 对齐回填 + M1 验收
- 已锁定决策（新会话不得推翻）:
  - spec `2026-09-16-存储与VFS-design.md` 全部条款（含 §11 八条裁决：迁移 TS 模块注册表、SQLITE_BUSY 不自动重试、名称校验三平台最严并集 + NFC、回收站不自动清理、4 个新增错误码、FTS body 提取规则、备份标记文件化、M1 含 FTS 写侧同步）
  - 计划 Global Constraints 全部条款；configLoader 暂不迁移裁决
  - 覆盖率阈值（核心 100%/全局 80%）禁止下调
- 阻塞点/风险:
  - Task 7-10 存在服务层过渡占位桩的跨任务约定（Task 7 建骨架时占位方法抛 E_STORE_INTERNAL，Task 8/9/10 逐个替换；Task 10 结束后必须无桩）——计划已注明，评审时检查
  - Task 12 的毫秒预算数值依赖实测（计划已给死推导公式：max(写入,查询)×10 向上取整至百毫秒档，下限 200ms）
  - electron 二进制下载慢用 `ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/`
- **下一步具体动作**: 在新会话（工作目录 D:\code\project\LearningText，分支 dev）加载 superpowers:subagent-driven-development 技能，读取 `docs/superpowers/plans/2026-09-16-M1-存储与VFS.md`，从 Task 1 开始逐任务派发子代理执行（每任务完成后两阶段评审，全绿才进入下一任务）
- 验证方式: 计划 Task 13 Step 3 验收清单全部 0 退出；`gh run watch` 三平台作业全绿

### 2.2 M1 完成后（后续里程碑预备，本会话不执行）
- M2 开工前按 TASK.md「待撰写 spec」表撰写 docs/05 搜索详细设计（落 `docs/superpowers/specs/YYYY-MM-DD-搜索-design.md`，superpowers 目录约定）
- 里程碑链：M1 存储+VFS → M2 搜索 → M3 预览 → M4 编辑器 → M5 导入导出+辅助 → M6 打包发布（docs/03 §6.2）

## 3. 环境与依赖状态 (Environment)
- 运行时/工具版本: Windows 11 + Git Bash；Node 24.19.0（.nvmrc=24）；gh CLI 已登录 mwrforever（仓库所有者，分支保护 API 已用 --input JSON body 方式验证可用）
- 关键配置: 远程 `https://github.com/mwrforever/LearningText.git`；dev/main 均有分支保护（必需检查 `build (ubuntu/windows/macos-latest)` + strict；dev enforce_admins=false 管理员可直推——推送时 remote 提示 bypass 属预期）
- 本机特有事项: npm allow-scripts 拦 install 脚本——electron-winstaller 遗留 M6 前 `npm approve-scripts`；electron 二进制已就位（node_modules/electron）
- 已知工程事实（M0 评审确立）: electron-builder files 必须含 dist/shared；sandbox preload 必须 rolldown 单文件捆绑（`check:preload` 守卫脚本已就位）；better-sqlite3 无自带类型（@types/better-sqlite3@9.6.0 已锁）

## 4. 全局约定 (Locked Conventions)
- **分支模型**：一切开发在 dev；main 是生产分支，项目完整落地后一次性合入
- 写代码前必读 `AGENTS.md`（宪法）；修宪**先记 CHANGELOG.md 再改正文**；实现类工作登记 TASK.md，不入宪法
- `docs/03` 需求基线：spec 只补细节，冲突先改 docs/03
- **文档位置约定（用户 2026-09-16 指定）**：spec（设计文档）落 `docs/superpowers/specs/YYYY-MM-DD-<主题>-design.md`；实施计划落 `docs/superpowers/plans/`；TASK.md「待撰写 spec」表编号仅为路线图序号
- 会话工作流：显式点名技能先加载；关键方案门用 AskUserQuestion；设计批准 → 实现计划（writing-plans）→ 执行（subagent-driven-development）不跳步
- 项目纪律：测试与实现同提交（核心 100% / 全局 ≥80%）；CI 失败禁止合入、禁止人工绕过；每任务 Conventional Commits 中文消息
