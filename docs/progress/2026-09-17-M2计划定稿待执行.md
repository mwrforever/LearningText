# 任务交接文档 (Session Handoff)

> ⚠ **交接声明（务必先读）**：本文档由 `session-handoff` 技能生成，是新会话续接任务的**唯一事实来源**，内容仅来自当前会话的事实，自包含、不引用其他进度文档或历史会话记录。
> 「未完成」章节中的全部任务必须在【新会话】执行；生成本文档的会话已交接封存，不再实现任何任务。
> 新会话续接前必须完整读取本文档、以本文档为准，禁止凭空推测进度或重复已完成的工作。
> 代码与文件变更明细不在本文档记录，新会话用 `git diff` / `git log` 自行查看。

## 元信息 (Meta)
- workspace_dir: D:\code\project\LearningText
- saved_at: 2026-09-23T00:58:00+08:00（update 刷新）
- 生成方式: update
- 版本控制提交号: HEAD=4e4968f（dev 本地与 origin/dev 同步）；**工作区含 M7 批次 30 处改动未提交**（新会话第一动作见 3.2）
- 交接状态: 已交接（本会话封存；M7 主体已实现但 E2E 未收敛，用户五项新反馈与打包发布全部移交新会话）
- 任务一句话描述: M7「树体验与单文件导入」主体已实现（六需求 + WCO 黑块修复，unit/integration 全绿、E2E 7 failed 未收敛、工作区未提交）——新会话续接：E2E 收敛 → 提交 → 用户五项新反馈修复（fix+review loop）→ 多平台打包发布（gh 监控）。

## 1. 已完成 (Completed) — 只做精简标记
- [x] **M0–M6 均已完成验收**（历史事实，证据归档于 docs/progress/ 各报告；本会话未触碰其实现）。
- [x] **M7 谋建琢主体**：树目录行 chevron 展开指示（rotate-90 transform 过渡）+ 展开态 FolderOpen + 类型图标着色（dir 琥珀/html 橙/image 翠绿/audio 紫/文本天蓝，双主题）；新建目录改行内原地命名（`lt-create-row`，Enter 确认/Esc 取消，失焦不取消——裁决留证设计系统 §十-4）；单文件导入链路（`io:pick-file` 通道 + `importService` 文件源 `isDirectory` 判别 + `sourceName` 导入即重命名 + `ImportResult.importedNodeIds` 导入后自动打开 + 登记簿更名 `dialogProducedPaths`）；「导入 HTML 文件」四入口（树工具栏/菜单 Ctrl+N/欢迎页）→ 非模态确认浮层（名称可改 + 树中点选目录改目标 + 固定 rename 策略）；目录点选模式泛化（`dirPickMode`/`data-pick-target`，move 与导入共用）。
- [x] **用户实测缺陷修复①（右上角系统按钮区整条发黑，亮色主题 + start.bat）**：`BrowserWindow` 补 `backgroundColor`（与 `titleBarOverlayFor` color 同源取色，按启动解析主题）；**用户已确认修复，不用再管**。
- [x] **测试与文档**：unit 511 全绿 / integration 7 通过（含 fixture 文件源用例）/ typecheck+lint 零告警；docs/03（FR-SHELL-02、FR-IO-01 文件形态、§7.1 `io:pick-file`）、设计系统 §十、CHANGELOG 2026-09-23 条目同步；新增 fixture `tests/fixtures/code.html`（用户实测示例文档 76KB）。
- [x] **E2E 锚点改造已做**：editor.spec 主链路与 guard 用例「新建文件」改桥建 + 树点选开签；`menu-new-file`→`menu-import-html`（触发面归单测锁定）；preview.spec 新增 M7 渲染验证用例（code.html 画布渲染断言 + 截图留证）。

## 2. 未完成 (Pending) — 精简且可执行（全部须在新会话执行）

### 2.1 M7 E2E 收敛（当前 7 failed + 1 flaky，两处根因已定位）
- 目标与当前状态: `npm test` 全绿收官。全量实跑结果：7 failed + 1 flaky + 19 passed（4.4m），失败集中在 editor 主链路/guard 与 preview 的 M7 用例及其连锁。
- 已锁定决策/阻塞风险: **两处已定位根因不得回退改造**——① preview.spec M7 用例 fixture 路径错位：`path.join(__dirname, '../../fixtures/code.html')` 应为 `'../fixtures/code.html'`（ENOENT 实证）；② 该用例位于 describe 中部，其打开的 code.html iframe 使后续 5 个用例 `page.frameLocator('iframe')` 无差别定位 strict 违例连锁失败——**整体挪至 describe 末尾**。
- **下一步具体动作**: 先修上述两处；再本地单跑 `npx playwright test tests/e2e/editor.spec.ts -g "主链路"` 与 `-g "unsaved-guard"` 看 error-context 排查 `openInTree('新建文件.html')` 超时（疑行内命名 Enter→created 广播挂树链路或 `seedFile` 空内容形态）；flaky guard 重试轮已通过只需观察。
- 验证方式: `npx playwright test` 全绿；`npm test` 全绿。**注意：`npm test 2>&1 | tail` 等管道会吞退出码（本会话实证 7 failed 被掩），门禁判定必须看完整输出。**

### 2.2 工作区提交（30 处 M7 改动未提交）
- 目标与当前状态: `git status` 30 处（M7 源码/测试/文档/fixture + 新目录 `src/renderer/src/features/io/`、`tests/fixtures/`、`.impeccable/`）。
- 已锁定决策/阻塞风险: 必须在 2.1 收敛后提交（当前 E2E 红不可入主干）；`.impeccable/` 为设计 hook 配置，随提交入库或入 gitignore 由新会话按内容判断。
- **下一步具体动作**: 按 Conventional Commits 中文分批提交（`feat(m7)` 主体 / `fix(m7)` E2E 收敛 / `docs(m7)` 文档同步），测试与实现同提交；dev 直接推（bypass 提示属预期）。
- 验证方式: `git log` 提交完整、`npm test` 绿后 `git push`。

### 2.3 用户五项新反馈修复（fix + review loop；派遣 subagent；无需产出 spec/plan；中间无需用户参与决策）
- 目标与当前状态: 用户 2026-09-23 亲测截图反馈五项（原话要点如下），全部未开始修复。
- 已锁定决策/阻塞风险: 执行方式为用户点名——**每项 fix 后必须 review loop 确认解决**；派遣 subagent 承担修复与审查；参考 superpowers 流程但不产出 spec/plan 文档；过程中不做用户决策门。
- **下一步具体动作**:
  1. **导入进度提示导入成功仍显示「正在导入」不消失**：根因已定位——`confirmImportHtml` 收口缺 `setImportProgress(null)`（`confirmImport` 有，见 Workspace.tsx 搜索 `setImportProgress`）；需求：成功/失败后自动消失 + 滑入滑出过渡动画（导入/导出两个进度面板同治：退场 `animate-out fade-out slide-out-to-bottom-2 duration-240` 播完再卸载；新进度广播到达需重置退场态防快速连开竞态）。
  2. **隐藏树「根」目录**：用户数据目录即默认根，不在树中展示「根」节点；像 VS Code 那样在操作栏/侧栏头下方以小字体展示保存路径（`storage:get-info` 的 root 可供）。
  3. **原地命名聚焦语义**：仅新建目录/文件时自动聚焦命名行；其余状态经选中目标 →「重命名」入口进入修改（现状基本吻合，验收确认即可）。
  4. **点击文件/目录后再新建应直接在当前选中目录下创建**——现状点击目录仅 `onToggle` 不记账选中上下文（selectedId 只在点文件时设置，contextParent 恒落根），需目录点击 = 选中（VS Code 语义：点击选中 + 展开折叠）。
  5. **目录收起/新建/导入点击偶发无反应**：疑 `onToggle` 闭包捕获旧 `expanded`（`new Set(expanded)` 非函数式更新），快速连点状态回滚——改函数式 `setExpanded(prev => ...)` 并排查同类状态更新。
- 验证方式: 每项 fix 后 review loop（subagent 审查）确认解决；`npm test` 全绿；逐项对照上述用户场景复核。

### 2.4 多平台打包发布流程（subagent 模式；用 gh 监控直至成功）
- 目标与当前状态: 让产品可发布——M6 数据侧与代码侧已就绪；fuses 未落地（TASK.md 待调研项，宪法 B.5-8）、release 工作流未创建、安装包未产出。
- 已锁定决策/阻塞风险: fuses 必须先调研清单再落地（宪法 B.5-8）；macOS 签名/公证无证书先发未签名包；electron-builder v27 将更名 `electronDownload`→`electronGet`（C.6 升级预警）。
- **下一步具体动作**: fuses 调研并形成清单 → electron-builder afterPack 接 `@electron/fuses` → 建 release 工作流（`.github/workflows/` tag 触发三平台 matrix + draft 人工闸门 + 签名 secrets 占位）→ 本机 `npm run package` 打 Windows NSIS 冒烟 → push + test tag 触发 CI → `gh run watch`/`gh run list` 监控三平台绿且 draft release 含三平台安装包。
- 验证方式: CI 三平台绿；draft release 产出 Windows NSIS / Linux 包 / macOS dmg；本机安装包可安装并跑通主链路。

### 2.5 用户亲测验收（随 2.3/2.4 完成后进行）
- 目标与当前状态: 用户以 `scripts\start.bat` 启动逐项验证；差异转 TASK.md 缺陷条目。
- 已锁定决策/阻塞风险: E2E 锚点契约（`lt-*` 类、aria-label、中文文案是测试锚点）改动必须同步测试。
- **下一步具体动作**: 交付 2.4 产物后引导用户按五项反馈 + 主链路复核。
- 验证方式: 用户逐项确认或差异登记。

### 2.6 存量打磨与接线（minor 项批次，低优先级）
- 目标与当前状态: TASK.md「执行项登记」deferred 条目（约 25 条）沿用原登记。
- **下一步具体动作**: 逐条读 TASK.md 对应行按「触发时机」排序实施；每项独立提交、测试与实现同提交。
- 验证方式: 每项按其描述红线/用例验证；`npm test` 保持绿。

## 3. 环境与全局约定 (Environment & Conventions)
- 运行时/工具版本: Windows 11 + Git Bash；Node 24.19.0（.nvmrc=24）；Tailwind 4.3.x / shadcn CLI 4.21.0（**CLI 不可用**——组件手工落 registry 模板）/ cmdk 1.1.1 / radix-ui 1.6.7 / lucide-react 1.47.0 / one-dark 6.1.3。
- 关键配置: 远程 `https://github.com/mwrforever/LearningText.git`；dev/main 均分支保护（dev enforce_admins=false 管理员直推 bypass 提示属预期）；SSH 推送偶发 Connection reset 等 30s 重试；Electron 下载慢用 `ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/`；CI 安装依赖设 `HUSKY=0`。
- 构建/启动命令: 从零复现 `npm ci && npm run rebuild`；开发 `npm run dev` + 另窗 `npm run start:dev`；生产验证 `npm run build && npm start` 或 `scripts/start.bat`（Windows）/`bash scripts/start.sh`；全量测试 `npm test`（unit+integration+e2e 串行，E2E globalSetup 自动全量构建）。
- 全局约定: 写代码前必读 AGENTS.md 宪法（含 C.7 谋建琢三段律；UI 工作 subagent 必须加载 @ui-ux-pro-max 与 @taste-skill）；修宪先记 CHANGELOG 再改正文；需求变更先改 docs/03；spec 落 `docs/superpowers/specs/`、计划落 `docs/superpowers/plans/`、设计落 `docs/design/`、进度证据落 `docs/progress/`；每任务 Conventional Commits 中文、测试与实现同提交；**关键方案门 AskUserQuestion 未即时应答时按推荐自主裁决并完整留痕**；**E2E 锚点契约**：`lt-*` 类/aria-label/中文文案是测试锚点，改 UI 必须同步测试；**zh-CN Windows 下 .bat 消息必须 ASCII**；依赖平台语义的结论必须探针实证（本机全绿不可替代 CI 三平台矩阵）。
- 本会话实证教训: ① `npm test 2>&1 | tail` 等管道命令吞测试退出码，门禁判定必须看完整输出；② Playwright spec 内 `__dirname` 相对测试文件真实目录（tests/e2e 取 fixture 用 `../fixtures`）；③ jsdom 焦点时序有伪影（挂载期焦点重建触发伪影 blur），`CreateDirRow`「失焦不取消」裁决背景见设计系统 §十-4，改动该裁决需重验组件测试。
