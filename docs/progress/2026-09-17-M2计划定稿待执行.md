# 任务交接文档 (Session Handoff)

> ⚠ **交接声明（务必先读）**：本文档由 `session-handoff` 技能生成，是新会话续接任务的**唯一事实来源**，内容仅来自当前会话的事实，自包含、不引用其他进度文档或历史会话记录。
> 「未完成」章节中的全部任务必须在【新会话】执行；生成本文档的会话已交接封存，不再实现任何任务。
> 新会话续接前必须完整读取本文档、以本文档为准，禁止凭空推测进度或重复已完成的工作。
> 代码与文件变更明细不在本文档记录，新会话用 `git diff` / `git log` 自行查看。

## 元信息 (Meta)
- workspace_dir: D:\code\project\LearningText
- saved_at: 2026-09-19T06:47:31+08:00（update 刷新）
- 生成方式: update
- 版本控制提交号: dev 本地 598f613（**含 3 个未推送提交**：91b2774 spec、bbe270f spec 修订、598f613 plan；M3 全部实现提交已推送，origin/dev=8b1951f）
- 交接状态: 已交接（本会话封存，未完成部分须在新会话执行）
- 任务一句话描述: **M3 预览里程碑已全量完成并验收**（SDD 9 任务 + 终审修复波，CI 三平台绿、已推送）；M4 编辑器的 spec（v0.1）与 11 任务实施计划已定稿于本地 dev（未推送），**待用户评审放行后开工执行**

## 1. 已完成 (Completed) — 只做精简标记
- [x] **M3 预览全量完成并验收**（SDD 9 任务 + 三次执行期产品裁决 + 终审 With fixes 处置，18 提交 ceed233..8b1951f 已推送，CI run 35394349240/35399015504 三平台绿）：出口清单 8 命令零退出、覆盖率核心 100%/全局 92.76%、E2E 9/9、NFR-04 中位 350ms。
- [x] **三次探针实证产品裁决**（均已勘误留痕）：① URL 解析器归一编码点段（spec 双机制→单机制）② Blink 空 authority「首段提 host」→ **固定 host `vfs://local/<路径>`**（shared 常量 VFS_URL_HOST）③ vfs 特权补 `corsEnabled` + 主文档 CSP `connect-src vfs:`。探针证据已归档 `docs/progress/2026-09-18-M3-*.md` 两篇（提交 8b1951f）。
- [x] **M4 spec v0.1 定稿**（91b2774/bbe270f）：`docs/superpowers/specs/2026-09-18-编辑器与保存管线-design.md`，11 项拟定裁决 D1–D11（单写管线双计时器、settings schemaVersion 2、guard 用 close 拦截+confirm、CM6 curated 扩展集、大文件 5MB/50MB 阈值、rename/move UI 入 M4、DevTools+热替换入 M4、滚动同步/搜索 UI 归 M5 等）。
- [x] **M4 实施计划定稿**（598f613）：`docs/superpowers/plans/2026-09-18-M4-编辑器.md`，11 任务 TDD 粒度、含逐字代码与验证命令；自查五条风险注随文（CM6-in-jsdom 断言形态二选一、Vitest advanceTimersByTimeAsync 后备、E2E 原生 popup 不可驱动降级、beforeunload 已规避、拖拽分隔条不进 E2E）。**实现代码为零。**

## 2. 未完成 (Pending) — 精简且可执行（全部须在新会话执行）

### 2.1 执行 M4 编辑器实施计划（**前置闸：用户评审 spec+计划并放行**——用户指示「计划完成后需要停下来」，本会话已停在评审门）
- 目标与当前状态: 按 `docs/superpowers/plans/2026-09-18-M4-编辑器.md` 完成 11 个任务（契约基座→保存/标签状态机→CodeMirror 内核→多标签→SaveController→菜单 guard→折叠记忆→打磨批次→热替换→E2E→收尾），达成 M4 出口（Task 11 验收清单全绿 + CI 三平台）。spec/计划已定稿于本地 dev，实现零。
- 已锁定决策（新会话不得推翻，用户评审明确推翻者除外）: spec D1–D11 全部条款（尤其 D2 单写管线双计时器语义、D3 close 拦截+confirm 选型、D7 大文件阈值、D9 charset）；计划 Global Constraints 全部条款（广播 `{rev, event}` 契约、固定 host `vfs://local`、覆盖率核心 100%/全局 ≥80% 禁止下调、渲染期禁写 ref、CM6 不可变习语）；计划自查五条风险注按注落地不得自创路径。宪法 AGENTS.md 强制生效。
- 阻塞点/风险: ① 本机 npm allow-scripts 拦 install 脚本（CM6 纯 JS 无碍；如遇原生依赖照 M2/M3 先例处理）；② CodeMirror 8 包版本若 registry 前移，以实查为准同步计划版本号并留报告；③ E2E 原生右键 popup 与 beforeunload 弹窗不可被 Playwright 驱动——已按计划降级处置（单测断言 handler），不得强行 E2E；④ Task 4 中 createEditorState handler 签名演进为 `onDocChanged(text, state)` 需同步 Task 3 测试（计划已注）。
- **下一步具体动作**: 新会话（工作目录 D:\code\project\LearningText，分支 dev）先 `git push origin dev` 推送 3 个文档提交；向用户确认 spec/计划评审结论——放行后加载 `superpowers:subagent-driven-development` 技能，读取 `docs/superpowers/plans/2026-09-18-M4-编辑器.md`，从 Task 1 起逐任务派发子代理（每任务两阶段评审全绿进下一任务）；若用户有书面修订意见，先落 spec/计划再执行。
- 验证方式: 计划各任务验证链全绿；Task 11 出口清单 `npm run lint && npm run format:check && npm run typecheck && npm run test:coverage && npm run build && npm run check:preload && npm run test:e2e && npm run package:dir` 全 0 退出；推送 dev 后 CI 三平台全绿。

## 3. 环境与全局约定 (Environment & Conventions)
- 运行时/工具版本: Windows 11 + Git Bash；Node 24.19.0（.nvmrc=24）；gh CLI 已登录 mwrforever（仓库所有者）；CodeMirror 包版本基线见计划 Tech Stack（2026-09-18 registry 实查）。
- 关键配置: 远程 `https://github.com/mwrforever/LearningText.git`；dev/main 均有分支保护（必需检查三平台 build；dev enforce_admins=false 管理员直推提示 bypass 属预期）；SSH 推送偶发 Connection reset，等 30s 重试即可。
- 构建/启动命令: 从零复现 `npm ci && npm run rebuild`（A.5-2 强制）；本机 npm allow-scripts 会拦 install 脚本；electron 下载慢用 `ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/`。
- 全局约定: 一切开发在 dev、main 生产分支项目落地后一次性合入；写代码前必读 AGENTS.md 宪法，修宪先记 CHANGELOG 再改正文；spec 落 `docs/superpowers/specs/`、计划落 `docs/superpowers/plans/`；每任务 Conventional Commits 中文、测试与实现同提交；**关键方案门 AskUserQuestion 用户未即时应答时按推荐自主裁决并完整留痕**（M1 e17da0f/M2 limit/M3 双机制与固定 host 四度先例）；**SDD 台账纪律**：`.superpowers/sdd/<plan>/progress.md` 逐任务登记 complete/fix round/minor deferred/裁决留痕（压缩后恢复的唯一事实源），工作区清理前把探针证据报告归档 `docs/progress/` 防悬空引用；**计划自查三铁律**——资源获取/释放成对断言、含 `\`/`</script>` 类字面量转录抽查、依赖浏览器/Electron/SQLite API 语义的结论必须探针实证（M3 三缺陷均首曝于 E2E：Node 单测不可替代 Chromium 探针）。
