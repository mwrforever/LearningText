# 任务交接文档 (Session Handoff)

> ⚠ **交接声明（务必先读）**：本文档由 `session-handoff` 技能生成，是新会话续接任务的**唯一事实来源**，内容仅来自当前会话的事实，自包含、不引用其他进度文档或历史会话记录。
> 「第 3 节 未完成」中的全部任务必须在【新会话】执行；生成本文档的会话已交接封存，不再实现任何任务。
> 新会话续接前必须完整读取本文档、以本文档为准，禁止凭空推测进度或重复已完成的工作。
> 代码与文件变更明细不在本文档记录，新会话用 `git diff` / `git log` 自行查看。

## 元信息 (Meta)
- workspace_dir: D:\code\project\LearningText
- saved_at: 2026-09-18（当日，update 刷新）
- 生成方式: update
- 版本控制提交号: dev 本地 cd83586（**含 4 个未推送提交**：6ba71ff 登记、eb2dc0c spec v0.1、1f16a8e spec v0.2、cd83586 plan+spec v0.3；远程 origin/dev 停在 7f462d2）
- 交接状态: 已交接（本会话封存，未完成部分须在新会话执行）
- 任务一句话描述: **M2 搜索已全量完成并验收（CI 三平台绿、已推送）**；M3 预览的 spec（v0.3）与 9 任务实施计划已定稿于本地 dev（未推送），**新会话直接按计划开工 M3**

## 1. 会话 Todo 追踪 (Session Todo)
- [x] 登记 M2 终审遗留项到 TASK.md 并提交
- [x] M3 预览 spec（v0.3）定稿并提交
- [x] writing-plans：M3 实施计划定稿（9 任务 TDD 粒度 + 自查）并提交
- [ ] 等待用户确认执行模式（SDD / inline），随后开工 M3

## 2. 已完成 (Completed) — 只做精简标记
- [x] **M2 搜索全量完成并通过验收**（SDD 10 任务 + 终审修复波，dev 4aead40..e612620 已推送，CI run 35267145394 三平台绿）：v2 trigram 迁移（换表+行数对账+父指针全量索引）、子树定位 parent_id 递归 CTE（回收站孪生树根治）、遗留三项收口、shared 契约/preload 桥、queryBuilder/snippetParser/searchService 双通道、IPC 接线、性能基准（实测 X′=343ms/P95=17.7ms/v2=24ms/冷启动=3ms，A.5-4 预算按公式上调 3500ms）、TASK/CHANGELOG 收尾、出口清单 8 命令全绿。
- [x] **M2 终审执行**（全分支 review：唯一 Important=limit>200 语义冲突，修复波 e612620 已闭环，七项 finding 复评全 ADDRESSED）：终审裁决记录与 deferred minors 见 CHANGELOG 与提交信息。
- [x] **M2 遗留项登记**（提交 6ba71ff）：4 条 deferred minors 按 M4 触发周期入 TASK.md（MatchIn z.enum 派生、filterFragments ≤2 值形态、搜索测试细化批次、片段高亮 toLowerCase 码元展开边界）。
- [x] **M3 spec 定稿**：`docs/superpowers/specs/2026-09-18-预览-design.md`（v0.3；用户书面评审确认八项裁决 D1–D8 + 落地评审修订指令：standard scheme 钉死、connect-src vfs:、CORS 通配、location.replace 禁 cache-busting、rev 防撕裂、schemaVersion 版本化、shell 插槽接缝、越界 404 无 clamp；v0.3 为计划自查精确化——越界双机制，见 §2.2-3）。
- [x] **M3 实施计划定稿**：`docs/superpowers/plans/2026-09-18-M3-预览.md`（9 任务、TDD 步骤粒度、含完整代码块与验证命令、无占位符；自查修正「先错后改」标注 ×5、越界测试断言 ×3 处、standard URL 归一语义错误）；**实现代码为零**。

## 3. 未完成 (Pending) — 精简且可执行（全部须在新会话执行）

### 3.1 执行 M3 预览实施计划（SDD 模式）
- 目标与当前状态: 按 `docs/superpowers/plans/2026-09-18-M3-预览.md` 完成 9 个任务，达成 M3 出口（Task 9 验收清单全绿 + CI 三平台）；计划与 spec 已定稿于本地 dev（4 个文档提交未推送），实现零。
- 任务清单: Task 1 rev 广播契约（transaction 自增+VfsChangedBroadcast 包装+preload/ipc/app 改型+既有测试连带）→ Task 2 最小设置通道（契约/服务/dataDir 层级/IPC/preload/装配）→ Task 3 vfsParse 纯函数 → Task 4 vfsProtocol handler+standard scheme 装配+index.html frame-src → Task 5 渲染层决策纯函数（refreshModel/treeModel/vfsUrl）→ Task 6 TreePanel+EditorPanel（jsdom+fake timers）→ Task 7 PreviewPanel+Workspace+App（订阅成对+shell 插槽）→ Task 8 E2E 七项验收+NFR-04 计时（独立临时 userData）→ Task 9 收尾登记（调研行/docs06 行闭环、M4 批次预留）+出口清单+推送 CI。
- 已锁定决策（新会话不得推翻）: spec 全部条款——尤其 D1–D8（路径式 URL、CSP 断外链+connect-src vfs:、P1 只设计、最小设置通道、最小工作台、ETag 弱校验+no-cache、单区间 206、sandbox allow-scripts）；用户评审修订指令（standard 钉死/replace 无 cache-busting/rev 防撕裂/schemaVersion/shell 插槽/越界无 clamp）；计划 Global Constraints 全部条款；覆盖率阈值核心 100%/全局 ≥80% 禁止下调。
- 阻塞点/风险: ① e2e `--user-data-dir` 生效性 Task 8 首日实测（计划已附后备注）；② rev 改型是跨里程碑接口变更——广播消费方一律 `{rev, event}` 形态（M4 遵循）；③ Task 7 PreviewPanel 订阅闭包已用 urlRef 终态钉死，勿再引入滞后帧问题；④ 计划各「实施者适配」注（既有测试样板对齐/契约键名）按注落地，不得自创键名；⑤ Electron 协议特权字段已经官方文档核实（standard/secure/supportFetchAPI/stream），装配以计划 Task 4 全文为准。
- **下一步具体动作**: 在新会话（工作目录 D:\code\project\LearningText，分支 dev）加载 superpowers:subagent-driven-development 技能，读取 `docs/superpowers/plans/2026-09-18-M3-预览.md`，从 Task 1 开始逐任务派发子代理执行（每任务两阶段评审，全绿才进下一任务）；开工首个提交前 `git push origin dev` 将本地 4 个文档提交推送。
- 验证方式: 计划各任务验证链全绿；Task 9 出口清单 `npm run lint && npm run format:check && npm run typecheck && npm run test:coverage && npm run build && npm run check:preload && npm run test:e2e && npm run package:dir` 全 0 退出；推送 dev 后 `gh run watch` 三平台全绿。

## 4. 环境与全局约定 (Environment & Conventions)
- 运行时/工具版本: Windows 11 + Git Bash；Node 24.19.0（.nvmrc=24）；gh CLI 已登录 mwrforever（仓库所有者）。
- 关键配置: 远程 `https://github.com/mwrforever/LearningText.git`；dev/main 均有分支保护（必需检查 `build (ubuntu/windows/macos-latest)`；dev enforce_admins=false 管理员直推时 remote 提示 bypass 属预期）。
- 构建/启动命令: 从零复现 `npm ci && npm run rebuild`（A.5-2 强制）；本机 npm allow-scripts 会拦 install 脚本（M6 前 `npm approve-scripts`）；electron 二进制下载慢用 `ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/`。
- 全局约定: 一切开发在 dev、main 生产分支项目落地后一次性合入；写代码前必读 AGENTS.md 宪法，修宪先记 CHANGELOG 再改正文；spec 落 `docs/superpowers/specs/YYYY-MM-DD-<主题>-design.md`、计划落 `docs/superpowers/plans/`；会话工作流 = 设计批准 → writing-plans → 执行不跳步，关键方案门用 AskUserQuestion（用户未即时应答时按推荐自主裁决并完整留痕，M1 e17da0f/M2 limit 先例）；每任务 Conventional Commits 中文、测试与实现同提交；**计划自查三铁律**（M1/M2 教训固化）——资源获取/释放成对（M1 db.close 缺漏）、代码块反斜杠字面量转录抽查（M2 `'\'` 笔误 ×3）、依赖 SQLite/Electron API 语义的 SQL/配置未实跑必错（M2 MATCH 列位/派生表重扫、M3 URL 归一），定稿前用探针或官方文档核实。
