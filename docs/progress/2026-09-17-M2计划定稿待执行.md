# 任务交接文档 (Session Handoff)

> ⚠ **交接声明（务必先读）**：本文档由 `session-handoff` 技能生成，是新会话续接任务的**唯一事实来源**，内容仅来自当前会话的事实，自包含、不引用其他进度文档或历史会话记录。
> 「未完成」章节中的全部任务必须在【新会话】执行；生成本文档的会话已交接封存，不再实现任何任务。
> 新会话续接前必须完整读取本文档、以本文档为准，禁止凭空推测进度或重复已完成的工作。
> 代码与文件变更明细不在本文档记录，新会话用 `git diff` / `git log` 自行查看。

## 元信息 (Meta)
- workspace_dir: D:\code\project\LearningText
- saved_at: 2026-09-23T13:50:00+08:00（update 刷新）
- 生成方式: update
- 版本控制提交号: HEAD=5d89f96（dev 已推 origin/dev 同步、工作区干净；本批三提交 = 29de488 fix(canvas) + 0dc8c0b fix(store) + 5d89f96 chore(release)；**tag v0.1.1 已打并公开发布**）
- 交接状态: 已交接（本会话封存，未完成部分须在新会话执行）
- 任务一句话描述: 四项历史前置项核验 + 用户复测反馈批次（①历史库 schema 收敛 v3 迁移修复回收站让名 ②画布文档面浏览器白底修「灰色遮罩」 ③原生弹窗统一应用内确认弹窗 ④树行操作入口常态可见）已全部交付并**公开发布 v0.1.1**（三平台产物 8 项）；新会话续接：用户安装实测与复测确认 → 存量打磨。

## 1. 会话 Todo 追踪 (Session Todo)
> 仅当 todo 列表存在未完成条目时固化本节；已全部完成时整节删除、后续章节编号顺延。
> 固化当前会话待办追踪列表在封存时刻的原样快照，只列条目，不加说明。
- [ ] 剩余（移交用户）：v0.1.1 安装包实测（从 Release 下载 `LearningText-Setup-0.1.1.exe` 安装并跑主链路）+ 本批四项修复的用户复测确认

## 2. 已完成 (Completed) — 只做精简标记
- [x] **本批主任务 · 四项历史前置修复项核验**（用户点名 2026-09-17 进度文档时代登记项）：①回收站同名阻拦 ②HTML 渲染灰色遮罩 ③删除+彻底删除后同名新建「无法操作」 ④新建目录失焦取消——②定位真因（Chromium 子文档透明画布使应用底色透出）并修 iframe 浏览器白底；①③ 当日随用户复测反馈修订结论（见下条）。
- [x] **本批主任务 · 用户复测反馈修复（真因＝历史库 schema）**：取证用户真实库副本发现 `node.virtual_path` 仍为**列级 UNIQUE**（2026-09-17 迁移**就地修改**对已建库无效）→ 回收站让名语义整条失效；新增 **v3 迁移**（表重建收敛到部分唯一索引 + AUTOINCREMENT 高水位回填）修复，集成测试 8 例 + 用户库副本真机验收全通。同批：4 处原生 `window.confirm` → 应用内 `ConfirmDialog`（弹窗样式反馈）、树行「⋯」常态可见（「操作键都没有」反馈）。
- [x] **发布 v0.1.1（公开发布，2026-09-23）**：CI 三平台绿（run 35822622309：ubuntu 2m50s / macos 3m11s / windows 4m0s）→ tag `v0.1.1` → Release 工作流三平台出包（run 35822947798）→ 产物 8 项（NSIS Setup + arm64 dmg + AppImage + blockmap + latest-*.yml）→ 本机 NSIS 打包冒烟（fuses 9/9 回读 + 打包产物对 legacy 库启动迁移实测：`user_version=3`、数据零丢失、迁移前自动滚动备份）→ `gh release edit --draft=false` 正式发布。v0.1.0 为未公开 draft（产物不含本批修复），保留待清理。
- [x] **测试基座修复**：`tests/e2e/app.spec.ts` 此前不带 `--user-data-dir`，本地全量 E2E 会读/写**开发者真实 userData**（本次实证：真实库被连带迁移）；已改独立临时目录，与其余 spec 同款纪律。
- [x] **M0–M7 与发布就绪批次均已完成验收**（历史事实；本会话未触碰其实现，含 fuses/release 工作流与三平台 draft 产物）。
- [x] **上一轮五项反馈修复 + E2E/CI 收敛 + 覆盖率回归补测**（历史事实；本会话未触碰其实现）。
- [x] **本会话主任务 · 差异①新建目录语义**：真机探针复现根因＝M7「失焦不取消」裁决使命名行失焦后永久滞留树中（用户所述「空目录长期处于新建态、再次同级新建仍落该已建目录」）→ 改**一次性收口**：失焦提交（空草稿＝取消）、失败 toast 并**同样关闭**命名行（避免焦点陷阱）、在途守卫 ref 化、IME 组合期守卫；空/非空目录呈现一致，重命名须显式走工具栏/行内菜单。旧裁决推翻留证见设计系统 §十-4。
- [x] **本会话主任务 · 差异②动效与微交互**：侧栏折叠/展开 180ms 宽度过渡（单元素常驻 + 退场存在性延迟卸载 + 双向焦点交接 + 装载期免动画门）、面板/设置页 100ms 入场、按钮按压双档（新建 `features/ui/classStrings.ts` 收敛 5 常量，替代 8 处重复定义）、拖拽三态、toast 退场 240ms；**推翻 D24/§九-8 两条「不做」裁决**（原裁决对象 grid 列模板自 M6 起已不存在，实测每帧布局 0.9–1.0ms 且与画布文档规模无关）；基建修复（删死 token `--duration-*`、补全局 ease-out 基线、退场类补终态保持）。
- [x] **双轮评审（taste 质量门 + 独立正确性审查）缺陷已全部修复复验**：typecheck 红（测试桩类型）、失败态焦点陷阱、在途守卫穿透、展开侧栏焦点丢失、reduced-motion 退场回弹滞留、装载期无动机动画、折叠态分隔条假亲和度、拖拽态色阶、拖拽卸载兜底、四处缺 ease-out。
- [x] **文档回写**：设计系统 §二/§三/§3.1/§6.1/§6.2/§6.4/§7.2/§7.3/§九-8/§十-4 + 新增 §十一（台账/基建/实测证据/不做清单）；布局蓝图 §2.3/§3；M5 打磨对照表 #5/#10/#11；CHANGELOG 登记本批；TASK.md 登记存量项（toast 宿主叠压）；本交接文档补验收处置记录。
- [x] **测试与门禁全绿**：单测 528 / 集成 135 / E2E 28 + `test:coverage` 阈值通过 + lint/format:check/typecheck；新增 E2E 侧栏几何与拖拽红线断言（含 pointercancel 守卫，经鉴别力验证）与失焦提交真机链。
- [x] **CI 三平台全绿**（dev，run 35811840218 / 35812211269：ubuntu 2m10s / windows 4m54s / macos 3m12s）。

## 3. 未完成 (Pending) — 精简且可执行（全部须在新会话执行）

### 3.1 v0.1.1 安装实测与用户复测（发版已完成，剩余为人工验证）
- 目标与当前状态: `v0.1.1` **已公开发布**（https://github.com/mwrforever/LearningText/releases/tag/v0.1.1，8 项产物）；应用侧修复已交付（旧库 schema 迁移 + 画布白底 + 弹窗统一 + 行入口可见化），CI 与 Release 工作流三平台全绿，本机打包产物启动迁移冒烟通过。
- 已锁定决策/阻塞风险: mac/win 安装包未签名（无证书期，macOS 首开需右键放行；仅 arm64）；asar 完整性 fuse 与签名 secrets 随证书就绪启用（TASK.md 已登记）；v0.1.0 draft 保留未删（产物不含本批修复，清理需用户决定）。
- **下一步具体动作**: ①从 Release 下载 `LearningText-Setup-0.1.1.exe` 安装（向导可选目录）并跑通主链路（新建/导入/编辑/保存/搜索/回收站/设置）；②复测本批四项修复（删除后同名新建不再被拦、无灰色遮罩、弹窗为应用内样式、目录行「⋯」常驻可见）；③如仍见「目录无法操作」请附操作序列与截图（本轮已按服务层逐步对账未复现，判定为呈现层误判）。
- 验证方式: 安装向导完成、安装后应用可启动且主链路可用；四项修复复测无回归；回收站「删除→同名重建」不再出现「同级已存在同名文件或文件夹」。

### 3.2 存量打磨与接线（低优先级）
- 目标与当前状态: TASK.md「执行项登记」存量待办沿用原登记；本会话新增一条——toast 宿主 `bottom-4` 与状态栏按钮 / 画布浮动钮叠压（`z-50` + `pointer-events-auto` 吞掉约 6–10px 命中区，存量缺陷非本批引入）。
- 已锁定决策/阻塞风险: 该项须与进度面板 `bottom-14` 协同上移，属呈现面位移改动；TASK.md 另有「多层后代 meta 不随单条 renamed/moved 广播刷新」等存量观察。
- **下一步具体动作**: 逐条读 TASK.md 对应行按「触发时机」排序实施；每项独立提交、测试与实现同提交。
- 验证方式: 每项按其红线/用例验证；推送前 `npm test` + **`npm run test:coverage`** 双绿（覆盖率门禁只见于 CI，本地不跑会翻车）。

## 4. 环境与全局约定 (Environment & Conventions)
- 运行时/工具版本: Windows 11 + Git Bash；Node 24.19.0（.nvmrc=24）；Tailwind 4.3.x / shadcn CLI 4.21.0（**CLI 不可用**，组件手工落 registry 模板）/ cmdk 1.1.1 / radix-ui 1.6.7 / lucide-react 1.47.0 / tw-animate-css 1.4.0 / @electron/fuses 2.1.3。
- 关键配置: 远程 `https://github.com/mwrforever/LearningText.git`；dev/main 均分支保护（dev enforce_admins=false，管理员直推 bypass 提示属预期）；SSH / gh API 偶发 Connection reset，30s 重试即过；Electron 下载慢用 `ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/`；CI 安装依赖设 `HUSKY=0`。
- 构建/启动命令: 从零复现 `npm ci && npm run rebuild`；开发 `npm run dev` + 另窗 `npm run start:dev`；生产验证 `npm run build && npm start` 或 `scripts/start.bat`（Windows）/`bash scripts/start.sh`；全量测试 `npm test`（unit+integration+e2e 串行，E2E globalSetup 自动全量构建）；覆盖率门禁 `npm run test:coverage`；打包 `npm run package`（fuses 翻转已挂 afterPack，产物在 release/）。
- 全局约定: 写代码前必读 AGENTS.md 宪法（含 C.7 谋建琢三段律；UI 工作 subagent 须加载 @ui-ux-pro-max 与 @taste-skill）；修宪先记 CHANGELOG 再改正文；需求变更先改 docs/03；spec 落 `docs/superpowers/specs/`、计划落 `docs/superpowers/plans/`、设计落 `docs/design/`、进度证据落 `docs/progress/`；每任务 Conventional Commits 中文、测试与实现同提交；**关键方案门 AskUserQuestion 未即时应答时按推荐自主裁决并完整留痕**；**E2E 锚点契约**：`lt-*` 类 / aria-label / 中文文案是测试锚点，改 UI 必须同步测试；**zh-CN Windows 下 .bat 消息必须 ASCII**；依赖平台语义的结论必须探针实证（本机全绿不可替代 CI 三平台矩阵）。
- 本批实证教训（新增）: ① **迁移脚本一经发布不可就地修改**——对已建库无效（user_version 已推进、迁移不重放），表结构变更一律追加新迁移（本次缺陷根因即 2026-09-17 的就地修改）；② **探针必须在用户真实数据形态上跑**——全新库会让 schema 漂移类缺陷整体隐形（据新库得出的「不可复现」会被用户复测直接推翻），涉及持久化语义的用户反馈先取真实库副本只读对账；③ E2E spec 必须自带 `--user-data-dir`（app.spec 曾缺失，本地全量跑会连带迁移开发者真实库）；④ 打包产物须做「legacy 库启动迁移」冒烟（dist 形态通过不代表 asar 打包形态通过）。
- 上批实证教训: ① Tailwind v4 的 `group-data-[x=y]/name:transition-none` **静默不生成规则**（同名变体挂 `pointer-events-none` 却正常生成）——类串能力必须对构建产物 grep 核证；② `animate-out` 的 fill-mode 为 none，reduced-motion 把动画压到 0.01ms 后会**回弹成不透明并滞留整段 JS 计时**——退场一律带终态保持（transition 终态或 `fill-mode-forwards`）；③ 连点反向重启动画会从关键帧极值重播而闪断（animate-in/out 是两个动画名），需要续走的动效走 CSS transition；④ `createRoot(document.body)` 会清空 body 既有子元素并摘除同测试的容器，脱离文档的输入框 `focus()` 是空操作（失焦链静默不触发）——测试宿主须自持 div；⑤「失败态保留 + 重新聚焦」会变成焦点陷阱（用户离开后每次点击重发失败请求并抢回焦点）——失败应当收口而非滞留；⑥ state 化的在途守卫在同一任务内连发时读旧值会穿透，守卫用 ref；⑦ 新增守卫断言应临时移除被守卫的代码验证它会红（本会话对 pointercancel 实测）；⑧ 真机探针（Playwright 驱动 Electron）是复现「交互语义类」用户反馈的最短路径——jsdom 的焦点时序不可信。
