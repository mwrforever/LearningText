# 任务交接文档 (Session Handoff)

> ⚠ **交接声明（务必先读）**：本文档由 `session-handoff` 技能生成，是新会话续接任务的**唯一事实来源**，内容仅来自当前会话的事实，自包含、不引用其他进度文档或历史会话记录。
> 「第 3 节 未完成」中的全部任务必须在【新会话】执行；生成本文档的会话已交接封存，不再实现任何任务。
> 新会话续接前必须完整读取本文档、以本文档为准，禁止凭空推测进度或重复已完成的工作。
> 代码与文件变更明细不在本文档记录，新会话用 `git diff` / `git log` 自行查看。

## 元信息 (Meta)
- workspace_dir: D:\code\project\LearningText
- saved_at: 2026-09-17（当日）
- 生成方式: rewrite
- 版本控制提交号: dev 分支 af2d3e5（含本交接文档提交，已推送 origin）
- 交接状态: 已交接（本会话封存，未完成部分须在新会话执行）
- 任务一句话描述: M1 已全量完成并验收（CI 三平台绿）；M2 搜索的 spec 与 10 任务实施计划已定稿于 dev 分支，**新会话直接按计划开工 M2**

## 1. 会话 Todo 追踪 (Session Todo)
> 封存时刻本会话 todo 列表已全部完成、无待固化条目，按要求不另行快照。

## 2. 已完成 (Completed) — 只做精简标记
- [x] **M1 存储引擎 + VFS 全部完成并通过验收**（SDD 13 任务 + 终审修复波，dev edcf73d..9267670 已推送）：前置修复两项、shared 契约扩展、迁移体系/事务/错误映射、VFS 十方法、IPC 十通道接线、性能基准、B.1/C.4 修宪回填；终审发现并修复「退出前 db.close() 缺失」与「子树路径谓词与回收站孪生树冲突（数据丢失级）」；覆盖率核心与全局均 100%；CI run 35152148944 三平台绿。
- [x] **需求基线修订 `virtual_path` 改部分唯一索引**（决策门用户未应答后按推荐自主裁决，CHANGELOG 先记后改，提交 e17da0f）：软删行让出路径，支撑回收站让名/还原撞名语义。
- [x] **M2 搜索 spec 定稿评审通过**：`docs/superpowers/specs/2026-09-17-搜索-design.md`（v1.0，用户确认 4 项语义门 + 迁移策略 A + 遗留项纳入范围；§9 十一条裁决），TASK.md 待撰写 docs/05 行已删（提交 f44bcb9、96da745）。
- [x] **M2 实施计划定稿**：`docs/superpowers/plans/2026-09-17-M2-搜索.md`（10 任务、TDD 步骤粒度、含完整代码块与验证命令、无占位符；自查修正 v2 对账排除根节点/trigram 边界用例/tie-break 确定性等 5 处；提交 af2d3e5）；**实现代码为零**。

## 3. 未完成 (Pending) — 精简且可执行（全部须在新会话执行）

### 3.1 执行 M2 搜索实施计划（SDD 模式）
- 目标与当前状态: 按 `docs/superpowers/plans/2026-09-17-M2-搜索.md` 完成 10 个任务，达成 M2 出口（计划 Task 10 验收清单全绿 + CI 三平台）；计划已定稿，实现零。
- 任务清单: Task 1 v2 迁移（node_fts 换 trigram 复制换表+行数对账排除根+idx_node_parent_all）→ Task 2 子树定位 parent_id 递归 CTE（替换 10 处路径谓词、撤除状态锚定、双回收站树回归）→ Task 3 M1 遗留三项收口（ListChildren strictObject、moveNode 同父短路、restoreNode 值合并新鲜 updatedAt）→ Task 4 shared 搜索契约+preload 桥 → Task 5 queryBuilder 纯函数 → Task 6 snippetParser 纯函数 → Task 7 searchService 双通道 → Task 8 IPC 接线+装配注入 → Task 9 文件库性能基准+A.5-4 复核回填 → Task 10 收尾登记+验收推送。
- 已锁定决策（新会话不得推翻）: spec 2026-09-17-搜索-design.md 全部条款（尤其 §9 十一条：空格 AND+引号短语、bm25 name10:body1 加 updated_at DESC/id ASC tie-break、纯文本+UTF-16 码元区间零 HTML、v2 复制换表方案 A、不新增错误码、filters M2 实现、词项上限 8、LIKE 回退不承诺 P95）；计划 Global Constraints 全部条款；覆盖率阈值核心 100%/全局 ≥80% 禁止下调；**v2 落地后既有迁移只追加不回改**。
- 阻塞点/风险: Task 2 是行为等价大重构——既有 M1 全部测试应零修改通过（仅新增用例是增量），若有用例红按「等价性破坏」回查而非改用例；Task 7 双通道 SQL 为动态形态（结构键惰性预编译缓存是 A.4-5 收口例外，计划已注明理由勿删）；Task 9 四项实测为数据依赖步骤（推导公式已给死，禁止挑好看采样，3 次取中位数如实报告）；electron 二进制下载慢用 `ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/`。
- **下一步具体动作**: 在新会话（工作目录 D:\code\project\LearningText，分支 dev）加载 superpowers:subagent-driven-development 技能，读取 `docs/superpowers/plans/2026-09-17-M2-搜索.md`，从 Task 1 开始逐任务派发子代理执行（每任务两阶段评审，全绿才进下一任务）。
- 验证方式: 计划各任务验证链全绿；Task 10 出口清单 `npm run lint && npm run format:check && npm run typecheck && npm run test:coverage && npm run build && npm run check:preload && npm run test:e2e && npm run package:dir` 全 0 退出；推送 dev 后 `gh run watch` 三平台全绿。

## 4. 环境与全局约定 (Environment & Conventions)
- 运行时/工具版本: Windows 11 + Git Bash；Node 24.19.0（.nvmrc=24）；gh CLI 已登录 mwrforever（仓库所有者）。
- 关键配置: 远程 `https://github.com/mwrforever/LearningText.git`；dev/main 均有分支保护（必需检查 `build (ubuntu/windows/macos-latest)`；dev enforce_admins=false 管理员直推时 remote 提示 bypass 属预期）。
- 构建/启动命令: 从零复现 `npm ci && npm run rebuild`（A.5-2 强制）；本机 npm allow-scripts 会拦 install 脚本（electron-winstaller 遗留 M6 前 `npm approve-scripts`）。
- 全局约定: 一切开发在 dev、main 生产分支项目落地后一次性合入；写代码前必读 AGENTS.md 宪法，修宪先记 CHANGELOG 再改正文；spec 落 `docs/superpowers/specs/YYYY-MM-DD-<主题>-design.md`、计划落 `docs/superpowers/plans/`；会话工作流 = 设计批准 → writing-plans → 执行不跳步，关键方案门用 AskUserQuestion；每任务 Conventional Commits 中文、测试与实现同提交；**计划拆任务自查须核对「资源获取/释放成对」**（M1 的 db.close 静默缺漏教训：spec→plan→五轮评审均未抓到，终审兜底）。
