# 任务交接文档 (Session Handoff)

> ⚠ **交接声明（务必先读）**：本文档由 `session-handoff` 技能生成，是新会话续接任务的**唯一事实来源**，内容仅来自当前会话的事实，自包含、不引用其他进度文档或历史会话记录。
> 「未完成」章节中的全部任务必须在【新会话】执行；生成本文档的会话已交接封存，不再实现任何任务。
> 新会话续接前必须完整读取本文档、以本文档为准，禁止凭空推测进度或重复已完成的工作。
> 代码与文件变更明细不在本文档记录，新会话用 `git diff` / `git log` 自行查看。

## 元信息 (Meta)
- workspace_dir: D:\code\project\LearningText
- saved_at: 2026-09-23T09:35:00+08:00（update 刷新）
- 生成方式: update
- 版本控制提交号: HEAD=a8dbca1（dev 已推 origin/dev 同步；本次交接文档更新为工作区唯一改动，随即提交推送）
- 交接状态: 已交接（本会话封存，未完成部分须在新会话执行）
- 任务一句话描述: 交接 5 项待办中 ①E2E 收敛 ②分批提交 ③用户五项反馈修复（fix+review loop）④打包发布（fuses+release 工作流+NSIS 冒烟+tag v0.1.0→三平台 draft release 全绿）全部完成；新会话续接：draft 人工发布闸门 → 用户亲测验收 → 存量打磨。

## 1. 会话 Todo 追踪 (Session Todo)
> 仅当 todo 列表存在未完成条目时固化本节；已全部完成时整节删除、后续章节编号顺延。
> 固化当前会话待办追踪列表在封存时刻的原样快照，只列条目，不加说明。
- [x] 2.1 E2E 收敛：全量 npm test 绿 + feat/docs 提交推送
- [x] 2.3 五项反馈批次：实现 ✓ 两轮审查 ✓ 全量绿 ✓ 提交推送
- [x] 2.3/2.4 CI 修复：dev 三平台绿（run 35774151348）
- [x] 2.4 发布就绪：fuses + afterPack + release.yml + NSIS 冒烟（cc171c3）
- [x] 2.4 发版：tag v0.1.0 → release 三平台全绿 + draft release 三产物就绪
- [x] 会话收尾：交接文档 + 记忆已刷新提交推送（a8dbca1）
- [ ] 2.5 剩余：draft 人工发布闸门 + 用户亲测验收（移交用户）

## 2. 已完成 (Completed) — 只做精简标记
- [x] **M0–M6 均已完成验收**（历史事实；本会话未触碰其实现）。
- [x] **①E2E 收敛**: 真实根因=树根层命名行双重渲染（M7 引入，合成根挂载即 loaded 使顶层分支与树内分支同时出行）——删顶层死分支；preview.spec fixture 路径修正为 `../fixtures` + M7 用例挪至文件末尾消除 iframe strict 连锁；全量 `npm test` 绿（unit 511 / integration 135 / e2e 26+1flaky）。
- [x] **②工作区分批提交**: feat(m7) 主体 1c9ca67 + docs(m7) a0d166e；`.impeccable/` 判定为本机设计 hook 缓存，入 .gitignore。
- [x] **③用户五项反馈修复**（批次 a8ef7b2，两项 subagent 审查通过）: ①导入进度收口+240ms 退场动画+竞态复位（新增 ioProgressPresence.ts）；②隐藏树「根」行+`lt-tree-root-path` 路径小字+三 spec 装配信号换锚 `nav[data-ready]`；③命名聚焦语义核查无偏差；④目录点选=选中+展开折叠（reveal 记账，新建/导入落点随点选）；⑤expandedRef 实时镜像+折叠真正收起子级（修复 M3 起「装载即恒可见」存量缺陷）。
- [x] **随批修复两处 E2E 实证缺陷**: renamed/moved 广播连带旧父层 stale（新增 markStaleAround + getNode 续体按新鲜 meta.parentId 标新父 + expanded 种子含根）；guard 用例折叠侧栏遗留态先条件展开（对照实验单跑即过实证）。
- [x] **④CI 三平台绿**（run 35774151348，4m31s/3m8s/2m35s）: 中途两次红已根因修复——mac 静态门禁=测试夹具入 .prettierignore（用户真实文档样本保真）；windows=性能基线两用例显式 timeout 30s/20s（自身预算>vitest 默认 5s）+ 覆盖率回归补测 9dfb70e（io:pick-file 供给闭包 / importService 源根竞态 catch 分支 / preload pickHtmlFile 包装通道，本地 test:coverage 全绿复验）。
- [x] **⑤发布就绪**（cc171c3，subagent 实现/审查闭环）: fuses 清单调研落地（四攻击面位关、asar 完整性双位随证书缓启）+ afterPack 接 @electron/fuses 2.1.3（`scripts/flip-fuses.mjs` 回读断言+`--check`）+ `.github/workflows/release.yml`（tag `v*` 三平台 matrix→draft，含 tag/版本一致性守卫）+ 本机 NSIS 118MB 冒烟 9/9 位复核通过；TASK.md 待调研项与 release 行关闭。
- [x] **⑥发版终态**: release 工作流 run 35774698464 三平台全绿（mac 首轮 artifact 旁路上传 ETIMEDOUT，`gh run rerun --failed` 即过，属瞬时网络）；draft release `v0.1.0` 含 8 资产：Setup-0.1.0.exe（NSIS）/ 0.1.0-arm64.dmg / 0.1.0.AppImage + 各 blockmap + latest-*.yml 自动更新清单。

## 3. 未完成 (Pending) — 精简且可执行（全部须在新会话执行）

### 3.1 v0.1.0 发版收尾（人工闸门 + 安装实测）
- 目标与当前状态: 产物与工作流已全部就绪（draft release `v0.1.0` 三平台包齐备，见上⑥）；剩人工发布动作与安装实测未做。
- 已锁定决策/阻塞风险: draft 是人工闸门（禁止无人确认即公开发布）；mac 仅 arm64 dmg（Intel 包缺失属已知边界，matrix 架构扩展归 docs/09 版本策略）；asar 完整性 fuse 与签名 secrets 随证书就绪启用（TASK.md 已登记）。
- **下一步具体动作**: ①提醒用户从 draft 下载 `LearningText-Setup-0.1.0.exe` 本机安装（向导可选目录）并跑通主链路；②确认无误后由用户执行 `gh release edit v0.1.0 --draft=false` 正式发布（或用户明确授权后由会话执行）。
- 验证方式: 安装向导完成、安装后应用可启动且主链路（新建/导入/编辑/保存/搜索/回收站/设置）可用；发布后 `gh release view v0.1.0` 非 draft。

### 3.2 用户亲测验收（交接原项）
- 目标与当前状态: 五项反馈修复+E2E 修复+发版产物就绪后，引导用户以 `scripts\start.bat` 启动逐项复核。
- 已锁定决策/阻塞风险: E2E 锚点契约（`lt-*` 类、aria-label、中文文案）改动必须同步测试——验收若引发 UI 改动按此纪律走。
- **下一步具体动作**: 逐项对照复核：①导入成功后进度提示消失（带滑出动画）；②树无「根」行、工具栏下路径小字；③命名行仅新建目录时自动聚焦；④点选目录后新建/导入落该目录；⑤快速连点收起/展开即时响应；外加主链路回归（新建/导入/编辑/保存/搜索/回收站/设置）。
- 验证方式: 用户逐项确认或差异登记 TASK.md（差异条目按缺陷流程修复）。

### 3.3 存量打磨与接线（低优先级）
- 目标与当前状态: TASK.md「执行项登记」约 25+ 条待办沿用原登记；本会话新增两条——多层后代 meta 不随单条 renamed/moved 广播刷新（树同步打磨批次）；package.json `release` 脚本（M0 遗留 `-mwl`，与 release.yml 语义冲突）待 docs/09 裁决去留。
- **下一步具体动作**: 逐条读 TASK.md 对应行按「触发时机」排序实施；每项独立提交、测试与实现同提交。
- 验证方式: 每项按其红线/用例验证；推送前 `npm test` + **`npm run test:coverage`** 双绿（覆盖率门禁只见于 CI，本地不跑会翻车——本会话实证）。

## 4. 环境与全局约定 (Environment & Conventions)
- 运行时/工具版本: Windows 11 + Git Bash；Node 24.19.0（.nvmrc=24）；Tailwind 4.3.x / shadcn CLI 4.21.0（**CLI 不可用**——组件手工落 registry 模板）/ cmdk 1.1.1 / radix-ui 1.6.7 / lucide-react 1.47.0 / one-dark 6.1.3 / @electron/fuses 2.1.3。
- 关键配置: 远程 `https://github.com/mwrforever/LearningText.git`；dev/main 均分支保护（dev enforce_admins=false 管理员直推 bypass 提示属预期）；SSH / gh API 偶发 Connection reset 等 30s 重试（本会话 release 监控撞到一次，重试即过）；Electron 下载慢用 `ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/`；CI 安装依赖设 `HUSKY=0`。
- 构建/启动命令: 从零复现 `npm ci && npm run rebuild`；开发 `npm run dev` + 另窗 `npm run start:dev`；生产验证 `npm run build && npm start` 或 `scripts/start.bat`（Windows）/`bash scripts/start.sh`；全量测试 `npm test`（unit+integration+e2e 串行，E2E globalSetup 自动全量构建）；覆盖率门禁 `npm run test:coverage`；打包 `npm run package`（fuses 翻转已挂 afterPack，产物在 release/）。
- 全局约定: 写代码前必读 AGENTS.md 宪法（含 C.7 谋建琢三段律；UI 工作 subagent 必须加载 @ui-ux-pro-max 与 @taste-skill）；修宪先记 CHANGELOG 再改正文；需求变更先改 docs/03；spec 落 `docs/superpowers/specs/`、计划落 `docs/superpowers/plans/`、设计落 `docs/design/`、进度证据落 `docs/progress/`；每任务 Conventional Commits 中文、测试与实现同提交；**关键方案门 AskUserQuestion 未即时应答时按推荐自主裁决并完整留痕**；**E2E 锚点契约**：`lt-*` 类/aria-label/中文文案是测试锚点，改 UI 必须同步测试；**zh-CN Windows 下 .bat 消息必须 ASCII**；依赖平台语义的结论必须探针实证（本机全绿不可替代 CI 三平台矩阵）。
- 本会话实证教训: ① `npm test 2>&1 | tail` 等管道吞测试退出码，门禁判定必须看完整输出；② Playwright spec 内 `__dirname` 相对测试文件真实目录（tests/e2e 取 fixture 用 `../fixtures`）；③ jsdom 焦点时序伪影——「失焦即取消」交互在 jsdom 下不可靠，用 Enter/Esc 显式收口（CreateDirRow 裁决留证设计系统 §十-4）；④ vitest 性能类用例自身预算>默认 5s 必须显式 timeout；⑤ 推送前必跑 `npm run test:coverage`（unit/integration 绿 ≠ 覆盖率阈值过）；⑥ 测试夹具=用户真实文档样本，入 .prettierignore 保真；⑦ E2E「flaky」先对照实验单跑定位前序用例遗留的全局 UI 态（guard 折叠侧栏实证）；⑧ subagent 报告中的 API 名以其本地源码核证为准，不凭记忆纠正。
