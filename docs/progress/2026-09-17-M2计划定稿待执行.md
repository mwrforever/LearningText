# 任务交接文档 (Session Handoff)

> ⚠ **交接声明（务必先读）**：本文档由 `session-handoff` 技能生成，是新会话续接任务的**唯一事实来源**，内容仅来自当前会话的事实，自包含、不引用其他进度文档或历史会话记录。
> 「未完成」章节中的全部任务必须在【新会话】执行；生成本文档的会话已交接封存，不再实现任何任务。
> 新会话续接前必须完整读取本文档、以本文档为准，禁止凭空推测进度或重复已完成的工作。
> 代码与文件变更明细不在本文档记录，新会话用 `git diff` / `git log` 自行查看。

## 元信息 (Meta)
- workspace_dir: D:\code\project\LearningText
- saved_at: 2026-09-22T20:39:15+0800（update 刷新）
- 生成方式: update
- 版本控制提交号: dev 本地与 origin/dev 同步于 bc25492（**已推送**；CI 三平台全绿 run 35654051105：ubuntu 1m51s / windows 4m43s / macos 2m26s；含前两轮 mac 修复 1499890、bc25492）
- 交接状态: 已交接（本会话封存；**M6 无未完成实现项**）
- 任务一句话描述: **M6「产品化重构」里程碑全部完成并通过三平台 CI**（VS Code 式壳层 + 所见即所得 + 数据目录迁移 + 安装位置可选 + 启动脚本）——剩余未完成项为 M6 发布批次（应用图标 / fuses 清单 / release 工作流 / 安装包实装手测 / 签名与许可证决策）与用户亲测验收

## 1. 已完成 (Completed) — 只做精简标记
- [x] **M0–M5 均已完成验收**（历史事实，证据归档于 docs/progress/ 各 M5 报告；本会话未触碰其实现）。
- [x] **M6 批次⓪ 基线与蓝图**：宪法 A.2-1 修订（数据根可改、userData 直下唯一指针文件 `data-dir.json`）、docs/01/03 需求基线修订（WYSIWYG/布局/数据位置/安装位置），M6 spec（12 条裁决）与布局蓝图定稿；提交 4da1246。
- [x] **M6 批次① 壳层重构**：自绘标题栏（titleBarStyle hidden + titleBarOverlay，平台探针实证后落地）+ 应用内菜单（手拷 shadcn menubar）+ 活动栏 + 侧栏（树/搜索/回收站）+ 图标化标签栏（设置=伪标签，不占 MAX_TABS）+ 状态栏（保存态/文档数/主题循环）+ 欢迎页 + settings schema v3→v4（shell.layout 改侧栏形态）+ `vfs:count` 通道；提交 dca5bf6。
- [x] **M6 批次② 所见即所得**：HtmlCanvas（每 HTML 标签保活沙箱 iframe）+ 协议注入编辑桥（data-lt-injected 标记、**逐输入即时**序列化上报）+ 保存管线零改动复用（画布缓存→SaveController）+ 刷新抑制（结构性：无写后重载）+ MediaCanvas（媒体开标签直载）+ 滚动同步/PreviewPanel/refreshModel/D20 双源整体退役；提交 26aaed0 + 1b5c9ed。
- [x] **M6 批次③ 数据目录与打包**：指针文件读写 + 迁移服务（校验→关库→复制→写指针→重启，失败不落地 D9）+ `storage:get-info`/`storage:change-data-dir`（登记簿信任边界）+ 设置页「数据与存储」分区（打开目录/更改位置强确认弹层）+ NSIS `oneClick:false`+`allowToChangeInstallationDirectory` + package.json author；提交 26aaed0/09584b6。
- [x] **M6 批次④ 启动脚本 + E2E + 打磨**：`scripts/start.sh`/`start.bat`（实跑全链通过）；E2E 全量改写实跑收敛 26 例全绿；琢段 taste 打磨（10 项纯呈现 + 设计系统文档同步）；出口全绿（单测 505 / 集成 133 / E2E 26 / coverage 三域 100% / typecheck+lint 零告警 / package:dir 冒烟通过）；提交 1b5c9ed + 09584b6。
- [x] **两处产品缺陷经 E2E 实证修复**：侧栏分隔条 0px 命中区（补 `w-1 shrink-0`）；注入桥 iframe 内 setTimeout 被渲染器搁置致上报尾部丢失（改逐输入即时上报）。
- [x] **CI 三平台绿 + mac 两轮修复**：overlay 断言平台条件化；overlay 用例显式钉 win32 闭合 `app.ts:64` 平台三元分支覆盖率。

## 2. 未完成 (Pending) — 精简且可执行（全部须在新会话执行）

### 2.1 M6 发布批次（打包发布最后一公里）
- 目标与当前状态: 让产品可发布——应用图标（build/ 无图标，当前 electron-builder 用默认 Electron 图标）、fuses 关闭清单与实际落地、release 发布工作流（tag 触发、draft 人工闸门、签名 secrets 占位）、三平台安装包实装手测（安装位置选择、数据目录迁移、主链路）；数据侧与代码侧均已就绪（见第 1 节），TASK.md「执行项登记/待调研/待决策」表中与本批次相关的条目为唯一清单来源。
- 已锁定决策/阻塞风险: fuses 清单必须先调研再落地（TASK.md 待调研项，宪法 B.5-8）；macOS 签名/公证与开源许可证属用户待决策项（无证书先发未签名包）；electron-builder v27 将更名 `electronDownload`→`electronGet`（升级预警，C.6-11）。
- **下一步具体动作**: 先做 fuses 调研并形成清单（输出到 TASK.md 或 docs/agmds-research/ 既有报告体系），按清单在 electron-builder 落地；随后补 `build/` 图标资源（建议 icon.ico 256px 起步）；再实现 release 工作流（`.github/workflows/` 下 tag 触发 + draft）。
- 验证方式: `npm run package` 三平台产物可安装；安装向导可选安装位置；安装后主链路手测（新建→编辑→保存→重启→数据目录迁移）通过；release 工作流以 test tag 跑通 draft 产出。

### 2.2 用户亲测验收（UI 与交互）
- 目标与当前状态: 用户尚未亲自运行验收（全部自动化门禁已绿：本机 + 三平台 CI）；产物已就绪。
- 已锁定决策/阻塞风险: 无（若发现问题按缺陷流程登记 TASK.md，改动须保 E2E 锚点契约——`lt-*` 类、aria-label、中文文案是测试锚点）。
- **下一步具体动作**: 双击 `scripts\start.bat`（或 bash 环境 `bash scripts/start.sh`）启动生产形态应用，按用户预期逐项验证：VS Code 式布局/图标/所见即所得编辑（直接改文字、删元素保存后永久移除）/设置页数据与存储迁移/欢迎页/菜单项。
- 验证方式: 亲测逐项通过或记录差异；有差异转 TASK.md 缺陷条目交后续批次。

### 2.3 存量打磨与接线（minor 项批次）
- 目标与当前状态: TASK.md「执行项登记」表中 deferred 条目（约 25 条，含还原失败半开改 relaunch、persistLayout 裸写入队、mime 表补 `.ogg`、searchService `filterFragments` 白名单形态、A.5-4 预算分档、搜索索引重建修复例程接线——M6 已按用户需求移除设置页占位按钮，接线项保留）。
- 已锁定决策/阻塞风险: M6 数据目录迁移已按「关库后失败=清理+重启走旧指针」语义落地，可作为「还原失败半开改 relaunch」的同向先例。
- **下一步具体动作**: 逐条读 TASK.md 对应行并按「触发时机」列排序实施；每项独立提交、测试与实现同提交。
- 验证方式: 每项按其描述的红线/用例验证；全量门禁 `npm test` 保持绿。

## 3. 环境与全局约定 (Environment & Conventions)
- 运行时/工具版本: Windows 11 + Git Bash；Node 24.19.0（.nvmrc=24）；Tailwind 4.3.x / shadcn CLI 4.21.0（**CLI 不可用**——npm allow-scripts 拦 install 脚本，组件一律手工落 registry 模板）/ cmdk 1.1.1 / radix-ui 1.6.7 / lucide-react 1.47.0 / one-dark 6.1.3（M6 实装锁定）。
- 关键配置: 远程 `https://github.com/mwrforever/LearningText.git`；dev/main 均分支保护（dev enforce_admins=false 管理员直推提示 bypass 属预期）；SSH 推送偶发 Connection reset 等 30s 重试；Electron 下载慢用 `ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/`；CI 安装依赖设 `HUSKY=0`。
- 构建/启动命令: 从零复现 `npm ci && npm run rebuild`；开发 `npm run dev` + 另窗 `npm run start:dev`；生产验证 `npm run build && npm start` 或直接 `scripts/start.bat`（Windows）/`bash scripts/start.sh`；全量测试 `npm test`（unit+integration+e2e 串行，E2E globalSetup 自动全量构建）。
- 全局约定: 写代码前必读 AGENTS.md 宪法（含 C.7 谋建琢三段律；UI 工作 subagent 必须加载 @ui-ux-pro-max 与 @taste-skill）；修宪先记 CHANGELOG 再改正文；需求变更先改 docs/03；spec 落 `docs/superpowers/specs/`、计划落 `docs/superpowers/plans/`、设计落 `docs/design/`、进度证据落 `docs/progress/`；每任务 Conventional Commits 中文、测试与实现同提交；**关键方案门 AskUserQuestion 未即时应答时按推荐自主裁决并完整留痕（六度先例）**；**E2E 锚点契约**：`lt-*` 类/aria-label/中文文案是测试锚点，改 UI 必须同步测试；**zh-CN Windows 下 .bat 消息必须 ASCII**（chcp 65001 + UTF-8 中文会解析失步，实跑实证）；依赖平台语义的结论必须探针实证（Windows 本地全绿不可替代 CI 三平台矩阵，本会话 mac 覆盖率两轮修复即为例证）。
